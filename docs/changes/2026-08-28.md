# 2026-08-28

## Fixed ADR-096's registration guardrail: cardinality check now covers `Equality`, not just `Range`

Found while reviewing Vitals/Meridian (the two proving-ground domains)
for real `x-masking-searchable` candidates the previous session — a
genuine gap in already-shipped, "Done"-marked code: `MaskingSchemaValidator.
ValidateSearchableConfig` only required a `cardinality` hint for `Range`-
kind fields. But [Naveed/Kamara/Wright, CCS 2015](https://www.microsoft.com/en-us/research/publication/inference-attacks-property-preserving-encrypted-databases/)
— the same paper the guardrail already cites — names **deterministic
encryption** as frequency-analysis-vulnerable, and a blind `Equality`
index is deterministic encryption by definition (same plaintext, same
ciphertext, always). A `Low`-cardinality classified field (Meridian's
`MatchedListEntryId` — a bounded, enumerable sanctions-list domain) could
register `Equality` with zero guardrail coverage before this fix.

- `src/EventStore.SchemaRegistry/MaskingSchemaValidator.cs`: the
  cardinality/`acknowledgeLeakageRisk` check now runs for `indexKind is
  "Equality" or "Range"`, not `"Range"` alone; `bucketGranularities` stays
  required for `Range` only.
- `docs/adrs/adr-096-searchable-blind-index-bucketed-range.md` and
  `docs/data/schema-registry.md` corrected in the same pass (additive —
  states the original Range-only scope and why it was widened, not a
  silent rewrite).
- New regression tests in `SearchableEncryptionSqliteTests.cs`: a
  `Low`-cardinality classified `Equality` field is rejected without
  `acknowledgeLeakageRisk`, succeeds with it, and a field with no
  `cardinality` declared at all is rejected regardless of classification
  (matching `Range`'s existing unconditional-requirement behavior). The
  pre-existing equality-query test's own schema needed `cardinality:
  "High"` added (`Email`, genuinely high-cardinality) to keep passing
  under the now-stricter rule.
- Full suite verified: unit (17/17), Sqlite integration excluding the
  known pre-existing auth-flakiness classes (142/142) — no regressions.
- `TODO.md`'s matching item removed.

## Propagated `x-masking-searchable` into Vitals and Meridian's feature docs

Real, concrete examples added to all five feature docs, each with a
schema fragment, a sequence diagram or Gherkin scenario (or both), and
an explicit note on the cardinality/guardrail call the example makes:

- **Vitals — `patient-enrollment-and-informed-consent.md`**: `LegalName`
  (`Equality`, `High` cardinality) + `DateOfBirth` (`Equality`, `Low`
  cardinality, `acknowledgeLeakageRisk`) for cross-site duplicate-subject
  detection, with the **compound-match** mitigation stated explicitly
  (require both fields, never `DateOfBirth` alone). Two new Gherkin
  scenarios. Cross-referenced from **`trial-data-export-and-subject-
  rights.md`**, whose GDPR-erasure sequence diagram now shows the real
  intake step (resolving a caller's stated name/DOB to an internal
  `EntityId`) before the erasure request can even be constructed — a
  gap the original diagram skipped past.
- **Meridian — `customer-onboarding-and-identity-verification.md`**:
  `DateOfBirth` (`Range`, `Low` cardinality — a real bounded age-
  eligibility query) + `ClaimedLegalName` (`Equality`, `High`
  cardinality — duplicate-applicant-across-DIDs detection), with a
  sequence diagram and two Gherkin scenarios.
- **Meridian — `document-and-biometric-capture.md`**:
  `ExtractedDocumentNumber` (`Equality`, `High` cardinality —
  document-reuse fraud detection across applicants), with a Gherkin
  scenario.
- **Meridian — `periodic-screening-and-sar-escalation.md`**:
  `MatchedListEntryId` (`Equality`, `Low` cardinality,
  `acknowledgeLeakageRisk` — cross-applicant investigation by matched
  sanctions-list entry), with a Gherkin scenario, and an explicit note
  on why `MatchedName` is deliberately *not* indexed the same way.
- `TODO.md`'s three matching items removed. Docs-only — no code touched
  in this part of the pass, verified via a full solution build.

A fourth TODO item remains open, refined mid-pass per direct request:
automated Playwright UI playbooks (screenshots assembled into markdown
user guides, named for the epic/feature) must be a real, re-runnable
script/CLI pipeline, not a one-off hand-assembled artifact — the whole
point is that a playbook can be regenerated as the UI changes.

## Built automated Playwright UI playbooks (later still, same day, direct request)

`ADR-055` named `EventStore.E2ETests` but never built it. Built this
session, plus the playbook-generation extension the TODO item above
named: a real Playwright walkthrough of the actual running app,
screenshots captured at each meaningful step, assembled into a markdown
user guide.

**Two real things found only by actually running it, not assumed:**
- `Microsoft.Playwright.MSTest` 1.61.0 pins `MSTest.TestFramework`/
  `TestAdapter` **2.2.7** and the old VSTest-based `Microsoft.NET.Test.Sdk`
  **17.8.0** — genuinely incompatible with the `Microsoft.Testing.Platform`-
  based MSTest 4.3.3 every other test project in this repo uses. Mixing
  them silently discovered **zero tests** ("No test is available," not an
  error). Fixed by dropping that package and managing Playwright's
  browser/page lifecycle by hand — `Assertions.Expect` is part of the
  core `Microsoft.Playwright` package, nothing lost.
- `KnownResourceStates.Running` alone isn't sufficient readiness signal
  for a Vite dev server resource under `Aspire.Hosting.Testing` — added
  an explicit poll of the resolved endpoint before Playwright navigates.

**Built**: `tests/EventStore.E2ETests` (boots the real `EventStore.
AppHost` via `DistributedApplicationTestingBuilder` — Postgres, migrator,
seed workers, DevIdp, `eventstore`, `client-web-vitals`, no second
orchestration path to maintain), `PlaybookRecorder.cs` (the actual
screenshot-to-markdown mechanism), and one real test,
`VitalsWorkflowAPlaybookTests.RecordPatientEnrollmentAndInformedConsentPlaybook`.
Naming convention confirmed with the user: `{Workflow}-{feature doc
name}.md` under `docs/playbooks/{domain}/`, reusing each domain's own
existing Workflow lettering rather than inventing a new "epic" concept.

**Verified for real, not merely built**: the test passes against the
live app and produces `docs/playbooks/vitals/workflow-a-patient-
enrollment-and-informed-consent.md` plus three real screenshots — one of
which incidentally demonstrates `ADR-009`'s masking wrapper live
(`legalName`/`dateOfBirth` render `"masked": "REDACTED"` for the
`follower-client`'s `events:follow`-only scope).

`ADR-055` gets an "Implementation note" addendum; `docs/06-solution-
structure.md`'s stale "NEVER BUILT" line for `EventStore.E2ETests`
corrected; new `docs/playbooks/README.md` catalog. `TODO.md`'s matching
item replaced with a narrower "extend coverage to the remaining
workflows" item, plus a separately-found, unrelated doc/reality drift
(`EventStore.UnitTests`' own "never built" claim, now false) flagged
rather than fixed in the same pass.

## Fixed the flagged `EventStore.UnitTests` doc/reality drift

`docs/06-solution-structure.md`'s `tests/` layout still described
`EventStore.UnitTests/` as "NEVER BUILT... every test in this solution
lives in the ONE project below [`EventStore.IntegrationTests`]
instead" — stale since `ADR-063`'s own decision ("adopt now, alongside
`ADR-055`'s `EventStore.UnitTests`") to actually build it for FsCheck
property tests and Polly+Simmy fault injection, later joined by
ordinary pure-logic unit tests (`OrderRevealingEncryptionTests`,
`CloudSearchIndexKeyStoreAdapterTests`). Confirmed via `git log
--diff-filter=A -- tests/EventStore.UnitTests/` (built in `7976205`)
and cross-referenced against `ADR-063` and `ADR-055`'s own "Corrected,
2026-08-12" note, which had tracked the gap but never recorded its
later closure.

- `docs/06-solution-structure.md`: replaced the stale description with
  an accurate one, and restated the `IntegrationTests`/`UnitTests`
  dividing line as "needs a real service/database" vs. "a pure function
  or small in-memory object graph is enough," not "unit" vs.
  "integration" in the abstract.
- `docs/adrs/adr-055-testing-strategy.md`: added a "Resolved,
  2026-08-28" note closing its own "Resolves once `EventStore.
  UnitTests` is either built for real or that item is formally
  descoped" line (additive, per this project's own ADR-history
  convention — the original note stays, a resolution is appended, not
  rewritten).
- `TODO.md`'s matching item removed. Docs-only, no code touched.

## Extended UI-playbook coverage to Meridian's Workflow A (both feature docs)

Picked up the other remaining `TODO.md` item: extend the Playwright
playbook mechanism past Vitals' Workflow A. Built two new
`[TestMethod]`s reusing `PlaybookRecorder` unchanged —
`MeridianWorkflowADocumentAndBiometricCapturePlaybookTests` and
`MeridianWorkflowACustomerOnboardingPlaybookTests` — both walking
`client-web-meridian`'s Browse → View flow against the seed continuity
applicant (`kyc:ApplicantIdentity:applicant-1001`). Both run for real
against a live `EventStore.AppHost` and pass, producing
`docs/playbooks/meridian/workflow-a-document-and-biometric-capture.md`
and `docs/playbooks/meridian/workflow-a-customer-onboarding-and-
identity-verification.md` plus their screenshots. `docs/playbooks/
README.md`'s catalog gets both new rows.

**Two real, unassumed limits found by actually trying to go further,
not just added to the catalog and left alone — both now tracked in
`TODO.md`, replacing its old blanket "add one `[TestMethod]` per
remaining workflow" item, which turned out to understate the real
work:**
- **`ADR-039`'s one-event-type-per-instance launch model caps how much
  further this can go without new infrastructure.** Confirmed by
  reading `subscriptionBuilder.ts`: a client instance's subscription is
  fixed to one `(AppId, EventType)` pair at launch — `client-web-vitals`
  only ever sees `PatientScreened`-derived entities, so Vitals'
  Workflows B–D (`Device`/`AdverseEvent`/`IonmAlert`) are genuinely
  unreachable from it, not merely unrecorded. Meridian's Workflow C has
  the same shape of gap (`SarFilingRecorded` needs step-up auth the
  seeder can't perform) and Workflow B has no sample workflow built at
  all yet.
  - A first draft of this pass's own test captions initially guessed a
    specific cause (a register/lookup casing mismatch) for a second,
    unrelated finding below, without checking — caught and corrected
    before landing, per this project's own verify-before-citing rule.
- **A second, independent, previously-unexplained gap**: both Vitals'
  `Patient` and Meridian's `ApplicantIdentity` Detail `ViewDefinition`s
  are registered by their seeders but never actually resolve at
  runtime — every live screenshot (this session's new ones and the
  prior session's Vitals one) shows the generic fallback instead.
  Checked and ruled out `ViewDefinitionService`'s own register/lookup
  path (it normalizes casing symmetrically on both sides, confirmed by
  reading the code) — real root cause not yet found, needs an actual
  network-trace investigation rather than another guess.
- Full solution build verified after adding the two new test classes;
  both new tests run and pass against a real, live `AppHost`.

## Corrected the "ViewDefinition never resolves" finding above — it was a screenshot-timing artifact

Investigated the second finding logged just above by actually measuring
it, rather than leaving it as a guess, per direct request to keep going
on `TODO.md`'s open items. **It was wrong**: `Patient`/`ApplicantIdentity`
Detail `ViewDefinition`s resolve correctly — a direct timing measurement
(a throwaway diagnostic `[TestMethod]`, run once, then deleted) showed
`GenericFallbackView` visible at `t=0ms` and the real templated view
(`TemplateRenderer`, `aria-label="Entity (ViewDefinition-rendered)"`)
visible at `t=500ms`. `EntityView.vue`'s `entry` (from the entity cache)
populates before `loadViewDefinition`'s own async GraphQL round trip
resolves, so the fallback is real but transient — every screenshot this
session and the prior one happened to land inside that ~500ms window
because the test's own wait condition (a heading containing the raw
`EntityId`) only ever matches `GenericFallbackView`'s heading markup,
never `TemplateRenderer`'s template-defined one, so it was structurally
guaranteed to catch the fallback, not prove its permanence.

- All three playbook tests (`VitalsWorkflowAPlaybookTests`, both
  `MeridianWorkflowA*PlaybookTests`) now wait for `aria-label="Entity
  (ViewDefinition-rendered)"` before capturing the Detail step, and all
  three playbooks regenerated with the real templated view instead of
  the transient fallback — `docs/playbooks/vitals/workflow-a-patient-
  enrollment-and-informed-consent.md` and both `docs/playbooks/
  meridian/workflow-a-*.md` files' Step 3.
- **A real, different, smaller gap surfaced instead by looking at the
  corrected screenshots**: both templates render every field label as a
  literal, unresolved `[key]` bracket (`TemplateRenderer.vue`'s own
  documented fallback for a translation key with no entry in the
  `translations` map) — no translation resource registers
  `patient_detail_title`/`subject_id`/etc. or `applicant_detail_title`/
  `applicant_id`/etc. anywhere yet. `TODO.md`'s entry replaced
  accordingly (the mechanism itself works; the resource file for these
  specific keys doesn't exist).
- All three playbook tests re-verified passing against a live `AppHost`
  after this fix.

## Registered the missing translations for Vitals'/Meridian's own ViewDefinition templates

Closed the smaller gap the correction above actually found. Added
Vitals' Patient (`patient_detail_title`, `subject_id`, `site_id`,
`protocol_id`, `eligibility_status`, `legal_name`, `date_of_birth`) and
Meridian's ApplicantIdentity (`applicant_detail_title`, `applicant_id`,
`document_type`, `claimed_legal_name`, `date_of_birth`, `did`)
translation keys to `client-web/packages/mvvm-client/src/i18n/
translations.ts`'s `placeholderTranslations`, across all three existing
locales (`en-US`/`fr-FR`/`ar-SA`) — the one resource set
`useEntityViewActions.ts` actually resolves against (`ADR-087`'s own
comment already frames this file's content as "domain-owned," and these
two proving-ground domains own exactly this content the same way they
already own their seed data and `ViewDefinition` templates).

- `translations.spec.ts`'s 5 existing tests still pass unmodified (they
  compare against the live `placeholderTranslations` object, not a
  hardcoded key list). Full `client-web` suite verified: `mvvm-client`
  110/110, `reference-app` 41/41.
- All three playbook tests regenerated once more with the fix in place
  — `docs/playbooks/vitals/workflow-a-patient-enrollment-and-informed-
  consent.md` and both Meridian files now show real labels ("Patient
  Detail," "Subject ID," ...) instead of `[patient_detail_title]`-style
  brackets. `TODO.md`'s entry for this removed.

## Unblocked two more Vitals playbooks by adding new client instances

Took the most tractable slice of the remaining `TODO.md` item: `ADR-039`'s
one-event-type-per-instance model meant `client-web-vitals` (locked to
`PatientScreened`) could never Browse a `Device` or `IonmAlert` entity,
so Vitals' Workflows B and D were UI-unreachable, not merely
unrecorded. Added two more Vite app instances to `EventStore.AppHost/
AppHost.cs`, exactly mirroring `clientWebVitals`'s own shape (same
`eventstore`/`devIdp` references, own pinned port, own CORS origin on
both `devIdp` and `eventstore`, own `WithParentRelationship(vitalsSeed)`
for dashboard grouping):

- `client-web-vitals-device` — `VITE_EVENT_TYPE=DeviceOnboarded`,
  `VITE_ENTITY_TYPE=device`.
- `client-web-vitals-ionmalert` — `VITE_EVENT_TYPE=IonmAlertRaised`,
  `VITE_ENTITY_TYPE=ionmalert`.

Two new playbook tests (`VitalsWorkflowBDeviceOnboardingPlaybookTests`,
`VitalsWorkflowDIntraoperativeMonitoringPlaybookTests`) reuse
`PlaybookRecorder` unchanged against these, producing `docs/playbooks/
vitals/workflow-b-device-onboarding-and-continuous-monitoring.md` and
`docs/playbooks/vitals/workflow-d-intraoperative-monitoring-and-alert-
response.md`. Both entities render via `GenericFallbackView` (no
`ViewDefinition` registered for `device`/`ionmalert`, unlike `Patient`/
`ApplicantIdentity`) — captions state this plainly rather than
overclaiming a template that doesn't exist.

All 5 playbook tests verified passing together against one real,
live `AppHost` (one earlier solo run of the new pair hit a single
transient failure under concurrent AppHost startup load, not
reproduced on immediate retry or in the full 5-test run — not treated
as a real defect absent a repeatable cause). `TODO.md`'s entry
narrowed to the three gaps that remain genuinely open: Vitals'
Workflow B downstream half (no `AdverseEvent` seed data at all), and
Meridian's Workflows B (no sample workflow exists) and C
(`SarFilingRecorded` needs step-up auth the seeder can't perform) —
each needs real sample-data/infra work first, not another
`[TestMethod]`.

## Closed the Vitals AdverseEvent gap; investigated the two Meridian gaps for real

Direct request to keep going. Closed the most tractable of the three
remaining gaps and investigated the other two properly instead of
leaving the prior pass's characterization unverified.

**Vitals' Workflow B downstream half, closed for real**:
`Samples.Vitals.Seed` now publishes an `AdverseEventReported` event
(`ae-1042`, the same AE ID the feature doc's own worked example uses)
— there was no `AdverseEvent` entity to browse at all before this. A
new `client-web-vitals-adverseevent` instance (`EventStore.AppHost`,
subscribed to `AdverseEventReported`) plus
`VitalsWorkflowBAdverseEventPlaybookTests` produces `docs/playbooks/
vitals/workflow-b-adverse-event-capture-and-review.md`, verified
against a live `AppHost`.

**Meridian's Workflow B, investigated, not built**: grepping for
existing coverage before assuming the prior pass's "no sample exists"
framing was the whole story found `MeridianWorkflowBHttpSqliteTests.cs`
— the Relying-Party Access mechanism (UcanDelegation + OAuth Token
Exchange + `revealField`) is real and already proven end to end, just
never wired into `Samples.Meridian`/seeded/given a UI. Reading that
test's own header comment further: the doc's own `accessGrant` events
have no real counterpart — delegation is a client-signed token used
directly in a GraphQL mutation, never a `StoredEvent`. `client-web` has
no UI screen for this at all (confirmed: no relying-party component
exists anywhere in `client-web/packages/reference-app`). Building a
playbook here needs a genuinely new Vue UI feature, not wiring — a real
product/scope decision, deliberately not made unilaterally just to
produce a screenshot. `TODO.md`'s entry corrected to state this
precisely instead of the prior, narrower "no sample exists" framing.

**Meridian's Workflow C, partially closed**: `MeridianWorkflowC.cs`
already registers `SanctionsScreeningPerformed` (published by the seed)
alongside `SarFilingRecorded` (never published — needs the RFC 9470
step-up auth its own `RequiredSignature` declares, which the seeder
can't perform). Added `client-web-meridian-screening` (subscribed to
`SanctionsScreeningPerformed`) and `MeridianWorkflowCPeriodicScreening
PlaybookTests`, producing `docs/playbooks/meridian/workflow-c-periodic-
screening-and-sar-escalation.md` (screening half only, captioned as
such). **A genuine, separate finding surfaced building it**: the
registered `ApplicantIdentity` `ViewDefinition` template's own bound
fields don't match this event's payload shape at all — only
`applicantId` renders, `ScreeningDate`/`MatchFound`/etc. never appear
on screen despite being genuinely published, because a `ViewDefinition`
is scoped to an `EntityType` while the data actually available is
scoped to whichever one `EventType` a given client instance subscribes
to, and nothing reconciles the two. Not fixed (a real template-content
decision, out of scope here) — the playbook's own caption and
`docs/playbooks/README.md` state it plainly instead of the screenshot
silently looking sparse for no stated reason.

All 7 playbook tests verified passing together against one real, live
`AppHost`. `TODO.md`'s two remaining items rewritten to reflect what
was actually found, not the prior pass's initial guess.

## Closed the SAR-escalation gap for real, on direct "keep going" request

Investigated `StepUpEvaluator` (`src/EventStore.Domain/SchemaRegistry/
StepUpEvaluator.cs`) directly before assuming step-up auth needed a real
DevIdp round trip: it checks two ordinary claims on the caller's
`ClaimsPrincipal` (`"acr"`, `"auth_time"`), nothing more — and
`Samples.Meridian.Seed`, like `Samples.Vitals.Seed`, already talks to
`PublishService` in-process with a hand-built `ClaimsPrincipal`, never
over HTTP/DevIdp at all. `MeridianWorkflowCScenarioAssertions.cs`'s own
`AfterSteppingUpTheRetriedSarFilingSucceedsAndCapturesASignature` test
proved the exact same shape already works. This reframed "needs a real
RFC 9470 step-up flow" from a big, separate authentication feature into
a small, mechanical seed addition.

- `Samples.Meridian.Seed/Program.cs`: a second, matched
  `SanctionsScreeningPerformed` (`MatchFound: true`, the feature doc's
  own worked-example values — `MatchConfidence: 0.87`, `MatchedName:
  "Jane Smith"`, `MatchedListEntryId: "SDN-44291"`), a compliance
  officer's `authorityDecision` accepting it, then `SarFilingRecorded`
  itself, published by an `officerPrincipal` carrying `identity:aml-
  review` plus `acr: "urn:kyc:acr:step-up"` and a fresh `auth_time` —
  `PublishSeedEventAsync`'s helper extended with optional
  `ClaimsPrincipal`/`reviewPending`/`meaning` parameters to support it,
  every pre-existing call site unaffected (still defaults to `seedUser`,
  `reviewPending: false`, `meaning: null`).
- New `client-web-meridian-sarfiling` instance (`EventStore.AppHost`,
  subscribed to `SarFilingRecorded`) and
  `MeridianWorkflowCSarFilingPlaybookTests`, producing `docs/playbooks/
  meridian/workflow-c-sar-filing.md` — verified against a live
  `AppHost`: the SAR genuinely reaches `AuthorityStatus: accepted`,
  confirming the step-up gate was actually satisfied, not bypassed.
  Same `ViewDefinition`/payload-shape mismatch as the screening
  playbook (only `applicantId` renders) — captioned honestly, not
  reworked.
- `MeridianWorkflowCSqliteTests`/`ScenarioAssertions` re-run to confirm
  the seed's new schema registrations didn't collide with anything —
  unaffected (seed and integration tests register schemas
  independently).
- Full solution build clean; all 8 playbook tests verified passing
  together against one real, live `AppHost`.
- `TODO.md`: the SAR-escalation item removed (genuinely closed);
  Meridian Workflow B's entry kept as the one remaining gap, now stated
  as categorically different (no UI surface exists, not missing seed
  data) since every other gap in that category is now closed. The
  `ViewDefinition`/payload-shape mismatch is promoted to its own
  tracked item (found twice now, on two different event types).

## Fixed the ViewDefinition/payload-shape mismatch, on direct "keep going" request

Closed `TODO.md`'s remaining cosmetic finding with a minimal, seed-owned
fix rather than a framework change: extended the `ApplicantIdentity`
`ViewDefinition` template `Samples.Meridian.Seed` already registers with
`screeningDate`/`listsChecked`/`matchFound`/`filingReferenceId`/
`narrative` bindings alongside Workflow A's original five. An unbound
field already renders `''` (`TemplateRenderer.vue`'s own documented
behavior), so this costs nothing for instances that don't have these
fields and gives the screening/SAR instances real content instead of a
near-empty screen. `MatchedName`/`MatchedListEntryId` deliberately left
out (masked, `identity:aml-review`-gated, no client instance's login is
expected to hold that claim); `Narrative` (also masked the same way)
included anyway as a live masking demonstration.

- `client-web/packages/mvvm-client/src/i18n/translations.ts`: five new
  keys (`screening_date`, `lists_checked`, `match_found`,
  `filing_reference_id`, `narrative`) across all three locales, same
  placeholder-quality bar as the existing set. `mvvm-client` suite
  re-verified (110/110).
- Both Meridian Workflow C playbooks regenerated and re-verified against
  a live `AppHost`: Screening Date/Match Found now render on the
  periodic-screening playbook, Filing Reference ID/masked Narrative
  (`"***"`) on the SAR-filing one.
- **One new, small, separate observation while checking the result**:
  `ListsChecked` (a JSON array field) still renders blank. Checked the
  real cause before writing another vague TODO entry:
  `EventTypeSchemaReader.cs` deliberately skips top-level array-typed
  properties when building an event type's GraphQL payload type at all
  — an already-documented, previously-accepted narrowing (its own code
  comment, and `docs/changes/2026-08-03.md`/`2026-08-04.md`), not a new
  gap. No new `TODO.md` item added; the playbook's own caption states
  the real reason instead.
- All 8 playbook tests re-verified passing together against a live
  `AppHost`. `TODO.md`'s `ViewDefinition`/payload-shape item removed —
  Meridian Workflow B (no UI surface at all) is now the only item left.

## Built the Relying-Party Access UI feature, on direct "keep going" request, and found two real bugs doing it

The last `TODO.md` item was a genuine fork, not a mechanical extension:
Meridian's Workflow B (Relying-Party Access, `ADR-043`/`044`) has a real,
proven mechanism (`MeridianWorkflowBHttpSqliteTests.cs`) but no
`client-web` UI surface at all, and building one meant designing and
shipping a new screen. Held off building it unprompted twice; built it
this pass once asked directly to keep going regardless.

**New client-side crypto, not a stubbed demo.** The whole ADR-043/044
mechanism now runs live in the browser, no server-side test fixture or
pre-computed token involved:

- `client-web/packages/mvvm-client/src/api/ucan.ts` — `generateUcanKeyPair`
  (WebCrypto ECDSA P-256), `computeJwkThumbprint` (RFC 7638, mirrors
  `EventStore.Dpop.JwkThumbprint.Compute` exactly), `signUcanDelegation`
  (mirrors `UcanDelegation.Create` exactly, including its own `cap`
  claim's PascalCase `Claim`/`EntityScope` wire shape — `System.Text.Json`'s
  default, case-sensitive on the server's own deserializer). 6 new unit
  tests lock the exact wire format, not just "it doesn't throw."
- `client-web/packages/mvvm-client/src/api/rbacClient.ts` —
  `registerTrustRoot` (`PUT /rbac/trust-roots/{issuerDid}`).
- `client-web/packages/mvvm-client/src/api/authClient.ts` — added
  `exchangeUcanDelegationForToken` (RFC 8693 token exchange), mirroring
  `MeridianWorkflowBHttpSqliteTests.cs`'s own `ExchangeAsync` exactly.
- `client-web/packages/mvvm-client/src/composables/useRelyingPartyAccess.ts` —
  ties the whole sequence together (register trust root → sign
  delegation → exchange, retried since `RbacProjectionWorker`'s own
  Follow tail is asynchronous → reveal), 5 new unit tests covering the
  happy path, the retry, and three distinct failure modes.
- `client-web/packages/reference-app/src/components/relyingParty/
  RelyingPartyAccessPanel.vue` — a new, Meridian-only "Relying-Party
  Access" tab (`App.vue`), a raw form in the same deliberately-unguided
  style as the Event Composer tab, not a polished wizard.

**Two real bugs found only by actually running this against a live
`AppHost`, neither reachable from a unit test or an isolated
`WebApplicationFactory` integration test:**

1. **`RbacProjectionWorker` (`ADR-067`) was registered in `EventStore.
   DevIdp/Program.cs` but never actually configured anywhere in
   `EventStore.AppHost`** — `Rbac:AppIds` defaults to empty, which makes
   the worker a permanent no-op; grep confirmed zero matches for any
   `Rbac__*` environment variable anywhere in `AppHost.cs`. Every
   existing test either applies `TrustRootService`'s own fold directly
   (`DelegatedGrantsRbacFederationHttpSqliteTests.cs`) or drives
   `RbacProjectionWorker.CatchUpOnceAsync` explicitly
   (`RbacProjectionWorkerHttpSqliteTests.cs`) specifically to dodge a
   `WebApplicationFactory` self-reference hazard — neither path ever
   exercises the real `BackgroundService` inside a genuinely running
   `AppHost`, so this had no way to surface before a real browser
   actually tried to register a trust root against the live app.
   Fixed: `devIdp.WithEnvironment(...)` now sets `Rbac__AppIds__0/1`
   (`trial1`, `kyc`), `Rbac__HostBaseUrl`, `Rbac__DevIdpBaseAddress`, and
   `Rbac__Client__*`, matching `RbacProjectionWorkerHttpSqliteTests.cs`'s
   own already-proven configuration exactly.
2. **`revealField`'s `eventId` argument needed the `UUID` GraphQL scalar,
   not the generic `ID` scalar** — HotChocolate's default binding for a
   C# `Guid` parameter (`RevealFieldMutation.cs`) is `UUID`, and no prior
   `client-web` caller of `revealField` existed to have already gotten
   this right. Found via the real error GraphQL returned ("the variable
   `eventId` is not compatible with the type of the current location"),
   not by reading the schema first.

**Verified for real**: `MeridianWorkflowBRelyingPartyAccessPlaybookTests`
runs the whole flow in a real Chromium browser against a live `AppHost`
and produces `docs/playbooks/meridian/workflow-b-relying-party-access.md`
— the final screenshot shows "Revealed value: John Doe," the seed's own
real `ClaimedLegalName`, proving the full chain actually worked end to
end rather than a stubbed response. All 9 playbook tests verified
passing together; full `client-web` suite verified (121/121 `mvvm-client`,
41/41 `reference-app`); the related `RbacProjectionWorker`/delegated-grants
integration tests re-verified unaffected (12/12). `TODO.md` is now
empty.

## Sequence diagrams, role-based playbook paths, and a real step-up-retry bug fix

Direct request: add a PlantUML sequence diagram to every UI playbook,
rename every playbook to reflect the persona performing the task rather
than the workflow letter, and add more proving-ground use-case coverage.

- `PlaybookRecorder.WriteMarkdownAsync` gained an optional
  `sequenceDiagramPlantUml` parameter, rendered as a "## Sequence
  Diagram" section right after the generated-by note. Every one of the
  9 existing playbooks got one, hand-authored per test (matching this
  repo's own "PlantUML is hand-drawn, not generated" convention), with
  `alt`/`opt` blocks wherever a genuine alternate path exists (a
  rejection, an RFC 9470 step-up challenge, an unmatched-vs-matched
  screening branch, ADR-094's acknowledged-vs-missed alert deadline).
  Two captions were corrected along the way after checking the seed's
  own `ClaimsPrincipal` against what the caption claimed: Meridian's
  `IdentityClaimSubmitted` and Vitals' `AdverseEventReported` are both
  ordinary, immediately-accepted publishes for this continuity data, not
  the result of an analyst/PI review that never actually ran — the
  domain's own richer review path is real (cited from each workflow's
  own source file) but wasn't what this specific seed data exercised.
- Naming went through two direct-feedback revisions: first
  `{role}-{task}.md` (dropping the original `{workflow}-{feature doc
  name}.md` scheme entirely), then moving `{role}` into its own
  directory segment (`{domain}/{role}/{task}.md`) rather than a filename
  prefix. All 10 playbooks (the 9 originals plus the new PI Queue one,
  below) regenerated at their final paths and verified together against
  a live `AppHost`. Every stale file under an older name is gone —
  `git rm` for the previously-committed `workflow-*.md` set, plain `rm`
  for the untracked intermediate flat-named set that was never
  committed. `docs/playbooks/README.md`'s catalog rewritten accordingly,
  split into a Vitals table and a Meridian table, each row still noting
  its Workflow letter even though the path itself no longer carries it.
- **New playbook using already-built UI, not new production code**:
  `VitalsPrincipalInvestigatorQueuePlaybookTests` walks the real
  `VitalsPiQueue.vue` screen against a genuinely live, continuously-
  arriving pending item (`Samples.Vitals.Simulator` publishes a fresh
  `IonmAlertRaised` with `ReviewPending: true` roughly every 20s,
  forever) rather than fixed seed data.
- **A real, previously-undiscovered bug found building it, not a test
  artifact**: the Accept button failed with a generic error every time.
  `publishClient.ts`'s RFC 9470 step-up-retry check read `body.error`,
  but `PublishEndpoints.cs`'s `BuildStepUpChallenge` returns an RFC 7807
  `ProblemDetails` body (`type`/`title`/`status`) with no `error` field
  in the JSON at all — the real field is `title`, confirmed by capturing
  the actual 401 response body via a temporary Playwright response
  listener before guessing. Every step-up retry through this client had
  silently fallen through to an ordinary failure before this fix, for
  every `RequiredSignature`-gated event type this client ever publishes
  against, not just Vitals' `authorityDecision`. Fixed in
  `publishClient.ts`; `useEventComposer.spec.ts`'s own mock (which had
  been matching the bug's own wrong assumption, `{error: ...}`, rather
  than the real server response) corrected to the real shape in the same
  pass, so this class of regression is now actually caught.
- Meridian's matching KYC Analyst Queue playbook is the one item from
  this pass still open (`TODO.md`) — same shape, `MeridianAnalystQueue.vue`
  against `Samples.Meridian.Simulator`'s own periodic
  `SanctionsScreeningPerformed` publishes.
- Full solution build clean; `client-web` suite verified (`mvvm-client`
  121/121); all 10 playbook tests verified passing together against a
  live `AppHost`.

Also tracked in `TODO.md`, not started this pass: per-application
(Vitals/Meridian) `README.md`s covering workflow interactions; Vitals'
Workflow C (Trial Data Export and Subject Rights, still entirely
uncovered — needs new UI wiring, same shape as Relying-Party Access); a
`style-guide.md` for `client-web`'s UI/UX; and adopting Naive UI with a
left-hand-nav shell (Azure Portal/DevOps-style) — deliberately sequenced
after the above, since a navigation-pattern rewrite would invalidate
every Playwright selector built this pass if done concurrently.

## Closed the KYC Analyst Queue playbook gap; found a second masking-render bug

Direct request to keep going down `TODO.md`. Built the Meridian
counterpart to the Vitals PI Queue playbook from the previous pass:
`MeridianKycAnalystQueuePlaybookTests` walks `MeridianAnalystQueue.vue`
against a genuinely live, continuously-arriving pending item
(`Samples.Meridian.Simulator` publishes a fresh `SanctionsScreeningPerformed`
every ~25s, matching roughly 1 in 3 ticks) rather than fixed seed data —
same shape as the PI Queue test, reused directly.

**A second real, previously-undiscovered bug, found the same way as the
step-up one**: the very first real screenshot of this queue with a
masked field actually present in its payload (`MatchedName`/
`MatchedListEntryId`, both `x-masking`'d) showed `AuthorityQueue.vue`'s
own `summarize()` rendering them as the literal, useless string
`"[object Object]"` — a bare template-literal interpolation of the
`{value, masked, erased}` wrapper object every masked field arrives as.
`EntityBrowser.vue` already had the right fix for this exact shape
(`"[masked/complex]"` for any object-typed value); mirrored into
`AuthorityQueue.vue` in the same pass, plus a new regression test
(`AuthorityQueue.spec.ts`) locking it in.

- Full `client-web` suite verified (`reference-app` 42/42, up from 41).
- All 11 playbook tests (9 originals + both queue playbooks) verified
  passing together against one real, live `AppHost`.
- `TODO.md`'s "Add 2 new playbooks using already-built Queue UI" item
  is now fully closed. `docs/playbooks/README.md`'s catalog and its own
  narrative updated accordingly.

Also this pass: fetched and merged `origin/main` (a single commit
deleting `.github/workflows/ci.yml`/`dependabot.yml`, per direct
request) — clean merge, no conflicts, unrelated to anything touched
here — and pushed the branch.
