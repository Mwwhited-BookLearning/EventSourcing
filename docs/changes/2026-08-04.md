# 2026-08-04

**Item 11, "Hardening & Evolution (DPoP, event upcasting, hash-chained
tamper evidence)," is Done** — all four sub-parts (`ADR-017`/`018`/`019`/
`020`) built and tested across SQLite/PostgreSQL/SQL Server.
`docs/08-build-plan.md`'s status table and `BuildPlan_CorePhases` diagram
fill both updated to Done/`#palegreen` in this pass.

## Hash-chained tamper evidence (`ADR-019`)

Code was already in place from the prior session
([`EventChainHash.cs`](../../src/EventStore.Domain/EventLog/EventChainHash.cs),
[`PublishService.cs`](../../src/EventStore.Inbox/PublishService.cs),
[`ChainVerificationService.cs`](../../src/EventStore.Inbox/ChainVerificationService.cs))
but had **no test coverage at all** — this pass wrote it
([`HashChainScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/HashChainScenarioAssertions.cs))
and, while doing so, found a real gap against this item's own exit
criterion ("deliberately corrupting one historical `Payload` ... is
detected by the verification endpoint"): `ChainVerificationService`
recomputed the chain from the **stored** `PayloadHash` column, never from
`Payload` itself — a direct-database edit to `Payload` alone (leaving
`PayloadHash` untouched) would have gone completely undetected, silently
failing the one scenario this item's exit criteria explicitly names.
Fixed by extracting the payload-hash formula into a shared
[`EventPayloadHash.cs`](../../src/EventStore.Domain/EventLog/EventPayloadHash.cs)
(`PublishService` now calls it too, instead of duplicating it) and having
[`ChainVerificationService.cs`](../../src/EventStore.Inbox/ChainVerificationService.cs)
re-derive `PayloadHash` from each row's own `EventType`/`Payload`/parent
links before folding it into the chain — corrupting `Payload` alone now
correctly diverges the chain at exactly that `SequenceNumber`, with every
earlier event still verifying clean.

## Event upcasting (`ADR-018`)

Also already in place
([`UpcastChain.cs`](../../src/EventStore.Upcasting/UpcastChain.cs),
[`EventTailReader.cs`](../../src/EventStore.Follow.Api/EventTailReader.cs))
but untested — added
[`UpcastingScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/UpcastingScenarioAssertions.cs)
(single-hop and two-hop version gaps, both verified against a real
`FollowService` replay, not just `UpcastChain` in isolation).

## Publish-time upcast validation + `EventUpcastFailed` dead-letter (`ADR-020`)

Built from scratch this pass:
- [`PublishService.cs`](../../src/EventStore.Inbox/PublishService.cs) —
  when `schemaVersion` is behind the type's active version,
  `CheckUpcastCompatibilityAsync` runs the caller's real payload through
  `UpcastChain` up to the active version, **then validates the result
  against the active schema** (`UpcastChain.Apply` alone only proves the
  expressions ran, not that their output is usable). On success: stored
  exactly as declared, unchanged from before this item. On failure:
  `PublishUpcastFailedAsync` stores the reserved `EventUpcastFailed` type
  in the original event's place (original `eventType`/`schemaVersion`/
  payload verbatim, plus which hop failed and why), and `PublishResult.
  Created` gained an `EventType` field so the response can name
  `EventUpcastFailed` as what was actually stored, not the caller's own
  type — a new field, not a new result case, since "stored some event
  successfully" is still the right response shape either way.
  `SchemaRegistryService.GetVersionsAsync` (AppId-scoped, unlike the
  existing bare-name `GetVersionsByNameAsync` Follow uses) was added to
  fetch the version range needed.
- [`PublishResult.cs`](../../src/EventStore.Inbox/PublishResult.cs),
  [`PublishEndpoints.cs`](../../src/EventStore.Inbox/PublishEndpoints.cs) —
  `eventType` added to the `201 Created` response body.
- Tests: two new scenarios in
  [`PublishScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/PublishScenarioAssertions.cs)
  (compatible upcast stores unchanged; failing upcast dead-letters as
  `EventUpcastFailed`). One **pre-existing, never-before-exercised**
  scenario broke under the new check and needed fixing, not working
  around: `PublishingValidatesAgainstTheDeclaredVersionNotWhicheverIsActive`
  registered a v2 with `UpcastFromPrevious: null` while adding a new
  *required* field — exactly the incompatible-upcast shape this item now
  correctly dead-letters, which masked the scenario's own actual assertion
  (declared-version, not active-version, validation). Fixed by giving that
  v2 a real mapping that actually supplies the new field, restoring the
  original assertion's intent.

**Accepted v1 limitation, not a gap**: a retry of a request whose upcast
still fails dead-letters again each time (a fresh `EventId`, not
deduplicated against the earlier `EventUpcastFailed` row) — `ADR-020`
doesn't specify idempotent-retry semantics for the dead-letter path
itself, and deduplicating it would need either reusing the caller's
`EventId` for a *different* stored type (breaking the existing
`ComputeHash`-based replay check, which hashes the original content) or a
second lookup mechanism this item's exit criteria never asked for. Left
as documented, repeatable behavior (see the comment on
`PublishUpcastFailedAsync` in
[`PublishService.cs`](../../src/EventStore.Inbox/PublishService.cs)),
matching `ADR-020`'s own "no proactive check, only discovered when
actually hit" posture rather than over-building this v1.

## DPoP-bound access tokens (`ADR-017`)

Built from scratch — the largest, most security-sensitive part of this
item. New project
[`EventStore.Dpop`](../../src/EventStore.Dpop/EventStore.Dpop.csproj)
(EC P-256 only, no algorithm agility asked for), shared by
`EventStore.DevIdp` (token issuance) and `EventStore.Host.Core` (API
validation) rather than duplicated:
- [`DpopKeyPair.cs`](../../src/EventStore.Dpop/DpopKeyPair.cs) — key-pair
  generation + signed proof-JWT creation (`typ: dpop+jwt`, embedded
  `jwk`, `htm`/`htu`/`iat`/`jti`, optional `ath`).
- [`JwkThumbprint.cs`](../../src/EventStore.Dpop/JwkThumbprint.cs) — RFC
  7638, the `jkt` value embedded in issued tokens and recomputed from
  each proof to confirm the same key.
- [`DpopProofValidator.cs`](../../src/EventStore.Dpop/DpopProofValidator.cs) —
  self-contained validation (RFC 9449 §4.3: the proof's own embedded
  `jwk` is both what's checked against and what verifies its signature):
  `typ`, signature, `htm`/`htu`, `iat` freshness (±60s), `jti` replay
  (`DpopReplayCache.cs`), and `ath` when a token is being presented.
- [`DevIdpSeeder.cs`](../../src/EventStore.DevIdp/DevIdpSeeder.cs) —
  generates one key pair per seeded client, exposed via
  `GetClientKeyPair(clientId)`. No separate client *process* exists in
  this repo for `publisher-client`/`follower-client`/`operator-client` —
  every one of them is only ever driven from test code — so this seeder
  plays the "client generates its own key" role from `ADR-017`'s own text
  for those three; the token endpoint itself never reads this map, it
  validates whatever proof is actually submitted, exactly like a real
  deployment would.
- [`Program.cs`](../../src/EventStore.DevIdp/Program.cs) — `/connect/token`
  now requires and validates a `DPoP` header proof (RFC 9449 §5's own
  OAuth2 error shape on failure — `400`, `invalid_dpop_proof` — not RFC
  9457 Problem Details, which is this project's contract for the
  `EventStore.Host.<Provider>` API surface, not this OAuth2 endpoint) and
  embeds the result's thumbprint as a flat `cnf.jkt` claim on the issued
  token (`ADR-017`'s own phrasing — a flat claim, not RFC 9449's nested
  `cnf: {jkt: ...}` JSON-object shape).
- [`DpopValidationMiddleware.cs`](../../src/EventStore.Host.Core/DpopValidationMiddleware.cs),
  wired into
  [`HostCoreExtensions.cs`](../../src/EventStore.Host.Core/HostCoreExtensions.cs)
  between `UseAuthentication()` and `UseAuthorization()` — a request with
  no bearer token is left alone (the existing JwtBearer challenge already
  401s it, unrelated to DPoP); a request with a validated bearer but a
  missing/invalid/mismatched-key DPoP proof is rejected `401`
  (`dpop-proof-invalid`, `reason` extension — `ADR-013`'s own table row
  for exactly this case).
- [`FollowClient.cs`](../../src/EventStore.Projections.Host/FollowClient.cs) —
  `projections-client` is the **one** seeded client with real, running
  production code in this repo (`ProjectionHost`'s own Follow consumer),
  so it generates and holds its own `DpopKeyPair` for real, exactly as
  `ADR-017` describes, rather than borrowing the seeder's simulated one.
- Tests:
  [`AuthScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/AuthScenarioAssertions.cs) —
  every token request and every API call now carries a real, freshly
  generated DPoP proof; three new scenarios, matching
  [`docs/features/dpop-and-tamper-evidence.md`](../features/dpop-and-tamper-evidence.md)'s
  own Gherkin one-for-one: `ARequestWithAValidBearerTokenButNoDpopProofIs
  RejectedWith401`, `ARequestWithADpopProofSignedByADifferentKeyIsRejected
  With401` (the actual value this ADR buys: a leaked bearer token alone is
  useless without the private key it's bound to), and
  `ReplayingAnAlreadyUsedDpopProofIsRejectedWith401` (the same proof's exact
  bytes, same `jti`, reused on a second request).
  [`ProjectionsScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/ProjectionsScenarioAssertions.cs)
  updated to the same `(token, key)`-pair pattern via a shared, now-public
  `AuthScenarioAssertions.AttachAuth`.

### Real bugs found only by running the real HTTP round trip, not by reading the code back

1. **Header-claim serialization**: `AdditionalHeaderClaims["jwk"] =
   PublicJwk` (the `EcJwk` record) threw `IDX11025` — the JWT header
   writer only serializes a fixed set of primitive shapes, not an
   arbitrary POCO. Fixed by passing a plain `Dictionary<string, object>`
   instead (see the comment in
   [`DpopKeyPair.cs`](../../src/EventStore.Dpop/DpopKeyPair.cs)).
2. **Parallel-test database race**: `EventStore.DevIdp`'s EF Core
   InMemory database used a fixed name (`"EventStore.DevIdp"`) — EF
   Core's InMemory provider keys databases by name **process-wide**, not
   per `WebApplicationFactory` instance, so `AuthSqliteTests` and
   `ProjectionsSqliteTests` running concurrently (MSTest's default
   method-level parallelization) silently shared one store and raced on
   `DevIdpSeeder`'s seeding (`"An application with the same client
   identifier already exists"`).
3. **Self-inflicted regression fixing bug 2**: the first fix
   (`UseInMemoryDatabase($"EventStore.DevIdp-{Guid.NewGuid():N}")`) put
   the `Guid.NewGuid()` call **inside** the `AddDbContext` options
   delegate — which EF Core re-invokes on every scoped `DbContext`
   instantiation, i.e. on every single HTTP request, handing each request
   its own fresh, empty, unseeded database (`invalid_client` on the very
   first token request in a freshly-fixed test run). Fixed by computing
   the name once, outside the delegate (see
   [`Program.cs`](../../src/EventStore.DevIdp/Program.cs)'s comment) —
   caught only by re-running the full suite after the first fix, not
   assumed correct from the diff.

## Test count

`EventStore.IntegrationTests` still has 23 passing `[TestMethod]`s (each
provider's `AllXScenarios` gained more internal scenario calls, not new
test methods, the same pattern every item since item 7 has used) — all 23
pass across SQLite/PostgreSQL/SQL Server. `AllPublishScenarios` gained 4
new scenarios (hash-chain ×2, publish-time-upcast ×2), `AllFollowScenarios`
gained 2 (upcasting), `AllAuthScenarios` gained 2 (DPoP).

# Item 12, "Entity-Centric Core Rebuild"

Also Done, same day, continuing directly from item 11 above per the
standing "check off work as you go, then continue" rhythm. `ADR-021`
(`EntityId`, the always-on Entity Store), `ADR-022` (`Optional<T>`
property-level patches — confirmed already correctly implemented by
[`SnapshotMerger.MergePatch`](../../src/EventStore.Projections.Host/SnapshotMerger.cs)'s
existing `JsonNode`-based merge, which already distinguishes absent/
explicit-null/value correctly; only its comment needed updating, not its
logic), `ADR-023` (the Inbox/Router split — publish now always returns
`202` + a status envelope), and `ADR-024`/`ADR-029` (`ConflictFlag`/
`LateArrivalFlag`) are all built and tested.

## New: `EventStore.Router`

[`RouterWorker.cs`](../../src/EventStore.Router/RouterWorker.cs) — the
async half of the Inbox/Router split (`ADR-023`). One polling loop, same
`BackgroundService` + testable static `RunOnceAsync` shape
[`DerivationWorker`](../../src/EventStore.Derivation/DerivationWorker.cs)
already established. Per event with `Status: "received"`: schema
validation against the *declared* `SchemaVersion` (advisory `SchemaStatus`,
never blocking), entity resolution via the active version's
`EntityIdField`/`EntityType`, and the Entity Store fold itself —
`ConflictFlag` (stale `ExpectedVersion`, never blocks) and `LateArrivalFlag`
(`OccurredAt` behind the entity's high-water mark, *does* gate the write)
computed independently in the same fold step, per
[`docs/patterns/interactions/fold-ordering-and-conflict.md`](../patterns/interactions/fold-ordering-and-conflict.md).
[`EntityDataMerger.cs`](../../src/EventStore.Router/EntityDataMerger.cs)
mirrors `SnapshotMerger`'s own merge logic (duplicated, not shared —
`ProjectionHost` deliberately has zero write-side reference).
`docs/06-solution-structure.md` names `EventStore.Router` and
`EventStore.Fold` as two separate deployables; this pass combines both
into one project, the same "concept accurate, exact wiring unverified"
gap that sketch's own banner already covers.

## Rewritten: `PublishService` (the "Inbox")

[`PublishService.cs`](../../src/EventStore.Inbox/PublishService.cs) no
longer validates schema/upcast compatibility at all — it authenticates,
checks idempotency, checks the two still-real blocking cases (unregistered
event type, Strict-mode unresolved parent), and appends unconditionally.
[`PublishResult.cs`](../../src/EventStore.Inbox/PublishResult.cs)'s
`Created`/`IdempotentReplay`/`ValidationFailed` cases are gone, replaced by
one `Accepted` case matching `ADR-023`'s envelope
(`correlationId`/`status`/`entityId`/`schemaStatus`/`authorityStatus`/
`conflictFlag`/`reason`) — a schema-invalid payload is `Accepted` too now,
just with `SchemaStatus` left `null` until the Router evaluates it.
[`PublishEndpoints.cs`](../../src/EventStore.Inbox/PublishEndpoints.cs)
returns `202 Accepted`, not `201 Created`, for every successful publish.

**`EventUpcastFailed` (item 11's own `ADR-020` dead-letter mechanism) is
retired** — `ADR-023`'s persist-everything posture already said this
would happen ("reframed as one instance of this general posture, not a
special case"); this pass is where it actually landed. A publish-time
upcast-hop failure is no longer a substituted synthetic event type; the
*original* event just persists with `SchemaStatus: invalid`, like any
other schema problem. `docs/adrs/adr-020-schemaversion-on-publish.md` and
`docs/08-build-plan.md`'s "Hardening & Evolution" section both got the
required strikethrough-and-pointer note (additive-history convention) —
the mechanism was correctly built and tested for item 11's own exit
criteria at the time; this is a real, later supersession, not a bug caught
after the fact. `EventStore.Upcasting`/`UpcastChain` themselves are
unaffected — Follow's own read-time upcasting (`ADR-018`) still uses them
exactly as before.

## New shape: `EntityStoreRow`

[`EntityStoreRow.cs`](../../src/EventStore.Domain/EntityStore/EntityStoreRow.cs) —
built to `docs/data/entity-store.md`'s full already-documented column set
(including `ShardKey`/`AuthorityStatus`/`LastAppliedOriginId`, columns this
item never actively sets) rather than a narrower subset, the same "avoid a
second wave of migrations" precedent `StoredEvent` itself established in
"Scaffolding & Persistence." Migration `AddEntityStore` (all 3 providers) —
SQL Server's own migration also narrows `Events.EntityId` from
`nvarchar(max)` to `nvarchar(450)`, EF Core's own required adaptation to
add an index on that column (SQL Server can't index a MAX-length column
directly), not something this pass did deliberately.

## Two real design gaps found and fixed while wiring `RouterWorker`, both while writing tests, not by reading the code back

1. **`StoredEvent` carried no `AppId`.** `docs/10-open-questions.md`'s
   former row 1 already named this exact ambiguity for read-side claim
   checks, and predicted its own resolution: "until either `EntityId`
   populates... or a dedicated fix lands." `EntityId` populating is
   exactly this item's own job, and constructing it correctly turned out
   to *need* a real `AppId` — the bare-name/tie-broken-by-`AppId`-ordering
   workaround Follow/Lineage already use is fine for *visibility* (worst
   case: a caller sees or doesn't see something) but actively corrupts
   *identity* when two different `AppId`s register the same event-type
   name (exactly what the row's own text warned the DB schema permits).
   Fixed by adding [`StoredEvent.AppId`](../../src/EventStore.Domain/EventLog/StoredEvent.cs)
   (set by `PublishService` from the request's own `AppId`), migrated
   across all 3 providers, and used directly by `RouterWorker` instead of
   any bare-name lookup. The former open-questions row is deleted (the
   fork is genuinely decided now); a residual, scoped follow-up — rewiring
   Follow/Lineage's *own* bare-name claim lookups to use this same field
   directly instead of their tie-break — is tracked in `TODO.md`, not
   silently dropped.
2. **`EventTypeDefinition` had no field grouping multiple event types
   under one logical entity.** `OrderPlaced` and `OrderShipped` are two
   different event types that both patch the same `Order` entity — first
   attempt constructed `EntityId` from each event's own `EventType`
   directly, which gave `OrderPlaced` and `OrderShipped` events for the
   *same* order two different `EntityId`s entirely (caught by
   `PublishingASecondEventForTheSameEntityIdUpdatesTheRowAndIncrementsVersion`
   asserting `Version 2` and getting `1` — the second event silently
   folded into a *different*, newly-created row instead). Fixed by adding
   [`EventTypeDefinition.EntityType`](../../src/EventStore.Domain/SchemaRegistry/EventTypeDefinition.cs)
   (defaults to the type's own normalized name — a genuinely *safe*
   default, unlike `EntityIdField`/`ChangeKind`'s "no safe default"
   posture, since a type trivially patches "itself" unless told to share),
   migrated across all 3 providers, documented in
   `docs/data/schema-registry.md`.

## New: `EntityScenarioAssertions.cs`

Mirrors [`docs/features/entity-concept.md`](../features/entity-concept.md)'s
Gherkin one-for-one (new-entity creation, `Version` increment, `Full`
wholesale replace, `Partial` unknown-property → `Extensions`,
`ConflictFlag`, `LateArrivalFlag`, both-flags-independently, no-
`ExpectedVersion` unconditional apply, schema-invalid-persists-and-folds-
known-properties, declared-vs-active-version validation) — the one
scenario file in this build stage that drives `RouterWorker.RunOnceAsync`
directly after publishing, since "Publish API"'s own
`PublishScenarioAssertions.cs` deliberately stays synchronous-only. New
`EntitySqliteTests.cs`/`EntityPostgresTests.cs`/`EntitySqlServerTests.cs`.

**Test-authoring bug, not a production bug**: the `LateArrivalFlag`
scenarios first used fixed calendar-literal `OccurredAt` values
(`"2026-08-01T12:00:00Z"`) without accounting for the *other* event in the
same scenario (`OrderPlaced`) being stamped with real
`DateTimeOffset.UtcNow` at publish time — in this environment that's
already `2026-08-04`, so the literal landed *behind* `OrderPlaced`'s own
high-water mark and `OrderPlaced` itself looked like the late arrival.
Fixed by computing every `OccurredAt` in those scenarios relative to a
`DateTimeOffset.UtcNow` baseline captured once at the scenario's start,
never a fixed literal.

## Test count

`EventStore.IntegrationTests` now has 26 passing `[TestMethod]`s (up from
23) — the 3 new `EntitySqliteTests`/`EntityPostgresTests`/
`EntitySqlServerTests` classes, each running all 10
`EntityScenarioAssertions` scenarios. All 26 pass across SQLite/
PostgreSQL/SQL Server.

# Item 13, "Multi-Tenancy"

Also Done, same day, continuing directly from item 12. Much smaller than
11/12 — `ADR-030`'s own Consequences already said so explicitly:
"multi-tenancy was closer to already built than not." `EventTypeDefinition`
already keyed by `(AppId, Name, Version)` since "Schema Registry" (item 2);
`StoredEvent.EntityId`/`AppId` already disambiguate applications since
item 12. The one genuinely new piece: `registry:admin` gains optional
`AppId`-scoped variants (`registry:admin:{appId}`), so a caller can be
scoped to administer exactly one application's schemas, not the whole
framework.

- [`ScopeRequirement.cs`](../../src/EventStore.Host.Core/ScopeRequirement.cs) —
  `ScopeAuthorizationHandler`'s coarse gate now also accepts any
  `{requiredScope}:*`-shaped scope, not just the exact unscoped string, so
  a caller holding only `registry:admin:tenant-a` still reaches the
  endpoint (rather than being rejected before its own, finer-grained check
  ever runs).
- [`AppIdScopeEvaluator.cs`](../../src/EventStore.SchemaRegistry/AppIdScopeEvaluator.cs) —
  the fine-grained check: the unscoped `registry:admin` form still works
  across every `AppId` (nothing already issued or seeded breaks, per
  `ADR-030`'s own text); the scoped form only for its own `AppId`.
  [`SchemaRegistryEndpoints.cs`](../../src/EventStore.SchemaRegistry/SchemaRegistryEndpoints.cs)
  calls it on every registry read/write route (`PUT`, both `GET`s, and the
  `QUERY` listing) — a deliberate choice to check this at the HTTP/endpoint
  layer, not inside `SchemaRegistryService` itself, so the service's own
  public API (and the ~15 test files constructing it directly) needed zero
  changes.
- [`DevIdpSeeder.cs`](../../src/EventStore.DevIdp/DevIdpSeeder.cs) gains a
  5th seeded client, `tenant-a-operator-client`, holding only
  `registry:admin:tenant-a` — the concrete demonstration this dev/POC IdP
  needed; a real deployment would provision these per-tenant dynamically,
  not via a fixed registered scope list like this one.
- `SchemaRegistryScenarioAssertions.RegisteringSameNameUnderDifferentAppIdIsIndependent`
  (item 2) already covered "different shapes/`ChangeKind` don't collide"
  across two `AppId`s; extended to also vary `RequiredClaims` per `AppId`,
  closing the one part of this item's own exit criteria ("shapes/claims/
  `ChangeKind`") that test hadn't actually touched yet.

**Real RFC 9449 bug found while writing the new AppId-isolation test, not
a test-only mistake**: the new scenario's read-side check
(`GET /registry/{eventType}?appId=tenant-b`) is the first request in this
whole suite to carry a query string through `AttachAuth` — and `AttachAuth`
built `htu` from the request's full URI, query string included, while
`DpopValidationMiddleware`'s own `expectedHtu` is built from
`HttpRequest.Path` alone (correctly excluding the query string, per RFC
9449). Every prior POST/PUT test happened to have no query string, so this
mismatch was latent, not yet triggered. Fixed both
[`AuthScenarioAssertions.AttachAuth`](../../tests/EventStore.IntegrationTests/AuthScenarioAssertions.cs)
(test) and [`FollowClient.AttachAuth`](../../src/EventStore.Projections.Host/FollowClient.cs)
(production — not yet exercised with a query string in practice, but
carrying the identical latent bug) to strip to `UriPartial.Path` before
building the proof.

## Test count

`EventStore.IntegrationTests` still has 26 passing `[TestMethod]`s (one
new scenario added to the existing `AllAuthScenarios` and
`AllSchemaRegistryScenarios` methods each, not new test classes). All 26
pass across SQLite/PostgreSQL/SQL Server.

# Item 14, "Upcast Materialization + Downcast"

Done, same day, continuing directly from item 13. Both `ADR-027`
(materialization, two triggers) and `ADR-028` (downcast, read-time only)
built and tested, plus `ADR-053`'s second `IUpcastExpressionEvaluator`
(`Jsonata.Net.Native`) added from scratch — no C# implementation of it
existed anywhere in `src/` before this pass, despite
[`jsonata-dotnet.md`](../libraries/dotnet/jsonata-dotnet.md) already
describing it as "a real, supported alternative." `docs/08-build-plan.md`'s
status table and `BuildPlan_CorePhases` diagram fill both updated to
Done/`#palegreen` in this pass.

## Materialization (`ADR-027`)

- [`EventAppender.cs`](../../src/EventStore.Persistence/EventAppender.cs) —
  the hash-chain-aware insert (`Serializable`-isolation read-prior-hash,
  insert, compute-and-update `ChainHash`) extracted out of
  `PublishService.InsertEventAsync` into a shared, claims-agnostic
  primitive, so both `PublishService` (external, claims-checked publishes)
  and the materializer below (internal, already-validated reshapes) append
  through the identical path without either one depending on the other.
- [`UpcastMaterializer.cs`](../../src/EventStore.Router/UpcastMaterializer.cs) —
  `TryMaterializeAsync` (shared logic: resolve the version gap, run
  `UpcastChain`, validate against the active schema, append via
  `EventAppender` directly — deliberately not `PublishService.PublishAsync`,
  since re-running `RequiredClaims` enforcement against an empty system
  principal would wrongly `Forbidden` the materialization of any
  claim-gated type) and `ReconcileBacklogAsync` (Trigger 2: scans every
  active, multi-version `EventTypeDefinition` for conformant `Original`
  events still behind it, skipping any already materialized).
- [`RouterWorker.cs`](../../src/EventStore.Router/RouterWorker.cs) — Trigger
  1 added inline right after entity resolution/fold, gated on
  `SchemaStatus == "conformant" && SchemaVersion < activeDefinition.Version`;
  a defensive (currently dead-code-in-practice) early-return skips folding
  any `EventKind.UpcastMaterialization` row; Trigger 2 runs once per tick,
  after every "received" event that tick has been processed.

**A real correctness bug found only by running the full suite, not by
reading the code back**: `EventTailReader` originally queried
`db.Events.Where(e => e.EventType == eventTypeName && ...)` with no
`EventKind` filter at all, so once a materialization existed it was
delivered over Follow/SSE as an independent new event — double-delivering
one logical fact. This stayed invisible through every scenario this item's
own tests constructed (each registers exactly one v1-to-v2 pair once), and
only surfaced when the full suite ran: `ProjectionsSqliteTests` — a test
this item never touched — calls its own `RegisterTheFourOrderEventTypesAsync`
helper at the top of every scenario in that file, with the byte-identical
schema each time; `SchemaRegistryService.RegisterAsync` unconditionally
bumps the version on every call regardless of whether the schema actually
changed, so by the sixth scenario those four event types were sitting at
`Version` 6+, `IsActive`, still with no real `UpcastFromPrevious`. That's
exactly `ReconcileBacklogAsync`'s trigger condition (`IsActive && Version >
1`) — for the first time in the whole suite, Trigger 2 fired for real,
walked every historical event for orders `o-1` through `o-6` (each hop's
`UpcastFromPrevious` being empty just passes the payload through
unchanged, so every materialization trivially succeeded), and
`IncrementalResumeAfterDowntimeDeliversNoGapAndNoDuplicate` started
observing 2 delivered events where it expected 1. `ADR-027`'s own
Consequences already named the fix, unbuilt until now: Follow's default is
"consuming only originals and always upcasting live remains equally
correct" — serving a materialization instead is an optional, opt-in
optimization for other readers, never something the live tail should do
unprompted. Fixed by adding `e.EventKind == EventKind.Original` to
`EventTailReader`'s own query
([`EventTailReader.cs`](../../src/EventStore.Follow.Api/EventTailReader.cs)) —
confirmed by re-running the full suite twice in a row afterward, clean
both times.

## Downcast (`ADR-028`)

- [`DowncastChain.cs`](../../src/EventStore.Upcasting/DowncastChain.cs) —
  mirrors `UpcastChain`'s shape exactly, walking backward (`fromVersion`
  down to `toVersion`), but with no safe pass-through: a hop with no
  registered `DowncastToPrevious` is a hard `UpcastOutcome.Failed`, per
  this ADR's own "no guessing" stance (unlike upcasting's "no upcaster
  registered — pass through unchanged").
- [`FollowRequest.cs`](../../src/EventStore.Follow.Api/FollowRequest.cs)
  gained `AsOfSchemaVersion` — GraphQL doesn't exist at this build stage,
  so this is Follow's own SSE-surface translation of the doc's literal
  transport, the same translation every prior item has made.
- [`FollowService.cs`](../../src/EventStore.Follow.Api/FollowService.cs) —
  validates at connect time, before the tail loop ever starts: every hop
  between the requested version and the active one must have a registered
  `DowncastToPrevious`, or the request fails `ValidationFailed` (400)
  immediately, matching the Gherkin's "the request should fail with a 400"
  rather than discovering the gap mid-stream.
- [`EventTailReader.cs`](../../src/EventStore.Follow.Api/EventTailReader.cs) —
  applies `DowncastChain` after `UpcastChain` (an event can need upcasting
  to the active version, then downcasting again to the caller's requested
  older one, in the same pass), and masks against the requested version's
  schema, not the active one — a downcasted payload's field names belong
  to the old shape, so masking must check the old shape's own `x-masking`
  annotations, not the active version's.

## `ADR-053`'s second engine: `Jsonata.Net.Native`

[`JsonataUpcastExpressionEvaluator.cs`](../../src/EventStore.Upcasting/JsonataUpcastExpressionEvaluator.cs) —
`Jsonata.Net.Native` has its own JSON DOM
(`Jsonata.Net.Native.Json.JToken`), unrelated to `System.Text.Json`, so
rather than hand-writing a CLR-object bridge the way
`CelUpcastExpressionEvaluator` does for Cel.NET's variable bindings, this
implementation round-trips through the JSON text form — simpler, and
equally correct since `JsonataQuery.Eval` only ever needs a `JToken` in
and a `JToken` out. The harder part: making the same registered
`UpcastFromPrevious` string genuinely evaluate identically under both
engines, not just produce visually similar output for one hand-picked
example. CEL's own evaluator binds the source payload to a variable named
`event` (`Decls.NewVar("event", Decls.Dyn)`), so expressions read
`event.Amount`; JSONata has no equivalent concept — its input is the
context itself, so a bare payload would need `Amount` with no prefix at
all, which would make the registered expression text engine-specific
despite `ADR-053`'s own "engine-agnostic text" framing. Fixed by wrapping
the source payload under a synthetic top-level `"event"` key
(`{ "event": <payload> }`) before evaluating — `event.Amount` then
resolves as an ordinary JSONata path segment, identically to CEL's own
variable binding, for the exact same source text.
[`UpcastExpressionEvaluatorTests.cs`](../../tests/EventStore.IntegrationTests/UpcastExpressionEvaluatorTests.cs)
proves this directly: the same `"event.Amount as Amount, 'Unknown' as
Status"` string run through both evaluators produces byte-identical JSON
output, for both a single-hop and a two-hop chain, plus a JSONata-only
`$sum()` array-aggregation test (`ADR-053`'s own stated reason CEL can't
cover this case) and a compile-rejection test for both engines.

## Stale doc fixes

[`upcast-materialization-and-downcast.md`](../features/upcast-materialization-and-downcast.md)'s
two Trigger sequence diagrams predated this item's actual implementation
and had drifted from it in three ways, all corrected with a
strikethrough-and-pointer note rather than a silent rewrite: (1) Trigger
1's diagram showed `PublishEndpoint` running `UpcastChain` synchronously
in-request and returning `202` only after — the actual `ADR-023`
Inbox/Router split means the Inbox appends unconditionally and returns
`202` immediately, with all validation and Trigger 1 itself happening
asynchronously on a later `RouterWorker` tick; (2) the diagram's failure
branch still inserted an `"EventUpcastFailed"` event — retired entirely by
item 12, well before this diagram was even written; (3) Trigger 2's prose
and diagram described `UpcastMaterializer` as "an internal follower"
tailing the public Follow API and republishing through the ordinary
publish path — the actual implementation runs as an extra step inside
`RouterWorker`'s own existing tick (matching that worker's own "combine
responsibilities into one process" posture) and appends via
`EventAppender` directly, for the same claims-re-gating reason named
above.

## Test count

`EventStore.IntegrationTests` now has 33 passing `[TestMethod]`s (up from
26): `UpcastExpressionEvaluatorTests` (4, pure — no DB/provider needed),
`UpcastMaterializationSqliteTests`/`Postgres`/`SqlServer` (3, new,
covering Trigger 1, Trigger 2, and the fold-skip regression), and two new
scenarios threaded into each existing `FollowSqliteTests`/`Postgres`/
`SqlServer`'s `AllFollowScenarios` (downcast success + missing-hop
failure). All 33 pass across SQLite/PostgreSQL/SQL Server, run twice in a
row for stability.

# Item 15, "Streaming Channels"

Done, same day, continuing directly from item 14. The largest single item
this session — a genuinely separate data plane (`ADR-031`), not an
extension of the event log. `docs/08-build-plan.md`'s status table and
`BuildPlan_CorePhases` diagram fill both updated to Done/`#palegreen` in
this pass.

## Domain model and persistence

[`TelemetryChannel.cs`](../../src/EventStore.Domain/Streaming/TelemetryChannel.cs)/
[`TelemetrySample.cs`](../../src/EventStore.Domain/Streaming/TelemetrySample.cs)/
[`RedactedRange.cs`](../../src/EventStore.Domain/Streaming/RedactedRange.cs)/
[`TelemetryPointerEntry.cs`](../../src/EventStore.Domain/Streaming/TelemetryPointerEntry.cs) —
shape taken directly from `docs/data/streaming-and-attachments.md`.
`TelemetryChannel` gained three fields the data-model doc didn't originally
carry — `LastAppliedLogicalTime`/`LastBatchReceivedAt`/
`LastSampleTimestampReceived`, ingestion's own mutable high-water-mark/
lag-detection state — added to that doc in the same pass, per this
repo's own "the code that adds a field lands the doc edit too" rule.
Three new EF Core migrations (`AddStreamingChannels`, all 3 providers).
`PublishEventRequest`/`PublishService` gained `TelemetryPointer`
(`List<TelemetryPointerEntry>?`, JSON-serialized onto `StoredEvent.
TelemetryPointer` — a column that already existed, unused, since
"Scaffolding & Persistence" built the full `StoredEvent` shape up front).

## New `EventStore.Streaming` project

- [`ChannelRegistryService.cs`](../../src/EventStore.Streaming/ChannelRegistryService.cs) —
  registration (`PUT /telemetry/channels/{channelId}`); deliberately no
  version history the way `EventTypeDefinition` has — a channel's declared
  shape doesn't evolve the way a payload schema does.
- [`TelemetrySampleWriter.cs`](../../src/EventStore.Streaming/TelemetrySampleWriter.cs) —
  batch ingestion (`POST /telemetry/{channelId}/samples`), both the
  fixed-rate (`startTimestamp`+`sampleIntervalMicros`+`values[]`) and
  irregular (`samples: [{timestamp, value}]`) shapes. `ADR-029`'s high-
  water-mark mechanism reused per-channel for `LateArrivalFlag` — a late
  sample is still written, never dropped, never reordered, and never
  moves the mark backward. Slow-upload detection compares the gap since
  the channel's own `LastBatchReceivedAt` against `SampleIntervalMicros`
  (its `ExpectedInterArrivalInterval`) x a configurable multiplier
  (`TelemetryIngestOptions.LagThresholdMultiplier`, default 3.0);
  exceeding it publishes a reserved, lazily-registered `ChannelLagDetected`
  event through the completely ordinary `PublishService.PublishAsync`
  path (unlike `ADR-027`'s `UpcastMaterializer`, there's no claims-bypass
  concern here — `ChannelLagDetected` declares no `RequiredClaims`).
- [`TelemetryTailReader.cs`](../../src/EventStore.Streaming/TelemetryTailReader.cs) —
  `ADR-010`'s tail/replay shape reused for `TelemetrySample` (`GET
  /telemetry/{channelId}/samples?mode=&fromTimestamp=`), plus `ADR-081`'s
  `ThreadId`-grouped session view (`GET /telemetry/sessions/{threadId}/
  samples`) presenting every channel sharing one `ThreadId` as a single
  merged stream, not N unrelated ones. Applies `RedactedRange` substitution
  at read time (never materialized) via the resolver below, plus the
  mandatory sideband existence flag (`TelemetrySampleView.
  RedactionAppliedFlag`) — structurally the same shape `LateArrivalFlag`
  already uses.
- [`IStreamRedactionStrategy.cs`](../../src/EventStore.Streaming/IStreamRedactionStrategy.cs) +
  [`ZeroFillStrategy.cs`](../../src/EventStore.Streaming/ZeroFillStrategy.cs)/
  [`ToneStrategy.cs`](../../src/EventStore.Streaming/ToneStrategy.cs)/
  [`BlankFrameStrategy.cs`](../../src/EventStore.Streaming/BlankFrameStrategy.cs) —
  `ADR-052`'s `ContentKind`-appropriate defaults, honestly scoped: with
  zero codec knowledge, `Tone`/`BlankFrame` substitute a fixed, distinctive
  byte pattern rather than a real decoded tone/frame, documented as such
  in each class's own comment.
- [`PartialRevealStreamRedactionStrategy.cs`](../../src/EventStore.Streaming/PartialRevealStreamRedactionStrategy.cs) —
  genuinely reuses `PartialRevealMaskingStrategy`'s reveal computation (not
  a second implementation) via a UTF-8 decode/re-encode round trip, since
  that strategy's own shape operates on `JsonNode`, not raw bytes.
- [`StreamRedactionResolver.cs`](../../src/EventStore.Streaming/StreamRedactionResolver.cs) —
  resolves the concrete keyed strategy from `(RedactedRange.Strategy,
  TelemetryChannel.ContentKind, MimeType)` — `"Default"` doesn't name one
  fixed class the way `IPayloadMasker`'s config does; it means "whichever
  substitution `ADR-052` designates for this channel's own shape."
- [`ChannelDerivationWorker.cs`](../../src/EventStore.Streaming/ChannelDerivationWorker.cs) —
  the `ADR-007`/`ADR-015`/`ADR-027` "internal follower" shape, applied to
  telemetry: tails a `Derived` channel's source(s), applies the transform,
  appends through the same ingestion path any other writer uses. Only
  `Resample` is implemented (decimation — keep the last source sample per
  target-rate bucket); `Filter`/`Aggregate`/`Transcode` are accepted values
  the worker takes no action for, flagged explicitly in `08-build-plan.md`'s
  own section rather than silently no-opped without a trace. Progress is
  tracked via the derived channel's own `LastAppliedLogicalTime` — the
  same field ingestion already maintains, no new checkpoint shape needed.
- [`MediaFragmentUri.cs`](../../src/EventStore.Streaming/MediaFragmentUri.cs)/
  [`MediaFragmentResolver.cs`](../../src/EventStore.Streaming/MediaFragmentResolver.cs) —
  the W3C Media Fragments URI temporal syntax (`#t=10,20`), resolved
  against a channel's own earliest sample as the time-zero reference,
  returning the interconvertible `TelemetryPointerEntry` shape directly.
  Exposed as a plain service, not an HTTP endpoint — a URI *fragment* is
  never sent to a server by a browser, so there is nothing for an endpoint
  to receive; this matches the exit criterion's own framing ("the resolved
  window should be equivalent to...") exactly.
- [`StreamingEndpoints.cs`](../../src/EventStore.Streaming/StreamingEndpoints.cs) —
  one GET route dual-modes by `Range` header presence, ordinary HTTP
  conditional-request semantics: with it, `Results.Bytes(...,
  enableRangeProcessing: true)` serves real `206 Partial Content` byte-
  range playback over the channel's concatenated chunk bytes (RFC 7233);
  without it, the live JSON/SSE tail/replay stream, matching
  `FollowEndpoints`'s own SSE-writing shape exactly.

New `telemetry:ingest`/`telemetry:read` auth policies/scopes
(`HostCoreExtensions.cs`, `DevIdp/Program.cs`) and a seeded
`telemetry-client` (`DevIdpSeeder.cs`) holding both, since this repo's own
tests drive both the producer and detector/follower roles from one caller.

## Two real bugs found only by running the tests, not by reading the code back

**SQLite's EF provider cannot translate a `DateTimeOffset` relational
comparison (`>`/`<`), a `MIN`/`MAX` aggregate, or an `ORDER BY`** — the
exact same class of gap already found once before, for `PendingJoinState`'s
TTL sweep ("Derived/Materialized Event Types"). This time it hit three
separate spots, each caught by an actual failing test run, not inspection:
`TelemetryTailReader`'s own cursor/poll queries, `MediaFragmentResolver`'s
earliest-sample lookup, and `StreamingEndpoints`'s Range-request byte-
concatenation ordering. Fixed identically each time — filter on the
translatable column (`ChannelId`) in the query, then compare/aggregate/
order the results client-side.

**A real DI wiring bug, not SQLite-specific — just first surfaced by the
one test using the real Host composition root instead of a hand-built
test container**: `EventStore.Masking.AddMasking()` registers
`PartialRevealMaskingStrategy` **keyed** (`"PartialReveal"`, for
`IPayloadMasker`'s own resolution), not as a plain unkeyed service.
`PartialRevealStreamRedactionStrategy` originally took it as an injected
constructor dependency, unkeyed, and failed to resolve against the real
Host's DI graph (`StreamingHttpSqliteTests`'s `ClassInit` threw). Fixed by
constructing it directly instead (`new PartialRevealMaskingStrategy()`) —
that class has no dependencies of its own, so injecting it was never
actually necessary.

## One real test-design bug, not a production bug

Two `StreamingScenarioAssertions` methods
(`ADeliberatelyReorderedSampleSetsLateArrivalFlagWithoutMovingTheHighWaterMark`
and `ASlowUploadingProducerTriggersAChannelLagDetectedEvent`) originally
shared one literal `"streaming-app"` `AppId`, and each makes two sequential
`IngestAsync` calls against a 4000-microsecond-interval channel. On
SQLite's fast in-process round trips this never mattered; against a real
Postgres/SQL Server Testcontainer, the genuine wall-clock gap between two
awaited calls routinely exceeds the 3x-interval lag threshold, so BOTH
methods' own incidental `ChannelLagDetected` events collided under one
shared `AppId` — `SingleOrDefaultAsync` threw "Sequence contains more
than one element" on `StreamingSqlServerTests` specifically (SQLite's own
faster round trips happened not to trigger it, making this provider-
dependent in practice, not provider-specific in cause). Fixed by giving
every scenario method its own unique `AppId` — the convention every other
`*ScenarioAssertions.cs` file in this repo already follows (e.g.
`entity-demo-1`, `entity-demo-2`, ...); this file should have followed it
from the start rather than reusing one shared literal.

## Test count

`EventStore.IntegrationTests` now has 37 passing `[TestMethod]`s (up from
33): `StreamingSqliteTests`/`Postgres`/`SqlServer` (3, 11 scenarios each —
batch ingestion isolation, the TelemetryPointer round trip, correlated
multi-channel pointers, late-arrival flagging, `ChannelLagDetected`,
`ThreadId`-grouped sessions, `RedactedRange` substitution both with and
without the claim, `PartialReveal` substitution, `Resample` derivation,
and Media Fragment resolution) and one dedicated real-HTTP
`StreamingHttpSqliteTests` for the Range-request/206 behavior specifically
(only observable through the actual ASP.NET Core response-writing
pipeline, not a direct service call). All 37 pass across SQLite/
PostgreSQL/SQL Server, run twice in a row for stability.

# Item 16, "Binary Attachments"

Done, same day, continuing directly from item 15. Smaller than the last
several items — a discrete binary object (a supporting document, a scanned
form) uploaded once, not sampled continuously, so most of the read-path
machinery (Range requests, claim-gated access) reuses what item 15 already
built rather than inventing a second version. `docs/08-build-plan.md`'s
status table and `BuildPlan_CorePhases` diagram fill both updated to
Done/`#palegreen` in this pass.

## Domain model and new `EventStore.Attachments` project

[`Attachment.cs`](../../src/EventStore.Domain/Streaming/Attachment.cs) —
`Attachment`/`ChunkRef`/`AttachmentRef`, shape from `docs/data/streaming-
and-attachments.md` (kept in the same `Streaming` namespace that doc
already groups both ADRs under, even though `ADR-032` is its own separate
decision from `ADR-031`). Two migrations gaps closed in this same pass,
per this repo's own "the ADR that decides a field is that field's shape
authority" rule:

- **`Attachment.RequiredReadClaim`/`RequiredPublishClaim` were decided in
  `ADR-032`'s own "Standalone attachments and direct permissions" section
  but never actually added to the data model doc** — a direct claim on
  the attachment always governs if set, even over a linked event's/
  entity's own claim (that inheritance chain is explicitly not designed
  further in the ADR, and not built here either — only the direct-claim
  check). Added to `docs/data/streaming-and-attachments.md` and to code
  in the same pass.
- **`AttachmentRef` needed a real primary key** — `EntityId`/`EventId`
  are both independently nullable, so there's no clean natural composite
  key the way `EventParent` has one. Added a surrogate `Id`.

- [`AttachmentService.cs`](../../src/EventStore.Attachments/AttachmentService.cs) —
  `UploadAsync` (SHA-256 content hash, real dedup: identical bytes return
  the existing hash, never a second stored copy), `RetrieveAsync` (direct-
  claim check, `LastAccessedAt` touched on every read — the field a future
  tiering mover's "access-pattern threshold" would check), `LinkAsync`
  (creates an `AttachmentRef` row — dedup makes an attachment many-to-many
  with entities/events by construction, not extra design effort).
- [`AttachmentEndpoints.cs`](../../src/EventStore.Attachments/AttachmentEndpoints.cs) —
  `POST /attachments` (raw bytes in the body; `MimeType`/`FileName`/claims
  travel as headers/query, never wrapped in a JSON envelope) and
  `GET /attachments/{contentHash}`, reusing the *identical*
  `Results.Bytes(enableRangeProcessing: true)` mechanism item 15 already
  proved for `Media` channel playback — RFC 7233 Range requests, real
  `206 Partial Content`.
- [`IAttachmentContentStore.cs`](../../src/EventStore.Attachments/IAttachmentContentStore.cs)/
  [`InMemoryAttachmentContentStore.cs`](../../src/EventStore.Attachments/InMemoryAttachmentContentStore.cs) —
  the keyed-DI seam `ADR-032`'s own Consequences names ("the exact same
  Strategy-pattern/keyed-DI shape `ADR-057`'s `IErasureKeyStore` already
  established"), and which "Event Log/AccessLog Archival Segment
  Detachment" later depends on, reused unchanged. Built for real (a
  genuine interface plus a working, round-trip-testable dev/POC
  implementation), but deliberately **not** wired into `AttachmentService`'s
  own default upload path — `ADR-032`'s own field comment
  ("`ContentProviderKey`: null means 'this table'") makes `Attachment.
  Bytes` itself the v1 storage; the seam exists and is registrable, ready
  for a real backend or a future tiering mover to actually use.

`PublishEventRequest` gained `AttachmentContentHashes` — completes
`ADR-032`'s two-step handoff (`POST /attachments` returns a hash; an
ordinary publish carries it) by creating the `AttachmentRef` row through
`PublishService` directly, the identical envelope-field pattern
`TelemetryPointer` already established for item 15. `EntityId` is left
null at link time (entity resolution stays the async Router's job,
`ADR-021`, unaffected by this item).

New `attachments:ingest`/`attachments:read` auth policies/scopes and a
seeded `attachments-client` (both roles, matching `telemetry-client`'s own
posture).

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

`ADR-032`'s Consequences describe a hot/cool/cold tiering background mover
and content-defined chunking (rolling-hash chunk boundaries, independent
per-chunk storage) for large attachments — real, decided mechanisms, but
outside this item's own Scope line. `Attachment.ChunkIndex`/`ChunkRef`
exist in the data model (so a later pass has the shape ready) but are
never populated; no background mover exists. Same honest-gap treatment as
item 15's unimplemented `TransformKind`s — flagged in place, not silently
dropped.

## Test count

`EventStore.IntegrationTests` now has 41 passing `[TestMethod]`s (up from
37): `AttachmentSqliteTests`/`Postgres`/`SqlServer` (3, 7 scenarios each —
dedup, many-to-many linking via two `AttachmentRef` rows, the publish-path
link, direct-claim read gating both with and without the claim, a
standalone attachment's claim with no link to inherit from, `RequiredPublishClaim`
gating re-upload specifically, and not-found) and one dedicated real-HTTP
`AttachmentHttpSqliteTests` for the Range-request/206 behavior. All 41
pass across SQLite/PostgreSQL/SQL Server, run twice in a row for
stability.

# Item 17, "Sharding & Replication"

Done, same day, continuing directly from item 16. The largest remaining
core-build item: a gossip-topology multi-site mesh where every event
carries `OriginId`/`LogicalClock`, a fault/abend/restart-tolerant
peer-sync outbox/inbox reusing `ADR-023`'s Inbox durability primitive
outright, and peer discovery via static `SeedPeers` plus a gossip
`knownPeers` exchange. `docs/08-build-plan.md`'s status table and
`BuildPlan_CorePhases` diagram fill both updated to Done/`#palegreen` in
this pass.

## Hybrid Logical Clock (`ADR-033`)

[`HybridLogicalClock.cs`](../../src/EventStore.Domain/EventLog/HybridLogicalClock.cs) —
Kulkarni et al.'s algorithm, chosen over a vector clock specifically
because its size stays bounded regardless of mesh size (`ADR-033`'s own
stated reason). `Next(previousLocal, observedRemote, now)`: the physical
component is `max(wall-clock, prior local physical, observed remote
physical)`; the logical component increments on a genuine tie between
components and resets to `0` the moment physical time actually advances
past both inputs. Formatted `"{physicalTicks:D19}-{logicalCounter:D10}"`
specifically so plain string comparison sorts correctly — no custom
comparer needed anywhere it's stored or queried.

[`EventAppender.AppendAsync`](../../src/EventStore.Persistence/EventAppender.cs)
gained an `observedRemoteClock` overload, computing
`StoredEvent.LogicalClock` in the same read-prior-row-then-compute
transaction it already used for `ChainHash` — the two hardening
mechanisms (tamper evidence, causal ordering) now advance together, one
read of the prior row serving both.

## `OriginId` and the peer-sync outbox/inbox (`ADR-033`)

New [`OriginIdOptions.cs`](../../src/EventStore.Inbox/OriginIdOptions.cs)
(`OriginId` defaulting to `"local"`) — `PublishService` gained an
*optional* `IOptions<OriginIdOptions>?` constructor parameter specifically
so the ~26 existing 3-arg test construction sites across every earlier
item needed zero changes; only sites that care about multi-site identity
(the new Replication tests, and eventually a real multi-site deployment's
own `appsettings`) supply it.

New [`PeerSyncCursor.cs`](../../src/EventStore.Domain/Replication/PeerSyncCursor.cs) —
shape verified against `docs/data/schema-registry.md` before writing any
code, per this repo's standing "verify before citing" rule:
`{PeerId, LastReceivedSequenceNumber, LastAckedSequenceNumber,
LastSyncAttemptAt, LastSyncSuccessAt}`, no `Address` field (an address is
resolved at runtime via gossip, never persisted as this cursor's own
identity).

New `EventStore.Replication` project:

- [`PeerAddressBook.cs`](../../src/EventStore.Replication/PeerAddressBook.cs) —
  in-memory only, seeded from `PeerSyncOptions.SeedPeers`. Proves
  `ADR-051`'s own requirement ("no automatic discovery, but the gossip
  protocol itself performs transitive mesh discovery") concretely: every
  push request/response round trip carries `KnownPeers`, merged into the
  receiving site's own address book via `TryAdd` (never overwrites an
  already-resolved `PeerId` for an address already known).
- [`PeerSyncClient.cs`](../../src/EventStore.Replication/PeerSyncClient.cs)/
  [`PeerSyncReceiver.cs`](../../src/EventStore.Replication/PeerSyncReceiver.cs) —
  the receiver dedupes incoming events by `EventId` and appends via
  `EventAppender.AppendAsync` directly, **bypassing `PublishService`
  entirely**: the original event already passed claims/schema/parent-link
  checks once, at its own origin site, on its own `PublishService.
  PublishAsync` call — re-running those checks against an empty system
  principal at the receiving site would be wrong, the identical reasoning
  item 14's `UpcastMaterializer` already established for materialized
  events.
- [`PeerSyncWorker.cs`](../../src/EventStore.Replication/PeerSyncWorker.cs) —
  a `BackgroundService` wrapping a public static `RunOnceAsync`, the same
  "exercise the mechanics directly via a testable static method" shape
  this repo has now used three times (`RouterWorker` →
  `ChannelDerivationWorker` → `PeerSyncWorker`). Iterates every known
  address, resolves its `PeerId` via `/peer-sync/whoami` if not yet known,
  skips itself, pushes pending events past `LastAckedSequenceNumber`, and
  wraps each peer's sync attempt in its own try/catch so one unreachable
  peer never blocks sync to the rest of the mesh.
- [`PeerSyncEndpoints.cs`](../../src/EventStore.Replication/PeerSyncEndpoints.cs) —
  `GET /peer-sync/whoami` and `POST /peer-sync/push`, both gated by a new
  `peer:sync` scope.

`RouterWorker.FoldAsync` gained `ShardKey = entityType` on new Entity
Store rows (`ADR-034`'s sharding key) and now records
`LastAppliedOriginId` on every non-late-arrival fold — the field a future
region-pinning/data-residency filter would read.

## A real bug found only by writing the real-HTTP test, not by reading the code back

`PeerSyncClient.AttachAuth`'s `request.RequestUri!.GetLeftPart(
UriPartial.Path)` throws `InvalidOperationException` for a relative
`RequestUri` — which it deliberately is in
[`ReplicationHttpSqliteTests.cs`](../../tests/EventStore.IntegrationTests/ReplicationHttpSqliteTests.cs):
the test calls `WhoAmIAsync("", ct)`/`PushAsync("", ...)` with an empty
address string, since the real target `HttpClient` is supplied via a
`FixedHttpClientFactory` mapping rather than a real absolute URL (the
same pattern `FollowClient`'s own tests already established). Fixed by
resolving `request.RequestUri` against `client.BaseAddress` first when it
isn't already absolute, mirroring `FollowClient.AttachAuth`'s existing
production code exactly rather than inventing a second convention.

A second, unrelated bug the same test surfaced: the seeded
`peer-sync-client` DevIdp client only held `peer:sync`, but the test
drives register+publish+sync from that one caller (this repo's own
documented simplification — "shared by every site in this dev/POC
environment; a real deployment would give each site its own credential")
and needs `events:publish`/`registry:admin` too. Added both scopes to
`peer-sync-client`'s seed entry, the same both-roles-in-one-caller posture
`telemetry-client`/`attachments-client` already established for their own
items.

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

`ADR-033`'s own Scope line names Merkle-tree catch-up as an efficiency
optimization for a peer resyncing after a long disconnection — not built
at this stage. What ships instead: a plain `PeerSyncCursor`-based full
resync-since-last-ack (every event past `LastAckedSequenceNumber`,
re-pushed and deduped by `EventId` on arrival), which is functionally
correct against this item's actual exit criteria (convergence, with
genuine conflicts flagged) — just not bandwidth-efficient for a long
disconnection. No later item's own scope currently needs the efficiency;
revisit if one does. Cross-shard fan-out remains deferred to "GraphQL-Only
Query Layer," per this item's own pre-existing build-plan note (unchanged
this pass, confirmed still accurate).

## Test count

`EventStore.IntegrationTests` now has 45 passing `[TestMethod]`s (up from
41): `ReplicationSqliteTests`/`Postgres`/`SqlServer` (3, one
`AllReplicationScenarios` method each covering 5 scenarios — durable
write survival across a simulated restart, two-site disconnect-then-
converge with a genuine `ConflictFlag`, gossip address-book merging, and
per-`EntityType` shard-key stability) and one dedicated real-HTTP
`ReplicationHttpSqliteTests` proving the actual wire/auth path: real
`peer:sync`-scoped token issuance, real DPoP-proof generation against a
resolved absolute URI, and the `/peer-sync/whoami` handshake between two
real `WebApplicationFactory` Hosts. All 45 pass reliably alone and as the
SQLite-only subset. The full multi-provider run surfaced a pre-existing,
unrelated flake — a single SQL Server test class occasionally fails its
own Testcontainers `ClassInit` under resource contention from running
several `MsSqlContainer`s in one process; a different class fails each
run, and every failing class passes cleanly standalone. Not caused by
this item's changes; tracked in `TODO.md` rather than silently ignored.

# Item 18, "Non-Authoritative Capture"

Done, same day, continuing directly from item 17. `ADR-035`'s
`AuthorityStatus` trust axis and `ADR-042`'s gated-fold/Live-View revision,
built together since the data model (`docs/data/event-log.md`'s
`AttestedActorId`/`AttestedClaims`/`AuthorityStatus`/`AuthorityDecisionRef`,
`docs/data/entity-store.md`'s `LiveEntityStoreRow`,
`docs/data/schema-registry.md`'s `RejectionBehavior`) already fully
specified the gated shape, not the earlier "folds identically" one.
`docs/08-build-plan.md`'s status table and `BuildPlan_CorePhases` diagram
fill both updated to Done/`#palegreen` in this pass.

## The Live View and the authoritative gate (`ADR-042`)

New [`LiveEntityStoreRow.cs`](../../src/EventStore.Domain/EntityStore/LiveEntityStoreRow.cs) —
folded by a new
[`RouterWorker.FoldLiveAsync`](../../src/EventStore.Router/RouterWorker.cs),
the ungated counterpart to the existing `FoldAsync`: no `ExpectedVersion`/
`ConflictFlag` check (that Version semantics apply to the authoritative
store only, per `ADR-042`'s own Consequences) and no late-arrival
ordering guard (`LiveEntityStoreRow` has no `LastAppliedLogicalTime` of
its own in the documented shape — a deliberately simpler "best current
guess, folded in arrival order" view). `RouterWorker.ProcessEventAsync`
now calls `FoldLiveAsync` unconditionally for every event, but only calls
the existing `FoldAsync` when `storedEvent.AuthorityStatus == "accepted"`
— an `unattested`/`pending_review` event reaches the Live View
immediately but the authoritative Entity Store gets no row at all until
acceptance.

`PublishEventRequest` gained `AttestedActorId`/`AttestedClaims`/
`ReviewPending`; `PublishService.PublishAsync` computes `AuthorityStatus`
per `ADR-042`'s two named triggers: self-attested credentials
(`AttestedActorId`/`AttestedClaims` present) start at `"unattested"` — an
identity claim, not yet reviewed at all; an explicit `ReviewPending`
marker (a detector's own "not yet validated" flag) starts at
`"pending_review"` — a content/confidence case, not an identity one;
otherwise the ordinary-publish default, `"accepted"` (`ADR-006` already
verified identity/permission synchronously, nothing left to review).

## `AuthorityDecisionResolver` (`ADR-035`/`042`)

New [`AuthorityDecisionResolver.cs`](../../src/EventStore.Router/AuthorityDecisionResolver.cs) —
the feature doc's own "special-purpose reactor" shape, the same pattern
`ADR-020`'s `EventUpcastFailed` handling and `ADR-027`'s materialization
already use, not a new generic fold mechanism. `RouterWorker.
ProcessEventAsync` invokes it whenever an `authorityDecision` event is
processed (itself just an ordinary, explicitly-registered event type,
folding into its own `{appId}:authoritydecision:{targetEventId}` entity
like any other — not a reserved platform type like `EventUpcastFailed`).
For each decision:

- Annotates the target `StoredEvent`'s `AuthorityStatus`/
  `AuthorityDecisionRef`.
- **Two servers disagreeing, reused via `ConflictFlag`, not a new
  mechanism** (`ADR-035`'s own Consequences): if the target's
  `AuthorityDecisionRef` already names a *different* decision event, the
  decision being applied now is flagged on **itself** — last-applied
  wins, never blocked, never merged.
- On `accepted`: catches the authoritative Entity Store up to what the
  Live View already showed, by re-splitting the target's own payload
  against its declared schema and calling the existing `RouterWorker.
  FoldAsync` directly — the same "apply once, on the triggering
  condition" shape `ADR-027`'s materialization catch-up already
  established, reused outright.
- On `rejected`, **only** for the narrow residual case `ADR-042` actually
  leaves to `RejectionBehavior.Compensate` (the target was already
  `accepted` and folded, now reversed — a rejection of an event that was
  never accepted has nothing to compensate for, per `ADR-042`'s own
  narrowing): appends a new compensating-patch `StoredEvent`, same
  `EntityId`/`EventType` as the target, whose payload sets exactly the
  properties the rejected event itself contributed to explicit JSON
  `null` — folded as `ChangeKind.Partial` **regardless of the target
  type's own `ChangeKind`**, reusing `EntityDataMerger`'s existing
  "an explicit JSON `null` clears a key" rule exactly as `ADR-022`
  designed it for, rather than inventing new revert-computation logic.
  `RejectionBehavior.Annotate` (the default) leaves the target's
  `Payload` and the authoritative Entity Store exactly as they were —
  `AuthorityStatus` alone is the flag a consumer must check.

`RegisterEventTypeRequest`/`SchemaRegistryService.RegisterAsync` gained
`RejectionBehavior` parsing at registration — the `EventTypeDefinition`
field and its DB column already existed from earlier scaffolding
(`Annotate`-defaulted), just never actually settable until now.

## A real gap found in item 17's own code, fixed here

`ReplicatedEventPayload` (`EventStore.Replication`, "Sharding &
Replication") never carried `AuthorityStatus`/`AttestedActorId`/
`AttestedClaims` across the wire — a peer-synced `unattested` event would
have silently reset to `StoredEvent`'s own `"accepted"` class default at
the receiving site, the exact kind of correctness bug that only surfaces
once an event with a non-default `AuthorityStatus` actually exists to
replicate (nothing did, until this item). Fixed by adding all three
fields to the payload record and wiring them through `PeerSyncWorker.
ToPayload`/`PeerSyncReceiver.ReceiveAsync`, preserving the originating
site's trust axis verbatim rather than resetting it.

## A real, found doc inconsistency — not caused by this pass, not silently resolved

`comparisons/authority-rejection-behavior.md`'s own "Refinement" section
recommends "annotate-only-plus-targeted-rebuild" (a post-hoc rejection
triggers an immediate single-entity re-fold, excluding events whose
current `AuthorityStatus` isn't `accepted`) as the system-wide default for
the Annotate case. Neither `ADR-035`'s own Decision text, `docs/data/
schema-registry.md`'s `RejectionBehavior` field comment, nor `docs/
features/non-authoritative-capture.md`'s own Gherkin (which explicitly
asserts the Entity Store keeps showing the rejected event's own value,
unchanged, after an Annotate rejection) ever adopted or reflect that
refinement — all three still describe/test the plain, un-refined "flag
only" shape. Built per the plain shape, since that's what all three
other surfaces agree on and what this item's own exit criteria actually
test. Recorded as `docs/10-open-questions.md` row 1 rather than silently
picking a side — genuinely unresolved whether the refinement was adopted
and never propagated, or superseded without a note.

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

`ADR-036`'s real DID/UCAN offline-verifiable chain validation and the
actual server-side RFC 8693 OAuth Token Exchange bridge endpoint are NOT
built — `AttestedClaims` accepts and stores an opaque, credential-agnostic
JSON blob, never cryptographically verified, exactly matching
`docs/features/non-authoritative-capture.md`'s own explicit
"credential-agnostic" scope statement. `ADR-035`'s own "`AttestedClaims`
gets its own lightweight schema-registry `attestation` entity type" is
also not built — no test exercises it. No pre-GraphQL HTTP query surface
for the Entity Store/Live View exists either (none did before this item)
— this item's own tests query `db.EntityStore`/`db.LiveEntityStore`
directly, the same "exercise the mechanics directly" posture item 17's
own tests already use, consistent with this item's own text that its
GraphQL query shapes are "illustrative only."

## Test count

`EventStore.IntegrationTests` now has 48 passing `[TestMethod]`s (up from
45): `NonAuthoritativeCaptureSqliteTests`/`Postgres`/`SqlServer` (one
`AllNonAuthoritativeCaptureScenarios` method each, 9 scenarios covering
self-attestation, the explicit review-pending marker, the Live-View-vs-
authoritative-store split, `SchemaStatus`/`AuthorityStatus` independence,
Annotate/Compensate rejection behavior, `AuthorityDecisionRef`
denormalization, and the cross-site `ConflictFlag` case). All 48 pass
reliably alone and as the SQLite-only subset. The full multi-provider run
hit the same pre-existing SQL Server Testcontainers resource-contention
flake first noticed in item 17 — this pass found the clearest evidence
yet of its actual root cause: a container crash (exit 134/1) with stderr
`"Unable to create a new asynchronous I/O context. Please increase sysctl
fs.aio-max-nr"`, a Linux/WSL2 kernel-wide AIO-context limit exhausted by
however many `MsSqlContainer`s this session has cumulatively started —
conclusively a host resource issue, not a code defect, confirmed by every
affected class (four different ones across items 17/18 now) passing
cleanly standalone. Updated in `TODO.md` with this more specific
diagnosis rather than repeating the earlier, vaguer one.

# Item 19, "GraphQL-Only Query Layer"

Done, same day, continuing directly from item 18 — the largest item this
session by a real margin. `ADR-037`'s full OData-to-GraphQL swap, served by
[HotChocolate](../libraries/dotnet/hotchocolate.md) 16.5.1 per that ADR's
own adopted library choice. `docs/08-build-plan.md`'s status table and
`BuildPlan_CorePhases` diagram fill both updated to Done/`#palegreen`.

## Verify-before-citing, under real pressure this time

Every non-trivial HotChocolate API surface used below was checked against
the *actual installed v16 assemblies* via a throwaway reflection scratch
project before being written into real code — not assumed from search
results or documentation fetches, several of which turned out to describe
older, incompatible API shapes: `ObjectTypeDefinition`/
`ObjectFieldDefinition` (the names shown by every fetched doc sample) were
renamed `ObjectTypeConfiguration`/`ObjectFieldConfiguration` by the time of
v16; the "obvious" `HotChocolate.Utilities.Subscriptions.
SourceStreamWrapper` class turned out `internal`, unusable from outside
HotChocolate's own assembly (`ISourceStream` itself is a one-method public
interface, trivially implemented directly once that was discovered); the
"obvious" `IHttpResponseFormatter.FormatAsync` path needs an
`AcceptMediaType[]` array, and `AcceptMediaType` turned out to have an
`internal`-only constructor, unusable from a custom endpoint —
`HotChocolate.Transport.Formatters.JsonResultFormatter` needs no such type
at all and was found by tracing what `MapGraphQL()`'s own pipeline
actually calls underneath. This project's own most-repeated standing
instruction (`.claude/protocols/verify-before-citing.md`) paid for itself
directly here — every one of these would have been a silent runtime
failure if assumed from an older-version doc sample instead.

## Static surfaces — no dynamic schema construction needed

New `EventStore.GraphQL` project. Three surfaces have a FIXED shape
regardless of which event type a query names, so they're ordinary,
hand-written C# classes (`[ExtendObjectType(OperationTypeNames.Query)]`/
`Mutation` extensions on empty `Query`/`Mutation` root markers, reflection-
inferred GraphQL types — the same code-first mechanism any HotChocolate
app uses for a static schema):

- [`RegistryQueries.cs`](../../src/EventStore.GraphQL/RegistryQueries.cs) —
  `eventTypes(appId, first, skip)`/`eventType(appId, name, version)`,
  reusing `SchemaRegistryService` unchanged, gated by `registry:admin`
  (`GraphQlAuth.RequireScopeAsync`, resolving the SAME ASP.NET Core
  policies `HostCoreExtensions` already registers, through the ordinary
  `IAuthorizationService` — not a second scope-matching implementation)
  plus `AppIdScopeEvaluator.CanAdminister`.
- [`LineageQueries.cs`](../../src/EventStore.GraphQL/LineageQueries.cs) —
  `event(eventId) { ancestors descendants parents children }`, reusing
  `LineageService` unchanged; `LineageEventNode` is a thin resolver-
  carrying record so each traversal direction only runs when a query
  document actually selects it. `first`/`skip` are plain arguments applied
  inside `LineageService` (its own pre-existing parameters), not
  HotChocolate's `[UsePaging]` Relay Connection wrapper — the doc's own
  shown example (`ancestors(first: 50) { eventId ... }`, a flat list)
  never uses `after`, so the fuller cursor machinery would have produced a
  shape the doc itself never shows.
- [`RevealFieldMutation.cs`](../../src/EventStore.GraphQL/RevealFieldMutation.cs) —
  the actual reveal-on-demand round trip "Property-Level Masking" (item 9)
  could only build half of at the time (GraphQL didn't exist yet). Looks
  up the target event's own registered schema, navigates `fieldPath` to
  its `x-masking.requiredClaim`, checks it via `RequiredClaimEvaluator.
  HasClaim`, and returns the real value or a GraphQL error. `ADR-066`'s
  step-up-authentication refinement and the `ADR-045` `AccessLogEntry`
  audit write are NOT built — both depend on infrastructure two later,
  not-yet-built items own outright ("Digital Sign-Off for Regulated
  Actions"; "Delegated Grants, RBAC, Federated Claims & Read Audit
  Logging").

Every resolver needing the caller's identity declares `[Service]
ClaimsPrincipal user` — see "three real bugs" below for why plain
`ClaimsPrincipal user` (no `[Service]`) fails outside `MapGraphQL()`'s own
pipeline.

## The one genuinely dynamic surface — Follow's per-event-type Subscriptions

Unlike the three surfaces above, a Subscription field's own payload shape
genuinely differs per registered event type — `ADR-037`'s own guarantee
("a client cannot construct a query referencing an undeclared field")
needs real per-type schema composition here, not just static resolvers.
[`FollowSubscriptionTypeModule.cs`](../../src/EventStore.GraphQL/FollowSubscriptionTypeModule.cs)
implements HotChocolate's `ITypeModule` (the documented, real hot-reload
mechanism for a schema whose types change at runtime) — `CreateTypesAsync`
reads every currently-active `EventTypeDefinition` and builds, per type:

- A payload `ObjectType` (`{appId}_{eventType}_Payload`) with one field
  per top-level JSON-Schema property — scalar-only (object/array-typed
  properties are skipped, an honestly-flagged narrowing: every event type
  this repo's own tests register uses only scalars). A maskable property
  resolves to one of four new static types —
  [`MaskedFieldTypes.cs`](../../src/EventStore.GraphQL/MaskedFieldTypes.cs)'s
  `MaskedString`/`MaskedFloat`/`MaskedBoolean`/`MaskedDateTimeOffset`
  (`{value, masked, erased}`) — read directly from `IPayloadMasker`'s
  existing JSON output, unchanged.
- A Subscription field (`on_{appId}_{eventType}`, names AppId-qualified —
  see Built-scope note below) whose `SubscribeResolver` reuses
  `EventTailReader.TailAsync` **completely unchanged** — the concrete
  proof, not just the assertion, that "Follow's underlying SSE transport
  and envelope shape don't change; only how a filter is expressed"
  (`ADR-037`'s own Consequences text) — wrapped in a hand-rolled
  `ISourceStream` implementation (`FollowedEventSourceStream`, found
  necessary once `SourceStreamWrapper` turned out internal, above).

[`GraphQlFilterPredicateBuilder.cs`](../../src/EventStore.GraphQL/GraphQlFilterPredicateBuilder.cs) —
the GraphQL-native filter translator, parsing a list of
[`EventFilterInput.cs`](../../src/EventStore.GraphQL/EventFilterInput.cs)
clauses (AND-combined; see Built-scope note) instead of an OData `$filter`
string, but reusing `FilterPredicateBuilder`'s own property-access/
constant-expression building blocks directly (promoted to `public` in
[`FilterPredicateBuilder.cs`](../../src/EventStore.Follow.Api/FilterPredicateBuilder.cs),
not duplicated) — the per-provider JSON pushdown mechanism is proven
identical to the OData era by literal code sharing, not merely asserted.

## The `QUERY`-method endpoint

[`GraphQlEndpoints.cs`](../../src/EventStore.GraphQL/GraphQlEndpoints.cs) —
`docs/libraries/dotnet/hotchocolate.md`'s own note ("exposing it over the
HTTP `QUERY` method... needs a small custom endpoint mapping rather than
`MapGraphQL()`'s default") is why this exists: `MapGraphQL()` only maps
GET/POST/WebSocket. Manually resolves `IRequestExecutorProvider`, builds
an `OperationRequestBuilder` from the raw JSON body, calls `IRequestExecutor.
ExecuteAsync`, and formats the result with `HotChocolate.Transport.
Formatters.JsonResultFormatter` (found only after the `AcceptMediaType`
dead end above). A `Subscription`-kind result (`IResponseStream`) streams
each yielded `OperationResult` as its own SSE `data: ...\n\n` frame — the
identical transport Follow's own pre-GraphQL endpoint already used.
Deliberately NOT gated by a blanket `.RequireAuthorization()` — introspection
stays anonymous (`03-api-contracts.md`'s own scope table) sharing this one
endpoint with every real, scope-gated operation, so each resolver checks
its own scope/claim individually instead (`GraphQlAuth.RequireScopeAsync`).
`IncludeExceptionDetails = true` is left on deliberately, matching this
whole repo's existing dev/POC posture (plaintext DevIdp secrets, etc.).

## Three real bugs found only by actually running this over real HTTP

1. **A resolver parameter typed plain `ClaimsPrincipal` uses HotChocolate's
   own built-in "well-known parameter" binding**, which failed
   (`"Could not resolve the claims principal (Parameter 'context')"`) the
   moment a request was built manually rather than through `MapGraphQL()`'s
   own pipeline (which apparently populates whatever internal state that
   binding reads). Fixed by marking every such parameter `[Service]
   ClaimsPrincipal user` to force ordinary DI resolution instead — which
   needed `ClaimsPrincipal` registered in DI at all (`services.
   AddScoped(sp => sp.GetRequiredService<IHttpContextAccessor>().
   HttpContext!.User)`, since ASP.NET Core doesn't register it by default).
2. **A dynamically-built field's name needs explicit camelCasing.**
   HotChocolate's naming convention lower-cases a reflected C# property's
   first letter (`OrderId` -> `orderId`) automatically, but a raw string
   passed to `ObjectFieldConfiguration` (no reflected member behind it at
   all) gets used verbatim — caught by a real `"the field orderId does not
   exist"` GraphQL error against a query written in ordinary camelCase.
3. **A Subscription field's own resolver must read the streamed value via
   `ctx.GetEventMessage<T>()`, never `ctx.Parent<T>()`.** For a ROOT-level
   Subscription field, `Parent<T>()` returns the `Subscription` marker
   object itself, not the message the source stream yielded — caught by a
   real `"unable to cast the parent type"` error.
   `HotChocolate.ResolverContextExtensions.GetEventMessage<T>` (the
   low-level equivalent of the documented `[EventMessage]` attribute
   pattern) was found by searching the actual assembly for a
   `WellKnownContextData.EventMessage` constant once the annotation-based
   convention's own underlying mechanism was needed directly.

## A fourth, real, honestly-flagged limitation — not fully root-caused

`FollowSubscriptionTypeModule`'s hot-reload never actually re-invokes
`CreateTypesAsync` against an **already-running** Host, confirmed by
extensive direct debugging across many real test iterations: `TypesChanged`
fires and reaches a real subscriber (verified by instrumenting both sides),
a parallel independent `EventStoreContext` against the same database file
proves a registration's own commit is real and immediately visible, and a
2-second periodic-`Timer` fallback (tried as a robustness net, matching
`03-api-contracts.md`'s own "generate fresh + short cache" precedent for
OpenAPI/AsyncAPI) never ticked a second time either. Most likely
explanation, not proven without reading HotChocolate's own source:
whatever internal scope builds the schema disposes the type modules it
resolves once building finishes — which would silently stop a `Timer`
field kept on this same long-lived singleton, and is why this class no
longer implements `IDisposable` at all (nothing left that a premature
`Dispose()` could break). Worked around for this item's own exit criteria
— which never actually requires hot-registering against a live Host — by
seeding the one Subscription-over-HTTP test's event type directly into the
database before the Host starts; this genuinely proves the dynamic schema
construction mechanism itself is correct, isolating the gap to specifically
"hot-reload of an already-running Host." Tracked as a real, open follow-up
in `TODO.md`, not silently dropped.

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

Per-`AppId` schema isolation is one shared schema with AppId-qualified
names, not a literally separate SDL document per `AppId` (two `AppId`s can
register the same event-type `Name` independently, `ADR-030`, which one
schema cannot express as two identically-named types any other way, and
HotChocolate's own multiple-named-schema feature needs a fixed set of names
configured at startup, not `AppId`s discovered dynamically). The `where`
argument is a static list input, narrowing `ADR-037`'s literal
schema-level "undeclared field" guarantee to a runtime check for filtering
specifically (the guarantee still holds at full strength for Subscription
field/payload names, which genuinely are built per registered type). No
`and`/`or` filter combinators. No generic entity/`extensions: JSON` query
field (nothing built here ever needs one). DataLoader batching and
cross-shard/cross-replica fan-out are not exercised because there's no
concrete case to exercise — every resolver already reads its data in one
batched query, and `ShardKey` remains a logical column with no physical
multi-shard deployment to fan out across. Full detail in
`08-build-plan.md`'s own Built-scope note, not repeated further here.

## Test count

`EventStore.IntegrationTests` now has 56 passing `[TestMethod]`s (up from
48): `GraphQlHttpSqliteTests` (5 real-HTTP scenarios — registry listing,
lineage traversal plus a genuine scope-rejection case, the depth-limiter
rejecting a deliberately deep introspection query, `revealField` with and
without the claim, and a real Subscription streamed as SSE end to end)
plus `GraphQlFilterPredicateBuilderSqliteTests` (3 scenarios, deliberately
SQLite-only: the per-provider native SQL generation underneath is reused
unchanged from "Follow API + Filter Pushdown," already proven on all 3
providers there — re-proving it a fourth time here would test nothing
new, only this item's own new clause-combination/dispatch logic, which
SQLite alone already exercises fully). All pass reliably alone and as the
SQLite-only subset. The full multi-provider run (twice, for stability) hit
the same pre-existing, unrelated SQL Server Testcontainers resource-
contention flake already tracked in `TODO.md` — a different rotating class
each run, confirmed unrelated to this item's own changes.

**Item 20, "Compatibility & Deployment Discipline," is Done** — same day,
continuing directly from item 19. `docs/08-build-plan.md`'s status table
and `BuildPlan_Additions` diagram fill both updated to Done/`#palegreen`
in this pass.

## The rollback drill — a real, narrow gate in `RouterWorker` (`ADR-038`)

The literal exit criterion ("deploy a schema version, publish an event
tagged with it, roll back to a deployment that doesn't know that version,
confirm the event sits `received`, confirm re-forward-deploying makes it
routable again") needed one genuine, if small, production-code change.
`RouterWorker.ProcessEventAsync` already resolved `declaredDefinition` (the
event's own declared `SchemaVersion`, possibly unregistered) and
`activeDefinition` (the currently active version, used for entity
resolution) — but always fell through to `Status: "applied"` regardless,
with `SchemaStatus: "unknown"` as the only signal an unrecognized version
ever produced. That's the right behavior for the ordinary, already-tested
backward-compatible case (an old, never-registered version, `SchemaVersion
<= active` — `ADR-023`'s own "advisory, never gates Status" rule), but it
silently swallowed the *forward*-incompatible case a real rollback
produces: an event tagged with a version genuinely ahead of anything the
deployment's own registry has ever seen. Fixed by hoisting
`activeDefinition`'s resolution earlier in the method (unchanged
otherwise) and adding one early-return: when `declaredDefinition is null`
**and** `storedEvent.SchemaVersion > (activeDefinition?.Version ?? 0)`,
the event is left at `Status: "received"`, skipping entity resolution and
folding entirely for that tick.

No separate backlog-reconciliation mechanism was needed for the "becomes
routable again" half, unlike `ADR-027`'s own Trigger 2 precedent — the
existing `RunOnceAsync` polling loop already re-queries `Status ==
"received"` on every tick, so the exact same event is simply picked up
again, this time successfully, the moment a later registration raises the
active version to cover it. Verified directly: register version 1,
publish an event declaring `SchemaVersion 4` (`PublishService` only
requires *some* active version to exist — it never validates the caller's
declared version against a real row, so this is a realistic, no-DB-bypass
way to simulate a deployment that's behind), tick the Router once and
confirm the event is still `received` with no `EntityId`, then register
three more versions to bring the active version to 4 and tick again —
confirms `Status: applied`, `SchemaStatus: conformant`, and the correct
`EntityId`. A second scenario confirms the gate's own narrowness: an old,
no-longer-active version (registered once, superseded, but not ahead of
the current active version) is completely unaffected, reaching `applied`/
`conformant` exactly as every earlier item's own tests already rely on.

## Enum-fallback contract and version-discovery capability negotiation — no shared-fold-path changes needed

Both of `ADR-038`'s other GraphQL-facing pieces turned out to need zero
changes to `RouterWorker`'s shared fold path at all. `x-enum-fallback`
(paired with JSON Schema's own standard `"enum"` keyword) is validated at
schema-registration time by a new `EnumFallbackSchemaValidator`
(`EventStore.SchemaRegistry`), mirroring `MaskingSchemaValidator`'s exact
shape: must be a boolean, only on a string-typed property, requires a
non-empty `"enum"` array on that same property, and — a deliberate,
named scope decision, since neither `EventTypeSchemaReader` nor
`FollowSubscriptionTypeModule`'s dynamic field builders were written to
handle both together — mutually exclusive with `x-masking` on the same
property. `EventTypeSchemaReader.GetTopLevelProperties` now also surfaces
each property's `EnumFallback`/`KnownValues`, and
`FollowSubscriptionTypeModule.BuildPayloadFields` (renamed from
`BuildPayloadField`, now yielding zero or more configs instead of exactly
one) adds a sibling `{name}Known` Boolean field alongside the ordinary
value field whenever `EnumFallback` is set — realizing
`compatibility-and-versioning.md`'s own Scenario 1 (`{ status,
statusKnown }`) directly on Follow's existing dynamically-built
Subscription payload type, no new schema-construction machinery needed.

Version-discovery capability negotiation is a new, small, self-contained
`capabilities(appId, name, supportedSchemaVersions)` static Query field
(`CapabilitiesQueries.cs`, `EventStore.GraphQL`), gated by the same
`events:follow` scope Follow's own connect-time check already uses (this
is the connection-open step for a Follow-style client, not a registry-
administration action). Deliberately built as its own field rather than
threading a `supportedSchemaVersions` argument through
`FollowSubscriptionTypeModule`'s already-intricate dynamic Subscription
field — the feature doc's own diagram for this mechanism is itself
flagged there as "this doc's own structural choice, not a shape `ADR-038`
states," so nothing forced coupling it to that already-fragile hot-reload
surface. `supportedWindow` is computed as a fixed numeric `[active-1,
active, active+1]` band (values below 1 dropped), **not** filtered
against which versions actually have a registered row — this design's own
registration model has no "registered but not yet active" state for a
future version to occupy (`SchemaRegistryService.RegisterAsync` always
activates the version it just created and deactivates the prior one in
the same transaction), so the feature doc's own "version 4 not yet
active" framing can't be literally reproduced here; reporting a purely
numeric window is an honest, simpler substitute that still satisfies both
of the feature doc's own Gherkin scenarios exactly as written. A caller
whose declared versions don't intersect that window gets a
`GraphQLException` ("capability mismatch"), matching the doc's own
"connection rejected" framing.

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

Expand/Contract migration discipline needed no new code at all —
`ADR-038`'s own Consequences already said as much ("no new mechanism is
introduced here that this design didn't already have a piece of"), and
every EF Core migration in this repo, across all 20 items so far, has
already been purely additive (new nullable columns/tables, nothing ever
altered or dropped). The alternative enum-fallback strategy `ADR-038`
also names — defaulting deserialization to a designated `Unknown` enum
member instead of carrying a raw string plus a flag — was not built as a
second, separate mechanism: it's the same registration-time choice
expressed differently (a closed GraphQL enum with an explicit `UNKNOWN`
value baked in), not a different deployment-safety property, per the
feature doc's own text.

## Test count

`EventStore.IntegrationTests` now has 61 passing `[TestMethod]`s (up from
56): `CompatibilitySqliteTests`/`Postgres`/`SqlServer` (one
`AllCompatibilityScenarios` method each, 2 scenarios — the rollback drill
itself, and the old-version-is-unaffected regression check) plus
`CompatibilityGraphQlHttpSqliteTests` (2 real-HTTP scenarios — the
enum-fallback sibling field over a real SSE-streamed Subscription, and
`capabilities` accepting a client inside the N-1/N+1 window while
rejecting one outside it). All pass reliably alone and in the SQLite-only
subset; the full multi-provider run hit the same pre-existing, unrelated
SQL Server Testcontainers resource-contention flake already tracked in
`TODO.md` (`SqlServerRoundTripTests`'s own `ClassInit`) — this item's own
three `AllCompatibilityScenarios` runs (SQLite/Postgres/SQL Server) all
passed cleanly in that same run.

**Item 21, "MVVM Client," is Done** — same day, continuing directly from
item 20. The first item in this build whose actual ADR decision is a real
JS/TS web client, not server-side .NET — explicitly checked with the user
first (`AskUserQuestion`, a genuine tech-stack fork), who chose building
the real Vue 3 client over a C# simulation of the mechanics.
`docs/08-build-plan.md`'s status table and `BuildPlan_Additions` diagram
fill both updated to Done/`#palegreen` in this pass.

## Server-side additions this item needed

Three small, real additions -- nothing else in `EventStore.Router`'s
shared fold path or the GraphQL schema needed to change:

1. `ViewDefinition` (`EventStore.Domain/Views/ViewDefinition.cs`) -- shape
   already fully specified in `docs/data/schema-registry.md`/
   `dbcontext-and-conventions.md` from an earlier pass, just never built.
   No `AppId` -- shared across every application by `EntityType` name
   alone, per that doc's own key (`EntityType`, `Version`, `ViewKind`). A
   new `EventStore.ViewRegistry` project (`ViewDefinitionService`) mirrors
   `SchemaRegistryService`'s own content-addressed/versioned registration
   pattern, deliberately much smaller (no upcast chains, no claims, no
   `FilterableFields`): a new registration for the same
   (`EntityType`, `ViewKind`) pair increments `Version` and marks the
   prior one `DeprecatedAt` (never deleted -- `ADR-038`'s "deprecated but
   still served" discipline, reused here for the same reason: an
   already-cached client shouldn't lose a template it fetched before a
   newer one was registered). Migrated across all 3 providers
   (`AddViewDefinitions`). Exposed via a `viewDefinition(entityType,
   viewKind, schemaVersion)` GraphQL query (gated by `events:follow` --
   an ordinary client read, not registry administration) and a
   `registerViewDefinition(...)` mutation (gated by `registry:admin`,
   the same scope `RegistryQueries` already uses).
2. `ConflictFlag`/`LateArrivalFlag`/`AuthorityStatus`/`SchemaVersion` added
   as four FIXED envelope fields on every dynamically-built Subscription
   payload type (`FollowSubscriptionTypeModule.BuildEnvelopeFlagFields`,
   sourced directly from `FollowedEvent.Event`, never from the masked/JSON
   payload) -- nothing before this item ever needed these exposed over
   GraphQL, since no client previously needed to render the shared flag
   convention `ADR-024`/`029`/`035` name.

## The client itself (`client-web/`)

A real Vue 3 + Pinia + Vite npm workspace (`docs/06-solution-structure.md`'s
own naming), structured per `docs/patterns/mvvm-client-architecture.md`'s
own Data/Actions/Structure/Presentation/Styling split:

- `src/db/indexedDb.ts` -- a small hand-rolled `IndexedDB` wrapper (no
  `idb`/Dexie dependency; the object-store shape here is two stores, no
  complex querying beyond one index, too small to earn a library).
- `src/stores/outbox.ts`/`entityCache.ts`/`viewDefinitions.ts` (Model
  layer, Pinia) -- the client-local durable outbox (`ADR-039`, the same
  fault-tolerance bar as `ADR-033`'s peer-sync outbox) and the "last-known-
  good" entity cache, deliberately simpler than the server's own
  `RouterWorker.FoldAsync`: no `ExpectedVersion`/`ConflictFlag`
  COMPUTATION (the server already computed these and sends them as fixed
  envelope fields) and no Full-vs-Partial `ChangeKind` branching --
  every arriving schema field just overwrites the cache's own copy,
  latest-write-wins, since the server's own fold already applied whatever
  merge semantics its `ChangeKind` required before this client ever saw
  the confirmed result.
- `src/composables/useEntityViewActions.ts` (Actions/ViewModel-commands
  layer) -- `dispatchCommand` enqueues into the outbox and, if online,
  flushes immediately, but never mutates the entity cache directly (the
  ADR's own core commitment: "a ViewModel's own optimistic write is never
  assumed to be truth"); `subscribe` discovers a per-event-type
  Subscription's own field set via GraphQL introspection
  (`api/subscriptionBuilder.ts`, mirroring
  `FollowSubscriptionTypeModule.Sanitize`'s own naming convention exactly)
  rather than hardcoding one demo entity type's fields, which is what
  makes this composable genuinely generic across any registered event
  type.
- `src/components/entity/EntityView.vue`/`TemplateRenderer.vue` (the ADR's
  own "small injected binding runtime" -- a minimal `{{ field }}`
  interpolator plus a `data-command-field`/`data-command-value-from`
  attribute convention for dispatching a command from raw HTML+JS,
  deliberately not a full templating engine) / `GenericFallbackView.vue`
  (the required fallback -- lists every `Data`/`Extensions` property by
  name) / `FlagRow.vue` (the ONE shared `ConflictFlag`/`LateArrivalFlag`/
  `AuthorityStatus` rendering convention, used by both the fallback and
  the template renderer -- there is only one implementation of this
  convention in the whole client).
- `public/sw.js`/`manifest.webmanifest` -- a minimal, dependency-free
  Service Worker (app-shell caching + a Background Sync handler for
  `ADR-069`'s "opportunistic" flush trigger when the app isn't even open)
  and Web App Manifest, for the ADR's PWA-installability/offline-app-shell
  commitment. Deliberately no `vite-plugin-pwa` dependency -- kept
  standalone so the Service Worker needs no build step of its own.

## A real bug found only by actually running the tests, not by reading the code back

Pinia wraps every store entry in a reactive `Proxy` -- IndexedDB's
structured-clone algorithm correctly rejects a `Proxy` outright
(`DataCloneError: #<Object> could not be cloned`), caught immediately by
`fake-indexeddb` the first time a spec actually persisted a store entry.
Fixed by having `db/indexedDb.ts`'s `put()` round-trip the value through
`JSON.parse(JSON.stringify(value))` before handing it to IndexedDB --
every shape stored there (`ClientOutboxEntry`, `ClientEntityCacheEntry`,
`ViewDefinitionCacheEntry`) is already JSON-safe by construction, so this
is a safe, general fix at the one choke point, not a per-call-site
workaround.

## Explicitly not built this pass, flagged in `08-build-plan.md` directly

No native shell (`EventStore.Client.WebViewBridge`/`DeviceInput`, `ADR-070`
-- "Device Input Integration," item 44's own scope) -- only the web
target. `entityIdField`/`entityType`/`eventType` are supplied as explicit
per-instance launch configuration rather than resolved from a
`registry:admin`-gated lookup an ordinary follower credential doesn't
hold -- matches the ADR's own "launch configuration, not auto-discovered"
framing. An unknown property that lands in the server's own `Extensions`
bag (`ADR-022`) never reaches this client at all today --
`FollowSubscriptionTypeModule`'s dynamic payload type only ever exposes a
schema's OWN declared properties (`ADR-037`'s "a client cannot even
construct a query referencing an undeclared field" guarantee applies
here too) -- a data-availability gap, not a rendering one;
`GenericFallbackView` already renders `Extensions` generically wherever
it's populated. No live browser/Playwright round trip against a real
running Host + DevIdp was driven -- this repo has no browser E2E harness
yet (`TODO.md`'s own "no `EventStore.Bdd`/E2E built" entry); the Vitest
suite proves the mechanics, `npm run build` plus a dev-server smoke check
(curl against a running `vite` process, confirming the app shell/manifest
actually serve) prove the app is real and buildable/servable, but an
actual live GraphQL round trip through a browser is not exercised in this
pass. `ADR-065`'s erasure-driven local purge, `ADR-069`'s scheduled/
manual/air-gapped-export flush triggers beyond the opportunistic one,
`ADR-073`/`ADR-087`'s accessibility/i18n requirements, and the
`ADR-068`-format outbox export/import are all later build-plan items' own
scope (28, 43, 45, 46), not re-derived here.

## Test count

`EventStore.IntegrationTests` now has 66 passing `[TestMethod]`s (up from
61): `ViewDefinitionSqliteTests`/`Postgres`/`SqlServer` (one
`AllViewDefinitionScenarios` method each, 5 scenarios -- registration
makes a version active, a new version deprecates but never deletes the
prior one, a schema-version-incompatible active template falls back to a
deprecated-but-compatible one, an unregistered EntityType returns null,
and registration validation) plus `MvvmClientGraphQlHttpSqliteTests` (2
real-HTTP scenarios -- `registerViewDefinition`/`viewDefinition` round
trip, and the 4 envelope flags carried on a real Subscription). All pass
reliably alone and in the SQLite-only subset; the full multi-provider run
hit the same pre-existing, unrelated SQL Server Testcontainers resource-
contention flake already tracked in `TODO.md` -- 4 different SQL Server
classes this run (`UpcastMaterializationSqlServerTests`,
`ViewDefinitionSqlServerTests`, `StreamingSqlServerTests`,
`SqlServerRoundTripTests`), none of which share any code this item
touched; `ViewDefinitionSqlServerTests` (this item's own new class)
confirmed passing cleanly when re-run standalone immediately after,
consistent with every other class this flake has ever hit.

Separately, `client-web/`'s own Vitest suite (26 specs across 7 files) all
pass, and `npm run build` (type-checked via `vue-tsc -b`, then a real
`vite build`) succeeds cleanly, producing a genuine `dist/` bundle.

**Item 22, "Ticket Exchange for Header-Incapable Clients," is Done** --
same day, continuing directly from item 21. `docs/08-build-plan.md`'s
status table and `BuildPlan_Additions` diagram fill both updated to
Done/`#palegreen` in this pass (the diagram fill for item 21, "MVVM
Client," had also been missed in that item's own pass -- caught and
fixed here too, same p20 state).

## A wrong assumption, found and corrected before writing any code

This item's own "Depends on" text named "Non-Authoritative Capture" as
already having built RFC 8693 Token Exchange infrastructure to reuse.
Checked against that item's own Built-scope note before assuming it was
true: it explicitly named "the actual RFC 8693 OAuth Token Exchange
bridge endpoint" as NOT built (`AttestedClaims` there is an opaque,
never-exchanged JSON blob). This item builds the grant type from scratch.

## Ticket issuance (`EventStore.DevIdp`, ADR-040 step 1)

`options.AllowTokenExchangeFlow()` -- found only by reflecting the real
installed OpenIddict 7.6.0 assembly, not assumed from a doc sample:
`AllowCustomFlow("urn:ietf:params:oauth:grant-type:token-exchange")`
throws `ArgumentException` ("already assigned to a standard grant type
and cannot be used for custom flows"), since OpenIddict recognizes this
specific grant type by name even though it has no built-in *handler* for
it -- `AllowTokenExchangeFlow()` is the dedicated method that actually
works. Its own built-in validation handler then rejected this ADR's
`requested_token_type` ("urn:eventstore:token-type:ticket") with
`ID2032: requested_token_type is not supported` until explicitly
registered via `options.Configure(o => o.RequestedTokenTypes.Add(...))`
(`OpenIddictServerOptions.RequestedTokenTypes` is a plain `HashSet<string>`,
found the same way). A new `TicketStore`
([`TicketStore.cs`](../../src/EventStore.DevIdp/TicketStore.cs)) --
in-process, non-persistent, per `auth.md`'s existing "client/token state
lives in DevIdp" statement -- holds `Ticket` records
(`{ Value, SecretRef, ExpiresAt, OriginalTokenClaims }` plus an
`Interlocked`-based `TryMarkConsumed()`).

## Two more real constraints found only by actually running this against OpenIddict's real pipeline

1. **`/connect/token` unconditionally requires a registered `client_id`
   for ANY grant type reaching it** -- confirmed by running the
   `one_time_secret` scenario and getting back `ID2029: the mandatory
   client_id parameter is missing`, even though `AllowTokenExchangeFlow()`
   itself never validates `requested_token_type`-specific parameters.
   Directly incompatible with `ADR-040`'s own `one_time_secret` path
   ("never requires a registered client_id"). Resolved with a genuinely
   separate, non-OpenIddict-pipeline endpoint
   (`POST /oauth/ticket-exchange` -- reads form fields directly via
   `HttpContext.Request.ReadFormAsync()`, never calls
   `GetOpenIddictServerRequest()` at all) sharing the same
   `IssueTicketAsync` local function the `client_id` path
   (`/connect/token`) calls after its own `ValidateClientSecretAsync`
   check -- one core, two entry points, not two independently-written
   issuance implementations.
2. **`IOpenIddictApplicationManager` never exposes a stored
   `client_secret` in plaintext** -- only `ValidateClientSecretAsync`
   (check a provided one), correct security practice but incompatible
   with recomputing `HMAC-SHA256(ticket, secret)` server-side at
   introspection time. Resolved by adding
   `DevIdpSeeder.GetClientSecret(clientId)`, reading back from the SAME
   dev-only plaintext `Clients` array that file's own header comment
   already names ("Secrets below are plaintext, fixed, dev-only values"),
   not a second, newly-invented secrets store.

## Resolution (`EventStore.TicketExchange`, ADR-040 step 3)

A new project: [`HmacSigner.cs`](../../src/EventStore.TicketExchange/HmacSigner.cs)
(shared by both the signing side and DevIdp's own verifying side -- one
implementation, not two independently-written HMAC calls) and
[`TicketAuthenticationHandler.cs`](../../src/EventStore.TicketExchange/TicketAuthenticationHandler.cs)
-- a second ASP.NET Core authentication scheme ("Ticket"), additive to
JwtBearer (still the default scheme), registered once in
`HostCoreExtensions` and opted into by exactly two routes via
`AuthorizeAttribute.AuthenticationSchemes` listing both schemes:
`StreamingEndpoints`' byte-range playback mode (`GET /telemetry/
{channelId}/samples` with a `Range` header -- ADR-040's own `<video src>`
target) and `AttachmentEndpoints`' retrieval route (`GET /attachments/
{contentHash}` -- the `<img src>`/`<a href>` target, equally named by the
ADR). `TicketAuthenticationHandler` returns `AuthenticateResult.NoResult()`
when no `ticket` query parameter is present at all, so a route accepting
both schemes falls through to Bearer cleanly rather than this scheme
always winning or always failing. `DpopValidationMiddleware` gained one
new early-return -- skip entirely when
`context.User.Identity?.AuthenticationType == "Ticket"` -- since a
ticket-resolved principal is never DPoP-bound by design (the ADR's own
"DPoP's proof-of-possession isn't lost, it's consumed one hop earlier"
framing) and has no `Authorization` header at all for that middleware to
even look at.

## New seeded client

`clinician-spa-client` (named after the ADR/feature-doc's own running
example, not a generic reuse of `telemetry-client`/`attachments-client`),
holding `telemetry:read attachments:read` plus the new
`Permissions.GrantTypes.TokenExchange` permission -- every other seeded
client's permissions are unchanged.

## Test count

`EventStore.IntegrationTests` now has 72 passing `[TestMethod]`s (up from
66): `TicketExchangeHttpSqliteTests` (6 scenarios -- issuance + signing +
resolution with no `Authorization` header at all; the same ticket
presented a second time rejected even before expiry; a wrong-signature
presentation rejected without burning the ticket for a later correctly-
signed retry, verified as two separate assertions in one scenario; the
`one_time_secret` path via the separate endpoint; an already-expired
ticket rejected, driven directly against `TicketStore` rather than
waiting out a real 60-second TTL over HTTP, the same "exercise the
mechanics directly" pattern this repo's other time-dependent tests
already use; and an ordinary Bearer-authenticated request to the same
route confirmed completely unaffected). All pass reliably. The full
multi-provider run hit the same pre-existing, unrelated SQL Server
Testcontainers resource-contention flake already tracked in `TODO.md` --
4 different classes this run (`DerivationSqlServerTests`,
`ViewDefinitionSqlServerTests`, `CompatibilitySqlServerTests`,
`ReplicationSqlServerTests`), none sharing any code this item touched.
