# Change log — 2026-08-03

Continues the "work the TODO list" pass started 2026-07-31 (see
`2026-07-31.md` and `2026-08-01.md` for the session's full run-up); the
date rolled over again mid-session.

## Phase 2 complete: 13 domains' Salt mockups reworked to the 2–4 screen format

Direct request, offered as an explicit choice before starting (proceed
now / pause here / do the smaller found-gaps first) — user chose to
proceed. `.claude/templates/feature-doc-template.md`'s Salt-mockup
guidance was tightened earlier this session from a single static mockup
to a required 2–4 screen sequential user flow, but the tweak had only
ever been applied going forward, to the 4 feature docs added to the two
chosen proving-ground domains afterward — never backfilled to the other
13 domains' one feature doc each. All 13 are now reworked, dispatched as
4 parallel background agents
(`.claude/protocols/parallel-batch-dispatch.md`), each owning a disjoint
set of 3–4 domains, all diffing against the same real reference (`docs/
domains/clinical-trials-device-telemetry/features/adverse-event-capture-
and-review.md`'s existing 3-screen mockup):

- **biobanking**: staff's unmasked internal lineage viewer → IRB-approved
  grant-issuance form → external researcher's scoped read (in-scope
  unmasked vs. out-of-scope `403`).
- **government-case-management**: public portal request submission →
  records officer's redaction review/determination → requester's
  disclosed record with per-field masking.
- **digital-forensics-evidence-custody**: examiner's acquisition intake/
  upload form → chain-of-custody timeline → offline player's verification
  verdict (partial-masked case).
- **logistics-chain-of-custody**: warehouse scanner's handoff capture →
  customs officer's cross-site `Permissive`-mode exception filing →
  dispatcher's custody timeline.
- **brokerage-capital-markets**: trader's order ticket → risk officer's
  review queue (approve/reject) → compliance/back-office audit screen.
- **dscsa-pharma-supply-chain**: manufacturer's shipment scan queue →
  manufacturer's transaction sign-off (dramatizing the `RequiredSignature`/
  step-up branch) → receiving partner's inbound reconciliation.
- **education-credentials**: registrar's diploma issuance form →
  employer's initial (masked) verification view → employer's delegated-
  access result screen after an `ADR-043` grant.
- **public-health-surveillance**: Live View case queue → investigator's
  classification decision → confirmed record showing region-pinned
  replication + HL7v2 upstream delivery.
- **itar-export-controlled-defense-data**: access-request queue →
  approver's review/sign-off → foreign partner's delegated read within
  granted `entityScope`.
- **pharmacovigilance**: 4 screens — ICSR review queue → reviewer's
  causality-assessment/sign-off → confirmed authoritative record with
  outbound-reporting eligibility → regulator's bitemporal system-time
  playback (a scenario the doc had previously explicitly deferred as "out
  of scope to mock up further here" — now built out, since the doc
  already had a full sequence diagram grounding it).
- **insurance-telematics**: 4 screens — adjuster A's claim queue → claim
  review/decision (secondary-opinion grant) → adjuster B's delegated
  entity-scoped read → compliance officer's dispute export + system-time
  playback.
- **utilities-smart-metering**: grid-ops dashboard/triage queue →
  reviewer's accept/reject decision detail → confirmed dispatched record
  after the authoritative fold.
- **industrial-iot-predictive-maintenance**: plant operator's alert queue
  (mixed authoritative/non-authoritative rows) → technician's field-
  verification decision → confirmed record with CMMS webhook notification.

Every new screen is traceable to a real step in that same doc's own
existing sequence diagram(s), not invented — multiple agents explicitly
noted a case where a plausible-looking next screen (a "record secondary
opinion" action, a `WorkOrderCreated` event) had no corresponding
mechanism in the doc and said so directly rather than inventing one, the
same "no silent caps" discipline this session's other batches have shown.
Every diagram stayed plain PlantUML/Salt, no `!include`. Scope was held
strictly to the `## Salt (UI mockup)` section in each file — no sequence
diagram, ER diagram, state machine, or Gherkin scenario was touched by
any of the four agents, confirmed by each agent's own report and spot-
checked directly.

`TODO.md`'s Phase 2 removed outright, along with the now-empty phase-
grouping structure — the file returns to a flat list of the three
smaller, mutually-independent items found mid-session (`masking.md`'s
`erased`-branch gap, the `ADR-054`–`074` feature-doc coverage gap, and the
wider `RequiredClaims`-naming sweep), none of which depend on each other
or on anything just completed.

## The three remaining TODO items worked in requested order: 2, then 3

Direct instruction: work items 2, 3, 1 in that order (rather than the
order they were originally found in).

### Item 2: `docs/features/*.md` coverage for `ADR-054`–`093`

Scoped before starting, rather than writing new docs blind: of the ~40
ADRs in range, 5 already had a documented home in an existing item
(`06-solution-structure.md`'s own project layout, `08-build-plan.md`'s
new items), several are pure ops/build-process concerns with no external
Gherkin-actor story (release engineering, testing-strategy escalations,
leader election, OTel instrumentation — already covered by `08-build-
plan.md`'s own exit criteria), and `ADR-079` (sanctions screening) is
application-scoped to the KYC/Meridian domain, not core Duplex, so
belongs in a domain doc, not here. The remaining set split into **7 new
full feature docs** (a real, distinct actor/flow each) and **9 sections**
folded into existing docs (a natural extension, not a new capability),
dispatched as 4 parallel background agents:

- **New docs**: `rate-limiting.md` (`ADR-058`), `feature-flags.md`
  (`ADR-077`), `device-input.md` (`ADR-070`+`083`), `webhooks.md`
  (`ADR-060`+`093`), `digital-signoff.md` (`ADR-066`+`086`),
  `bulk-ingestion-and-interchange-adapters.md` (`ADR-072`), and
  `lineage-export-and-playback.md` (`ADR-068`).
- **New sections**: `ADR-054`→`spec-generation.md`, `ADR-067`+`071`→
  `schema-registry.md`, `ADR-061`→`replication-and-sharding.md`,
  `ADR-082`→`multi-tenancy.md`, `ADR-064`→`auth.md`, `ADR-065`+`069`+`073`→
  `mvvm-client.md`.

Two agents caught real problems rather than blindly following the
dispatch brief: one verified my prompt's assumption that `event-
security.md`/`auth.md` already illustratively framed `RequiredSignature`/
step-up and found it didn't, writing an honest cross-reference instead of
a false one; another caught itself about to write `207 Multi-Status` for
the batch-publish response by instinct, checked it against `03-api-
contracts.md`'s actual `202`-per-item-envelope semantics, and fixed it
before finalizing.

**Consolidation found and fixed three real cross-doc problems the
parallel dispatch itself couldn't catch** (each agent only sees its own
files):
- `03-api-contracts.md`'s own `exportLineage`/`playbackAsOf` sketch used
  `rootEventId`/`direction` and a wall-clock `systemTime` — checked against
  `ADR-068`'s actual text ("given a starting `EntityId`... gathers every
  causally-connected event") and found `03-api-contracts.md` itself was
  wrong, not the new doc or the domain docs that already used `entityId`/
  `asOfSequenceNumber`. Fixed `03-api-contracts.md` to match `ADR-068`,
  then reconciled `lineage-export-and-playback.md` and two domain docs
  (`insurance-telematics`, clinical-trials' `trial-data-export-and-
  subject-rights.md`) that had independently named the same field
  `systemTimePlayback` instead of `playbackAsOf`. The new doc's own agent
  had explicitly flagged this variance in-doc rather than silently
  picking a side — the right call, since it let this pass resolve it with
  the actual ADR text rather than a coin flip.
- `03-api-contracts.md`'s scope table also mis-classified `exportLineage`/
  `playbackAsOf` as GraphQL `Mutation`s in one place while its own code
  example correctly showed them as `Query` fields (neither mutates
  state) — fixed the table.
- `ADR-068`'s `OriginalSequenceNumber`/`OriginalChainHash`/`ImportedFrom`
  (already named in `CLAUDE.md`'s seven-envelope-field list) had never
  actually been added to `docs/data/event-log.md`'s `StoredEvent` class —
  a pre-existing propagation gap, resurfaced by the new doc's own agent
  flagging it rather than silently working around the absence. Added now.

Also fixed directly: `auth.md`'s banner didn't list `ADR-064` among
"Resolved this pass" even though the sections-batch agent had added a
full `ActorId` section — banner updated to match.

### Item 3: `RequiredPublishClaim`/`RequiredReadClaim` naming sweep beyond `docs/features/*.md`

Full repo grep, then classified every hit rather than blanket-replacing:
correctly left alone every pre-`ADR-050` ADR (describing its own
contemporaneous truth, the whole point of additive history), `08-build-
plan.md`'s preserved build-stage-accurate wording (its early items
deliberately still describe the pre-`ADR-037`/`050` mechanics they
actually build first), the glossary's intentional three-name entry, and
`docs/changes/*.md`'s own historical narration. Fixed real staleness in:
`01-c4-architecture.md` (a component diagram), `09-cqrs-read-models.md`,
`05-schema-registry-and-spec-generation.md` (3 more instances beyond its
own banner's stated scope), two patterns docs
(`graphql-query-language.md`, `step-up-authentication.md`), one
comparisons doc, and eight domain feature docs across six domains.

**Two post-`ADR-050` ADRs (`ADR-066`, `ADR-068`) had a genuine citation
error** — each cited `RequiredPublishClaim`/`RequiredReadClaim` as
current when `ADR-050` had already generalized it to `RequiredClaims` by
the time each was written. Fixed in place with an explicit correction
marker, the same precedent as this session's earlier `ADR-086`/`092`
citation fixes — not a decision reversal, a factual writing error caught
and corrected.

**Resolved the one genuinely undecided case, `TelemetryChannel.
RequiredReadClaim`, by leaving it alone and documenting why**: a channel
is read-only ingest (`ADR-031`) with no Publish direction to distinguish,
so `ADR-050`'s `Direction`-qualified list generalization doesn't obviously
apply — generalizing it anyway would be a real (if small) data-model
decision with no ADR behind it, which this project's own data-model-
ownership convention (`CLAUDE.md`) doesn't allow taking lightly. Added an
explicit disambiguation comment instead of guessing either way.

**One more real, deliberate design choice found and correctly left
alone**: `itar-export-controlled-defense-data`'s own feature doc already
documents `RequiredReadClaim` as an intentional *denormalized* single-
value copy of `EventTypeDefinition.RequiredClaims` on its own entity-store
row, for query-time convenience — not the stale field name at all, just a
same-named domain-specific field with its own already-written rationale.
Left it alone; only fixed the one place (a Gherkin registration step)
that had blurred registration-time `RequiredClaims` with the denormalized
copy.

### Item 1: `masking.md`'s `erased` branch and the strategy-count "contradiction"

**Not actually a contradiction needing a new decision** — resolved by
reading `ADR-009`'s own current text closely, which the TODO item's own
framing hadn't done. `ADR-009` already carries an in-place amendment
(struck-through "no erasure" text, and a "Two content strategies now,
`FixedValue` and `PartialReveal`... promoted out of the Future proposal"
passage, plus a third, `Hash`) — `PartialReveal` and `Hash` are already
decided and built, not proposed. `06-solution-structure.md`'s three
registered `IMaskingStrategy` implementations were right all along;
`masking.md` was the one document that never caught up to `ADR-009`'s own
revision. No new ADR was written or needed.

Rewrote `masking.md` accordingly: the wrapper is now three-way (`value`/
`masked`/`erased`, `ADR-057`), the strategy list is `FixedValue`/
`PartialReveal`/`Hash` (with `ADR-009`'s still-genuinely-declined list —
tokenization, generalization/bucketing, format-preserving encryption,
whole-object/array masking — named explicitly so the distinction between
"decided" and "declined" doesn't blur again the way it just did). Fixed
the one Gherkin scenario that was actively wrong (asserted `PartialReveal`
registration is rejected — it's accepted), added new scenarios for
`PartialReveal`/`Hash` behavior and for the `erased` branch (including
that it's unconditional — even a full claim-holder sees `erased`, since
"the key is gone" is a different fact than "you lack permission"),
updated the sequence diagram to show the decrypt-vs-erased branch point,
and updated the ER diagram's `x-masking` note. Also fixed, found while in
the file: a stale `Phase 8` build-plan cross-reference — and while
checking for others like it, three more (`09-cqrs-read-models.md`,
`docs/adrs/adr-017-dpop-bound-tokens.md`, `docs/features/binary-
attachments.md`) that the earlier build-plan-restructuring pass's own
repo grep had missed because the citation spanned a line break.

`TODO.md` down to one item: `ADR-013`'s stale error table.

## `ADR-013`'s error-response table fixed — `TODO.md` now empty

Direct instruction: fix it. Two rows were stale, handled two different
ways per the additive-history convention, since one is a genuine decision
supersession and the other is only a naming citation:

- **The `$filter`-undeclared-field `400` row struck through**, superseded
  by `ADR-037` — not for the same reason the two existing `ADR-023`-
  superseded rows are (those became `202` + `SchemaStatus`, the *same*
  request now persists instead of rejecting); this one is struck because
  the request that would trigger it can no longer be *constructed* at
  all — an undeclared field never appears in the per-`AppId` GraphQL
  schema, so it's a client-side validation error, not a server-emitted
  `400`. Made that distinction explicit in the Consequences section
  rather than lumping it in with the other two as if the reason were the
  same.
- **The scope/claim `403` row's field-name citation corrected in place,
  not struck through** — `RequiredPublishClaim`/`RequiredReadClaim` →
  `RequiredClaims` (`ADR-050`), with a "correction, verified against
  `docs/data/schema-registry.md`" marker, the same pattern already used
  for `ADR-066`/`ADR-068`'s citation fixes earlier this session. The
  row's actual decision (a `403` with a `reason` extension distinguishing
  scope from claim) never changed — only the name of the field it
  referenced did, so a strikethrough would have misrepresented an
  unchanged decision as a reversed one.

**Found while fixing the diagrams this same "read closely, don't assume"
discipline was applied to**: the user separately reported `rate-
limiting.md`'s Token Bucket sequence diagram was broken — traced to a
PlantUML syntax bug (backslash-escaped quotes inside a quoted participant
name, which PlantUML doesn't support; the string terminates at the first
unescaped `"`, breaking the rest of the diagram). Swept the whole repo
for the same pattern and found it in 8 more diagrams written by this
session's `docs/features/*.md`/domain-doc agents (`01-c4-architecture.md`,
two in `pharmacovigilance`'s ICSR doc, `clinical-trials`' subject-rights
doc, two in the `itar` domain doc, `digital-signoff.md`, two in
`webhooks.md`) — all fixed to use plain or single quotes instead.
**Lesson for next time**: a parallel-dispatched agent writing PlantUML/
Salt can introduce a real syntax bug that renders invisibly wrong in the
transcript (the agent's own report said "verdict: complete" for all of
these) — spot-checking prose accuracy isn't the same as verifying a
diagram actually renders; this class of bug needs an actual render check,
not just a read-through, to catch reliably.

`TODO.md`'s Active section is now empty.

## Implementation-readiness survey, and its five findings worked through

Direct question: is the framework and the two proving-ground domains
ready to implement? Answered by surveying every ADR's own "flagged as
remaining propagation work"-shaped marker (14 across 12 ADRs, none ever
centrally tracked in `TODO.md`) rather than assuming the empty tracker
meant nothing was left — most turned out already closed by later work in
the same session without the flagging ADR's text being updated to say so,
but 5 were genuinely still open. Also checked, on request: whether every
file's name matches its content (all 93 ADRs, 25 core feature docs, 21
domain feature docs) — clean, no mismatches. User asked to track and
work all 5 findings; all five are now resolved:

- **`01-c4-architecture.md`'s GraphQL resolver diagram gained
  `ExportLineage Resolver`/`PlaybackAsOf Resolver` nodes** (`ADR-068`),
  routed through the same `IPayloadMasker` transform every other resolver
  already uses — not a separate enforcement path. Also fixed, found
  while in the file: a stale `Phase 8` build-plan cross-reference on the
  same diagram's `IPayloadMasker` node.
- **`06-solution-structure.md`'s project layout gained
  `EventStore.InterchangeAdapters`/`.Hl7v2MllpListener`** (`ADR-072` — the
  MLLP listener is the one background TCP component in an otherwise
  all-HTTP/GraphQL solution, stated explicitly rather than glossed over)
  **and `client-web/offline-player/`** (`ADR-068` — an alternate
  `vite-plugin-singlefile` build target of the same Vue playback
  component, not a second app).
- **`ADR-093`'s "the ticket-exchange secret needs a new entity" claim was
  wrong, and is now corrected in place** (a "correction, verified
  against `ADR-040`" marker, not a strikethrough — the rotation
  *mechanism* this ADR decided is unaffected, only its assumption that a
  new persisted entity was needed). The shared secret is either the
  caller's already-registered OAuth2 `client_secret` (DevIdp-side state,
  outside `EventStoreContext` per `auth.md`'s own established
  convention — its rotation is ordinary OAuth2 client-credential
  rotation, not a new field) or a caller-generated `one_time_secret` with
  no persistence at all by design. No entity added; none was needed —
  this is the same "check whether it's already resolved before assuming
  a new decision is needed" discipline `masking.md`'s fix used earlier
  this session, applied one level earlier (before writing any code shape
  at all, not after).
- **`docs/features/ticket-exchange.md` written** (`ADR-040`) — two
  sequence diagrams (issuance+signing, resolution), a "no new entity"
  Data model section (explaining the point above), a 2-screen Salt
  mockup, and a Gherkin block covering single-use consumption, wrong-
  secret rejection, expiry, the `one_time_secret` path, and the
  claims/masking/audit pipeline applying unchanged to a resolved ticket.
- **`docker-compose.yml` removed from `TODO.md` rather than left open** —
  it isn't "doing left" this file tracks; it's the already-known,
  expected consequence of this repo having no `src/` yet. Nothing to fix
  in the docs themselves.

**Caught my own instance of the same PlantUML escaped-quote bug fixed
earlier today**, while writing `ticket-exchange.md`'s Salt mockup —
checked for it specifically this time rather than trusting the write was
clean, since the earlier fix's own lesson was exactly "don't trust a
diagram looks right just because the prose around it is accurate."

`TODO.md`'s Active section is empty again.

## `08-build-plan.md` reworked from scratch

Direct instruction: "08-build-plan.md should be reworked from scratch
first." Clarified via an explicit choice (fresh rebuild for confidence /
something specific known-wrong / change the organizational model) — user
chose the fresh rebuild: keep the existing dependency-checklist model
(named items, no `Phase N` numbering) but re-derive every one of the 48
items' Scope/Depends-on/Exit-criteria directly from its source ADR(s)
from scratch, rather than trust the file's incremental edit history.

Dispatched as 10 parallel background agents, each owning 5 consecutive
items, each drafting to its own scratch file rather than the shared
target (`.claude/protocols/parallel-batch-dispatch.md`'s "same file →
separate scratch files, consolidate centrally" pattern) — then read all
10 in full and hand-consolidated into a single rebuilt file, resolving
every cross-batch dependency finding into one consistent topological
order and rebuilding both PlantUML diagrams (`BuildPlan_CorePhases`,
`BuildPlan_Additions`).

**This surfaced a real, sizeable set of missing dependency edges** — a
claim-gated forward reference, a shared mechanism reused "unchanged" that
the citing item never actually declared a dependency on — plus a few
genuine scope errors, all fixed in place:

- **MVVM Client's dependency set was substantially wrong**: CQRS
  Read-Model Projections, Streaming Channels, and Binary Attachments were
  all removed as ungrounded (none is named anywhere in `ADR-039`'s own
  Context/Decision text as a build gate); **Sharding & Replication**
  added as the single most load-bearing dependency the item had been
  missing entirely (`ADR-090`'s read-your-writes guarantee).
- **Signing Secret Rotation's Scope line restated the pre-correction,
  wrong version of `ADR-093`** (claiming the ticket-signing secret becomes
  a schema-level current+previous pair — it doesn't, only
  `WebhookSubscription` does) — this had survived because the item's own
  text was never revisited after `ADR-093` was corrected in place earlier
  this session. Fixed, and its Depends-on line's "Ticket Exchange"
  dependency (never real — there was never a single-secret field there to
  revise) replaced with "Auth (OIDC/OpenIddict) + Orchestration."
- **Control-Plane Actions as Reserved Events' exit criteria overclaimed**
  `RoleGranted`/`AppTrustRootRegistered` test coverage that doesn't exist
  in any Gherkin scenario anywhere — narrowed to what's actually proven.
- An unverified class name, `PredicateTranslator`, was dropped from
  "Follow API + Filter Pushdown"'s scope (no ADR, feature doc, or
  `06-solution-structure.md` ever names it).
- **Property-Level Masking's rejection rule undercounted `ADR-009`'s own
  decided strategies** — the item said "reject anything but FixedValue,"
  which would incorrectly reject `PartialReveal`/`Hash`, both of which
  `ADR-009` explicitly promoted into v1.
- A dozen more real dependency additions across items too numerous to
  list individually here — see `docs/08-build-plan.md` itself, each one
  now explains what it found and why, inline at the point it's used.
- **A real ordering bug introduced mid-consolidation and caught before
  finalizing**: "Tenant-to-Tenant Federation Mapping" was initially placed
  before "Bulk Ingestion & External Interchange-Format Adapters" even
  though the former now correctly depends on the latter — swapped once
  the diagrams were checked for a valid topological order end to end.

**Several gaps this pass found are documentation/coverage work, not
build-plan structure** — tracked as 10 new items in `TODO.md` rather than
silently fixed here or silently dropped, including a real, ADR-self-
disclosed propagation gap (`ADR-087`'s i18n requirement never actually
written into `docs/features/mvvm-client.md`) and a still-open,
unresolved mystery: a background agent doing this rework reported
receiving a mid-task instruction to "create a
`.\docs\develop\{epic}\{feature}.md`," arriving truncated with no defined
taxonomy — correctly declined to act on it unilaterally on a file nine
other agents were concurrently touching, surfaced to the user rather than
guessed at.

## TODO item 10 (the `docs/develop/{epic}/{feature}.md` mystery) dropped

Direct instruction: drop it. Not resolved and not acted on — the
instruction's own source was never identified, so this isn't a decision
reversal, just a choice not to chase an unclear, possibly-spurious
mid-task message any further. No file was created, nothing else in the
repo referenced this item, so dropping it needed no other cleanup.

## The remaining nine TODO items worked — `TODO.md` empty again

Direct instruction: review the docs and work through everything left in
`TODO.md`. Dispatched as 8 parallel background agents (`.claude/
protocols/parallel-batch-dispatch.md`), each owning a disjoint file — 5
editing an existing feature doc, 3 writing a new one from
`.claude/templates/feature-doc-template.md` — then consolidated
`08-build-plan.md` (the one shared file every item's gap-note pointed at)
by hand afterward.

**Existing feature docs edited**:
- `features/mvvm-client.md` gained an "Internationalization &
  localization (`ADR-087`)" section alongside its existing Accessibility
  one — translation-key discipline in `ViewDefinition.TemplateContent`,
  `Intl`/CSS-logical-properties RTL layout, `Accept-Language` negotiation
  — plus two new Gherkin scenarios. The new section itself names one
  honest, still-open gap: `ADR-087`'s text doesn't say whether the
  generic native-fallback view (raw property-name labels, no registered
  view definition) is also in scope for translation-key resolution —
  flagged in place rather than guessed at.
- `features/lineage-export-and-playback.md` gained an
  `RFC3161Timestamp : string?` field on `ExportManifest` and a Gherkin
  scenario, verified against `ADR-086`'s own `Signature.RFC3161Timestamp`
  naming and `03-api-contracts.md`'s existing `exportLineage` comment
  (no literal field name existed for the export-bundle case specifically,
  so the same verified identifier was applied for naming consistency
  between `ADR-086`'s two use sites).
- `features/auth.md` gained a new sequence diagram and seven Gherkin
  scenarios for `RoleGranted`/`RoleRevoked`/`PermissionGranted`
  (`ADR-046`)/`AppTrustRootRegistered` (`ADR-044`) publication as
  hash-chained reserved events (`ADR-067`) plus an explicit
  replay-rebuild scenario, mirroring `schema-registry.md`'s existing
  `SchemaRegistered` precedent. Left `PermissionGranted`/
  `AppTrustRootRegistered`'s `EntityId` format unspecified (ADR-067 only
  gives an explicit example for `Role`) rather than inventing one, and
  declined to assert a `Role.Grantees` field one domain doc informally
  implies but `docs/data/schema-registry.md`'s actual class doesn't have.
- `features/streaming-channels.md` gained `ThreadId`-grouped
  multi-channel session coverage (`ADR-081`) and `RedactedRange`
  substitution coverage (`ADR-052`, reusing `PartialRevealMaskingStrategy`
  where applicable) — two new sequence diagrams, ER additions verified
  against `docs/data/streaming-and-attachments.md`, six new Gherkin
  scenarios. Left a separate, already-tracked `RequiredClaims`-naming
  drift in this file's ER diagram untouched — out of this item's scope.
- `features/entity-concept.md` gained `LateArrivalFlag : bool` (verified
  against `docs/data/event-log.md`) on its `StoredEvent` ER entity and
  two new Gherkin scenarios, explicitly distinguishing it from
  `ConflictFlag` per `docs/patterns/interactions/fold-ordering-and-
  conflict.md`: `ConflictFlag` compares `ExpectedVersion` and never
  blocks the write; `LateArrivalFlag` compares `OccurredAt` against
  `LastAppliedLogicalTime` and does withhold the change. One scenario
  fires both flags on the same event, matching the interactions doc's
  own composition table.

**New feature docs written**:
- `features/dpop-and-tamper-evidence.md` (`ADR-017`/`ADR-019`) — DPoP-
  proof validation extending `auth.md`'s existing token-flow diagram
  (missing-proof, bad-signature, `htu`/`ath`/`cnf.jkt` mismatch, and
  `jti`-replay branches, each mapped to `ADR-013`'s Problem Details
  table) and hash-chain verification via `GET /events/verify`. Declined
  to add a scenario for RFC 9449 §8's server-chosen nonce challenge since
  `ADR-017` explicitly scopes that out for v1.
- `features/upcast-materialization-and-downcast.md` (`ADR-027`/`028`/
  `053`) — publish-time materialization, the background
  `UpcastMaterializer`'s backlog reconciliation, the fold-skip invariant,
  and a read-time `downcastToPrevious` walk. Confirmed there's no
  separate `UpcastMaterialization` table — it's `StoredEvent.EventKind`/
  `MaterializationOfEventId` on the existing row, called out explicitly
  in the ER diagram rather than inventing a new entity.
- `features/compatibility-and-versioning.md` (`ADR-038`) — one home for
  all four of that ADR's decisions (enum unknown-value fallback, version-
  discovery capability negotiation, Expand/Contract migration, and the
  N-1/N+1 rollback window, restating rather than redefining the rollback
  drill already in `08-build-plan.md`). An explicit "wire-shape caveat"
  paragraph flags that `ADR-038` states the capability-negotiation
  handshake only abstractly — the doc's own realization of it is labeled
  as this doc's structural choice, not an ADR-stated shape.

**`08-build-plan.md` consolidation** (done by hand, not a background
agent, since every item's gap-note pointed at this one shared file):
every resolved item's "Note — ... tracked in `TODO.md`" callout rewritten
to state the resolution and link the new/edited doc, at Property-Level
Masking, Entity-Centric Core Rebuild, Hardening & Evolution, Upcast
Materialization + Downcast, Streaming Channels, Control-Plane Actions as
Reserved Events, RFC 3161 Trusted Timestamping, i18n/l10n Architectural
Scope, and Compatibility & Deployment Discipline. Two items got more than
a pointer-swap:
- **`revealField(...)` given an actual home**: added to "GraphQL-Only
  Query Layer"'s own Scope and Exit criteria (the first point in the
  build sequence where GraphQL actually exists to carry it); "Property-
  Level Masking"'s note updated to point there instead of describing an
  unhomed gap.
- **Binary Attachments' and Sharding & Replication's GraphQL-browse/
  cross-shard-fan-out forward-ordering notes reworded, not removed**: on
  inspection these were never actually "undeclared" — the notes
  themselves already stated the dependency explicitly (written in the
  same pass that opened the TODO item). Reworded to drop the stale
  "undeclared"/"tracked in TODO.md" language and state plainly that
  re-verifying those two exit criteria once "GraphQL-Only Query Layer"
  lands is a deliberate, accepted build-order consequence — not a doc
  gap, and not actionable further in a design-only repo with no `src/`
  to actually run that later build item against.

**`docs/patterns/README.md` kept in sync**, per its own standing
instruction: added a missing "Expand/Contract (Parallel Change) database
migration" catalog row (`ADR-038` — genuinely never catalogued before,
surfaced while writing `compatibility-and-versioning.md`), and extended
two existing rows' "Applied in" column — Tolerant Reader (now also citing
`ADR-038`'s enum-fallback/N-1/N+1 window as the same tolerance restated
as a deployment guarantee) and Strategy Pattern (now also citing
`ADR-053`'s `IUpcastExpressionEvaluator` seam, missed when that ADR was
originally written).

`TODO.md`'s Active section is empty again.
