# 2026-08-11

## Full docs-vs-implementation alignment audit (all 39 completed build-plan items)

Direct request: review the implementation against the design docs for
everything built so far (not just the immediately-preceding items 38/39)
and fix any drift found.

**Method**: 4 read-only research agents in parallel (one per item range,
1-10/11-20/21-30/31-39), each reading every relevant ADR/data-model doc/
feature doc/solution-structure passage for its range and comparing it
against the real `src/`/`tests/` code, reporting findings without
editing anything. Findings were explicitly scoped to exclude anything
`docs/08-build-plan.md`'s own text, `TODO.md`, or a `docs/changes/*.md`
entry already honestly narrated as a known narrowing/gap — only genuine,
*silent* drift in some other doc counted. Once all 4 reports were back,
6 fix agents ran in parallel across disjoint file sets (no two agents
ever touched the same file) to apply the corrections: ADRs got additive
strikethrough-and-correct edits per `.claude/protocols/additive-history-
editing.md`; every other doc type got a direct rewrite of the stale
passage.

**Result**: 44 files changed (+1516/-631 lines), zero code files
touched (confirmed via `git status` before committing — every change is
doc-only). `dotnet build EventStore.slnx` reconfirmed clean afterward
(doc changes can't break a build, but confirmed no agent strayed outside
its assigned files anyway).

### Data-model docs (shape authority, `docs/data/*.md`)

`docs/data/dbcontext-and-conventions.md`'s `EventStoreContext` sketch
had drifted the most of any single doc, having never been kept in sync
across many earlier items:
- Fixed the constructor signature (missing `IJsonPathTranslator`
  parameter).
- Removed `AppTrustRoots`/`Roles`/`UserPermissions`/
  `TrustedFederationIssuers` — these live on `EventStore.DevIdp`'s own
  `DevIdpDbContext`, not `EventStoreContext`; `docs/data/schema-
  registry.md` already said so, this sibling doc never caught up.
- Renamed `EventTypes`→`EventTypeDefinitions`,
  `WebhookOutboxItems`→`WebhookOutbox`,
  `AppDataResidencyPolic(y|ies)`→`AppResidencyPolic(y|ies)`.
- Added every DbSet missing from the sketch entirely:
  `DerivationDefinitions`, `DerivationCursors`, `PendingJoinStates`,
  `TelemetryChannels`, `TelemetrySamples`, `RedactedRanges`,
  `Attachments`, `AttachmentRefs`, `AccessLogEntries`,
  `LocalErasureKeyMaterials`.
- Fixed the `FilterableFields→EventTypeDefinition` FK to its real
  three-part key (adding `EventTypeAppId`).
- Deleted fabricated `HasIndex`/`HasMaxLength`/`ValueGeneratedOnAdd`/
  `HasColumnType` calls that don't exist anywhere in the real fluent
  config or migrations.
- **Found a new gap in the process**: `ChainCheckpoint` (`ADR-089`'s
  archival mechanism) was presented as a live DbSet but doesn't exist
  anywhere in `src/` at all, not even as a plain class. Now marked "not
  yet a registered DbSet" instead of live — tracked in `TODO.md`.

`docs/data/schema-registry.md` — renamed `AppDataResidencyPolicy`→
`AppResidencyPolicy`, added the real `LastAppliedSequenceNumber` field
and its synchronous-fold mechanism.

`docs/data/event-log.md` — the `ChainHash` formula was missing its
conditional `JSON(Signature)` fold-in (added by `ADR-066`, a real bug
fix narrated in `docs/changes/2026-08-10.md` but never propagated to
this doc). Fixed, with the exact conditional formula and real call
sites (`EventAppender.cs`, `ChainVerificationService.cs`).

### ADR Consequences corrections (additive, per protocol)

13 ADRs had Consequences sections still claiming something was "not yet
built" that has since actually shipped, or an example/Decision detail
that turned out subtly wrong once the real thing got built. Every
correction below is a strikethrough-plus-note per `.claude/protocols/
additive-history-editing.md` — nothing was silently rewritten:

- `adr-007`/`adr-009`: status corrected from "Deferred"/"deprioritized"
  to built (items 8/9 are Done). Same fix mirrored in `docs/07-
  adrs.md`'s index.
- `adr-019`: `ChainHash` formula note extended (see data-model fix
  above), cross-referencing `ADR-066`.
- `adr-023`: example response envelope corrected (dropped a nonexistent
  `timestamp`, added the real `conflictFlag`); noted `RouterWorker`
  never persists a discrete `"processing"` row — received→applied
  happens atomically in one tick, not via an intermediate persisted
  state.
- `adr-024`: added the ADR-078-requested cross-reference clarifying this
  ADR covers write-time conflicts only, not concurrent-fold safety.
- `adr-058`: corrected the claim that `Retry-After` is uniform across
  all three rate-limiter algorithms — only `TokenBucketRateLimiter`
  attaches it. Added a note that `TenantPartitionKey.Resolve` falls back
  to an unvalidated JWT peek at the Gateway (cross-ref `ADR-049`).
- `adr-060`: struck through "not yet placed" — `docs/data/schema-
  registry.md` already fully documents the webhook entities.
- `adr-062`: struck through "needs a `<PackageId>` line added per
  project" — the real mechanism is the root `Directory.Build.props`
  (item 39), not per-project lines.
- `adr-067`: struck through the `{appId}:role:{roleId}` EntityId
  example — the real shape is synthetic composite keys
  (`actorId:roleName`/`actorId:permission`).
- `adr-077`/`adr-078`: struck through "not yet built" — both shipped in
  items 31/32. `adr-078` also gained a note that only 3 independent
  `LeaderLease` rows exist, not the 4 named in its own Decision text —
  `UpcastMaterializer` runs inline under `RouterWorker`'s own lease,
  never getting an independent one.
- `adr-079`: struck through as doubly stale — the KYC domain README was
  already updated, and item 38 has since built and closed the seam (in
  test code, matching the ADR's own "domain-scoped, not core" intent).
- `adr-091`: struck through "no pipeline, workflow file... is created by
  this ADR" — `.github/workflows/ci.yml`/`.github/dependabot.yml` exist
  now (item 39), with a note that they're written but never actually
  executed by GitHub Actions itself (no push access in this
  environment).

### Central architecture docs

`docs/06-solution-structure.md` had the widest single-file blast radius
after the data-model doc: `EventStore.Client.Core` was rewritten as
never-built (the real client outbox/entity-cache/command-dispatch layer
is Pinia stores directly inside `client-web/`, not a separate .NET
project); a stale `EventStore.Client.Vue/` test-location reference was
corrected to `client-web/src/`; `EventStore.Rbac`/`EventStore.
Abstractions` were added to the project list (both existed in code, just
never got a bullet); the claim that `EventStore.Follow.Api`/`.Lineage.
Api` "no longer exist... folded into GraphQL" was corrected — both are
still real, separately-deployed projects; stale code sketches for
`IPayloadMasker.Mask()` (now async `MaskAsync`) and the upcast engine
(now the real sync `UpcastChain.Apply`/`UpcastOutcome` shape, with the
retired `EventUpcastFailed` dead-lettering snippet dropped) were
replaced; the claim that GraphQL exposes `entity(id) { attachments } }`
was corrected to a confirmed, not-yet-built gap; three stale
`[UseFiltering]` references were corrected to the real hand-rolled
`GraphQlFilterPredicateBuilder` mechanism; the data-lifecycle backup
table gained rows for `RedactedRange`/`AppResidencyPolicy`/
`FeatureFlagState`/`LeaderLease`/`WebhookSubscription`/`WebhookOutbox`/
`WebhookDeliveryCursor`/`LocalErasureKeyMaterial`, all previously
missing.

`docs/01-c4-architecture.md` — added the `EventStore.Gateway` container
in front of every other container as the sole external entry point
(`ADR-049`), replacing the stale diagram showing every external actor
connecting directly to Inbox/Router/GraphQL.

`docs/glossary.md` — added `OriginId`/`LogicalClock` (`ADR-033`);
Tolerant Reader/Postel's Law, Expand/Contract, the N-1/N+1 window, and
capability negotiation (all named explicitly in `ADR-038` but missing);
fixed the `TelemetryPointer` entry to the real list-based
`TelemetryPointerEntry` shape (`ADR-081`, Accepted) and added `ThreadId`,
removing stale "still under discussion" framing.

`docs/patterns/README.md` — added the missing Leader Election row
(`ADR-078`).

`docs/extensibility-points.md` — added a note on which package each
catalogued interface actually ships in, distinguishing the 5 interfaces
now consolidated into `EventStore.Abstractions` (item 39) from the ones
that deliberately stayed in their original projects.

### Numbered core docs

`docs/03-api-contracts.md`/`docs/04-odata-filter-pushdown.md`/`docs/05-
schema-registry-and-spec-generation.md` all had multiple stale passages
describing mechanisms that were evaluated and replaced during
implementation: a single `JsonFunctions.JsonValue` marker method (real:
four type-specific methods dispatched via `MethodNameFor`); HotChocolate's
`[UseFiltering]`/`[UsePaging]`/`[UseProjection]` middleware (real: a
hand-rolled `GraphQlFilterPredicateBuilder`, since the dynamic per-app
schema can't use HotChocolate's static-type-driven middleware); nested-
object/`and:`-combinator GraphQL filter syntax (real: a flat AND-only
list); a `SchemaValidationService`/`ParentLinkService` pair returning
sync 400/404 (real: inlined checks in `PublishService`, async advisory
`SchemaStatus` per `ADR-023`); masking restricted to `FixedValue` only
(real: all three strategies); singular `requiredPublishClaim`/
`requiredReadClaim` (real: the `RequiredClaims` array, per `ADR-050`).
Also added an explicit note that the `revealField` mutation's `ADR-066`
step-up gap remains open even after item 29 landed (that item only
wired step-up into publish-time enforcement).

`docs/07-adrs.md` — index rows for `ADR-007`/`ADR-009` corrected to
match the ADR files' own fixes above.

`docs/08-build-plan.md` — item 11's Scope bullet named the wrong
interface (`IEventUpcaster`, a distinct, still-undelivered seam) instead
of the real `IUpcastExpressionEvaluator`; item 16's deferred-
reverification note updated to say the reverification happened and
*failed* (the GraphQL-attachments gap is confirmed, not still pending);
item 19's built-scope note corrected to say the `revealField` step-up
gap is still open post-item-29, not implicitly closed by it.

`docs/09-cqrs-read-models.md` — replaced the "subscribes via a GraphQL
Gateway subscription document" banner with the real mechanism:
`ProjectionHost`'s `FollowClient` uses a plain HTTP `QUERY /follow/
{eventType}` request and raw SSE parsing, no GraphQL anywhere.

### Feature docs, patterns, library docs (18 files)

Recurring patterns across `docs/features/*.md`: the same `[UseFiltering]`/
flat-filter-syntax drift as the numbered docs (`filter-pushdown.md`,
`follow-subscribe.md`); the same GraphQL-vs-REST/SSE transport drift
(`cqrs-projections.md`); a stale `POST /oauth/token` route in place of
the real `POST /connect/token`/`POST /oauth/ticket-exchange` pair
(`ticket-exchange.md`, `auth.md`, `docs/patterns/ticket-exchange-
headerless-clients.md`); RBAC diagrams showing an in-process
`EntityStoreRow` fold instead of the real cross-process `EventStore.
Rbac` + `RbacProjectionWorker` fold (`auth.md`); a stale 4-client/4-scope
DevIdp seed table instead of the real 11/11 (`auth.md`); a fictitious
persisted `"processing"` status row (`entity-concept.md`); a nonexistent
`EventParentReader` component (`event-chains.md`, real: inlined in
`LineageService`); GraphQL attachment-browsing presented as built
instead of a confirmed gap (`binary-attachments.md`); DPoP failure
`reason` codes documented as a fixed enum when the real code returns
free text (`dpop-and-tamper-evidence.md` — softened the doc, deliberately
did NOT change the production auth code to match); a per-resource-type
interchange route instead of the real generic `/interchange/{adapterKey}/
{appId}` (`bulk-ingestion-and-interchange-adapters.md`, `multi-
tenancy.md`'s federation diagram); a stale `IPayloadMasker.Mask()`
signature (`docs/patterns/strategy-pattern-extensible-masking.md`); a
stale `.PeerSync` project name instead of `EventStore.Replication`
(`docs/libraries/dotnet/spiffe-spire.md`); an overstated "swappable via
configuration" claim for the upcast-evaluator choice (`docs/libraries/
dotnet/jsonata-dotnet.md`, softened to "a code-level DI edit today").
`docs/domains/digital-identity-kyc/features/periodic-screening-and-sar-
escalation.md` had one small wording fix: the `ISanctionsScreeningProvider`
composition it once called "a future pass" has now actually been
demonstrated, in test code, by item 38.

### CLAUDE.md

Found the exact same class of drift in `CLAUDE.md` itself while doing
this pass: a bullet referenced "`TODO.md`'s data-model drift table" as
still-existing cleanup work, and a separate bullet said `ChannelOrigin.
Origin` vs. `OriginId` disambiguation was "tracked in `TODO.md`" — neither
is actually in `TODO.md` anymore (the disambiguation is handled inline in
`docs/data/streaming-and-attachments.md`, and the drift table's own named
examples are exactly what this pass's data-model-doc fixes above closed).
Corrected both bullets to point at the real current state rather than a
stale backward reference.

## Signing Secret Rotation, Dual Signature (build-plan item 40) — webhook half only

Before building, verified `ADR-093`'s own claim that "OpenIddict already
supports a client holding more than one valid credential — no framework
change needed" (the premise the ticket-exchange half of this item rested
on). Searched OpenIddict's own documentation, source, and issue tracker
(`openiddict/openiddict-core`) for a built-in multi-secret-per-client
mechanism — found none; `OpenIddictApplicationDescriptor.ClientSecret`
is a single string per registered application. This repo's own "verify
before citing" standing instruction caught a false premise before
anything got built on top of it, rather than after.

Presented the finding to the user with two options (build a real custom
credential-validation mechanism, or descope and track as follow-up); the
user chose to descope. Corrected `ADR-093` additively (struck through
the false claim in both the Decision and Consequences sections, in both
places it appeared) and narrowed `docs/08-build-plan.md`'s item 40 scope
to the webhook half only. The ticket-exchange rotation mechanism (a
custom OpenIddict event handler, or a stopgap second registered
application) is real, undesigned follow-up work, tracked in `TODO.md`,
not silently dropped.

**Built (webhook half)**: `WebhookSubscriptionService` gained
`RotateSigningSecretAsync` (moves the current `SigningSecret` to
`PreviousSigningSecret`, generates a fresh one) and
`DiscardPreviousSigningSecretAsync` (clears `PreviousSigningSecret`,
ending the overlap window). `WebhookSigner.Sign` gained an optional
`previousSigningSecret` parameter — verified the real Standard Webhooks
multi-signature format directly against the spec text ("both signatures
are sent, space delimited, in the webhook-signature header") before
implementing, rather than assuming the shape `WebhookSigner.Verify`
already tolerated (built ahead of this item, during item 34, on the
receiving side) was necessarily what the spec actually says. Two new
endpoints in `WebhookEndpoints.cs`: `POST /webhooks/subscriptions/{id}/
rotate-secret` and `POST /webhooks/subscriptions/{id}/discard-previous-
secret`.

One comprehensive test in `WebhookDeliveryHttpSqliteTests.cs`
(`RotatingTheSigningSecretEmitsDualSignaturesDuringTheOverlapWindow
ThenASingleSignatureOnceDiscarded`) proves the full lifecycle end to
end: exactly one signature before rotation; two signatures during the
overlap window, each independently verifiable via `WebhookSigner.Verify`
against either the old or the new secret (a receiver caching only the
old secret still verifies successfully); back to exactly one signature
after discarding, verifiable only against the new secret, with the old
secret confirmed to no longer verify. SQLite-only, matching this file's
own established "signing/HTTP delivery mechanics are provider-agnostic"
posture. Full webhook regression suite (12 tests) re-run clean.

## Lineage Export & Bitemporal Playback (build-plan item 41) — Done

Server-side: `EventStore.LineageExport/` (`LineageExportService.ExportAsync`/
`ImportAsync`/`CheckRootAsync`, `BitemporalPlaybackService.ReconstructAsync`,
`ExportManifest`/`ExportedEventLine`/`LineageExportBundle` NDJSON,
`ManifestHash`, a 15-minute `IMemoryCache`-backed `LineageExportBundleStore`),
two new GraphQL Query fields (`exportLineage`/`playbackAsOf`) and two new
REST endpoints (`/lineage-exports/{id}`, `/lineage-imports`), wired into
all three Hosts. `LineageExportHttpSqliteTests.cs` (7 tests, real HTTP
through the actual Gateway).

Fixed a test-design bug found via the previous session's summary before
continuing: `ImportingAValidBundlePreservesProvenanceAndFoldsIntoThe
ReceivingEnvironment` had imported an exported bundle back into the SAME
database it was exported from, causing a legitimate `EventId` unique-
constraint collision (imports correctly preserve the original `EventId`,
per `ADR-068`'s literal text) — not a service bug. Rewrote the test to
construct a genuinely separate destination SQLite database and call
`LineageExportService.ImportAsync` directly, matching this ADR's own
Gherkin's "into a fresh environment" framing. All 7 tests pass; full
SQLite/Postgres regression suite re-run clean. A separate SqlServer-only
full-suite run showed 10-21 failures depending on run, but every failing
test name (`AllEntityScenarios`, `AllDerivationScenarios`, etc.) predates
this item and traces to Docker/Testcontainers readiness-check timeouts
under concurrent SqlServer-container load (`fs.aio-max-nr`-class resource
contention, already tracked in `TODO.md`) — a dedicated rerun of the same
SqlServer test classes passed clean once contention eased, confirming
this item introduced no regression.

**Offline player (`ADR-068 §3`), the piece flagged mid-session as not
yet built**: `client-web/src/playback/bundle.ts` (NDJSON parser,
mirroring `LineageExportBundle.ParseNdjson` field-for-field) and
`verifyBundle.ts` (manifest-hash recomputation + masked/erased-field
count). `BitemporalPlaybackControl.vue` (the live VCR control) and
`OfflineBundleViewer.vue` (the verification-result + event-list screen)
under `client-web/src/components/playback/`, mounted from both the
ordinary app and a new `client-web/offline-player/` entry point built via
a second Vite config (`vite.offline-player.config.ts`,
`vite-plugin-singlefile`) into one self-contained HTML file;
`client-web/scripts/embed-bundle.mjs` does the per-export "embed and
rebuild" step `ADR-068 §4` describes.

One genuine correctness question resolved by direct verification rather
than assumption, per this repo's "verify before citing" rule:
`ManifestHash.Compute` hashes `DateTimeOffset:"O"`-formatted text, but
`System.Text.Json` trims trailing-zero fractional-second digits when
serializing the same value — a real divergence, confirmed by writing and
running a small throwaway C# program, not just reading the two APIs'
docs. `reconstructODateTimeOffsetString` pads the JSON string's
fractional part back to 7 digits, losslessly (a value only gets trimmed
by `System.Text.Json` when the trimmed digits were genuinely zero) — the
padded reconstruction was then cross-checked against a real C#-computed
`ManifestHash.Compute` output for a fixed input set, byte for byte, and
that exact pairing is now a permanent regression test
(`verifyBundle.spec.ts`).

A second, narrower scope decision, also verified rather than assumed:
the player does **not** attempt to recompute each event's own
`PayloadHash` from its `Payload` (`EventPayloadHash.Compute`'s formula
also needs `ParentEventIds`, which `ExportedEventLine` never carries —
attempting it would false-flag any event with real causal parents,
`ADR-005`, as tampered). Traced directly through `PayloadMasker.cs`'s
`RevealAsync`/`MaskLeafAsync` to confirm even a fully-visible maskable
field is always wrapped in `{"value": ...}`, never passed through byte-
identical to the original stored `Payload` — meaning "fully verified"
vs. "verified except N masked fields" is honestly derived from the
manifest-hash check plus a masked/erased-field count, not a per-event
byte-exact re-hash the ADR's own sequence-diagram wording had implied
was straightforward. `ADR-068`'s Consequences section and the matching
feature doc both got an additive "Corrected, 2026-08-11" note recording
this — the ADR itself had already flagged this exact build target as
"not yet detailed," so this is filling in a deliberately left gap, not
overriding a firm prior decision.

Verified genuinely end-to-end, not just via `vitest`: ran
`npm run build:offline-player`, embedded a real bundle via
`embed-bundle.mjs`, and rendered the resulting double-clickable HTML
file headlessly in a real installed browser (Edge, `--headless=new
--dump-dom`), confirming the rendered DOM actually shows "Fully
independently verified." That same end-to-end pass caught a real bug
unit tests alone had missed: `offline-player/index.html`'s own
explanatory HTML comment happened to spell out the literal placeholder
marker string, so `embed-bundle.mjs`'s single, non-global `.replace()`
patched the comment's mention instead of the real one inside the
`<script>` tag — fixed by rewording the comment to describe the marker
without repeating its exact text. Client test suite: 49/49 passing (10
files). `docs/06-solution-structure.md` and `08-build-plan.md`'s own
`client-web/offline-player/` description (written in an earlier pass)
needed no correction — the implementation matched it as designed.

## RFC 3161 Trusted Timestamping (build-plan item 42) — Done

Same session, continuing directly from item 41. Two consumers, per
`ADR-086`: Digital Sign-Off's `Signature.RFC3161Timestamp` (opt-in per
event type, `RequiredSignature.EnableRfc3161Timestamp`) and Lineage
Export's `ExportManifest.Rfc3161Timestamp` (always populated when a TSA
is configured, no separate opt-in).

Researched the real client-side API before building anything, per this
repo's "verify before citing" rule: `System.Security.Cryptography.Pkcs`
(BCL, since .NET 5) already ships `Rfc3161TimestampRequest`/
`Rfc3161TimestampToken` — building/encoding a request, and parsing +
cryptographically validating a TSA's response — so no third-party RFC
3161 library is needed for `EventStore.Timestamping`'s
`HttpTimestampAuthorityClient` (the default `ITimestampAuthorityClient`,
`EventStore.Abstractions`, registered only when `Timestamping:TsaUrl` is
configured, the same "no silent fallback" posture `AddErasure`'s Vault
registration already established).

Needed a real TSA to test against, and building one from scratch (not
just consuming one) surfaced real gaps not visible from the client-side
docs alone, each resolved by reading `dotnet/runtime`'s actual source
(sparse-cloned this session) rather than guessing:
- The BCL ships no public API for *acting as* a TSA (issuing a token) —
  only for building a request and parsing/verifying a response. Building
  the fake TSA's `TimeStampResp`/`PkiStatusInfo` wrapper required reading
  the real (internal) ASN.1 templates directly, since no public type
  covers it.
- A first attempt at a signed response was rejected with "not
  understood" — traced to the mandatory `SigningCertificateV2`
  (`ESSCertIDv2`) signed attribute RFC 3161/5035 require, which
  `CmsSigner` doesn't add automatically; hand-encoded via `AsnWriter`
  once found.
- A second attempt was then rejected for carrying an unrequested signer
  certificate — `CmsSigner.IncludeOption` must be `None` to match a
  request that didn't ask for one (`RequestSignerCertificate: false`,
  the client default).
- The full request → fake-TSA-issue → verify round trip was run and
  confirmed working end to end (including reuse across multiple requests
  through the same in-process `TestServer`, proving the signing
  certificate/key survive across calls) *before* any of this shipped as
  either production or test code — `TimestampingTestSupport.cs`'s fake
  TSA is genuine RSA/CMS signing throughout, not a stub returning
  arbitrary bytes.

A second, unrelated cross-language correctness question, needed for
`LineageExportService`'s manifest timestamp to hash the right bytes:
does `System.Text.Json`'s `DateTimeOffset` serialization exactly match
`ManifestHash.Compute`'s own `:"O"`-formatted hash input? Verified
directly (a throwaway C# program) rather than assumed — the two do
*not* always match byte-for-byte (`System.Text.Json` trims trailing-zero
fractional-second digits; `:"O"` never does), the same fact item 41's
own offline-player work had already found and worked around client-side
that same session; this item's server-side code was written already
aware of it.

**One real bug found only by running the real HTTP round trip against
the real fake TSA, not by reading the code back**: `PublishService`'s
first attempt mutated the already-tracked `Signature` object's
`RFC3161Timestamp` property in place. Invisible to EF's change tracker —
`JsonValueConverter.NullableComparer<T>()`'s own snapshot function
returns the same object reference rather than a deep clone, a gap that
file's own comment already documented once before, for the exact same
class of converted property. The publish request still returned `202`
successfully and `Signature` itself was present; only the new field
silently never persisted. Fixed by assigning a **new** `Signature`
instance carrying the token, rather than editing the tracked one.

Tests: `TimestampingHttpSqliteTests.cs` (4 methods, Sqlite-only, same
provider-agnostic reasoning `DigitalSignOffHttpSqliteTests.cs` already
established) — the Signature half wires the real Host's own typed
`HttpClient` to the fake TSA's `TestServer` via
`ConfigurePrimaryHttpMessageHandler` (an in-process, no-real-socket
substitution, the standard technique for testing an `IHttpClientFactory`
typed client against a controlled backend); the Lineage Export half
constructs `HttpTimestampAuthorityClient` directly against the same fake
TSA. Every resulting token is independently decoded and re-verified via
`Rfc3161TimestampToken.VerifySignatureForHash` against the fake TSA's own
certificate — proving actual RFC 3161 compliance, not merely "some bytes
got stored." Full regression suite re-run clean except the two already-
tracked, pre-existing flakes (`TODO.md`): SqlServer-testcontainers
resource contention (worse this session than previously recorded — see
that entry's own severity-update note) and the SSE-subscription timing
flake — neither touches Timestamping, Lineage Export, or Digital
Sign-Off.

## Residual code-side gaps surfaced (not doc bugs — tracked in `TODO.md`)

A handful of findings were genuine implementation gaps the docs now
correctly describe as open, not doc corrections: `ChainCheckpoint` never
built; `IUpcastExpressionEvaluator`'s CEL/JSONata choice not actually
config-swappable; a stale "temporary field" code comment in
`RegisterEventTypeRequest.cs`; the `revealField` step-up gap; GraphQL
attachment browsing. Full detail in `TODO.md`'s own entry for this pass
— not restated here per this repo's own "don't duplicate `TODO.md`'s
list elsewhere" convention.
