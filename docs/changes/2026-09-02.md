# 2026-09-02

## Closed: `FollowClient` returns discriminated results instead of throwing for known outcomes (TODO.md)

`docs/patterns/known-outcomes-are-not-exceptions.md`'s own named follow-on,
finally done: `FollowClient.TailAsync` (renamed `ConnectAsync`) and
`GetChangeKindAsync` used to call `EnsureSuccessStatusCode()`, converting a
well-understood, routine outcome (an event type with no registered schema
yet for this AppId; a `RequiredClaims` gate the caller's own token doesn't
satisfy) into a thrown `HttpRequestException` the moment it crossed the
HTTP boundary — exactly the class of bug
`docs/bugs/framework/service/rbac-fold-404-logged-as-error-forever.md`
found and only partially fixed (one caller's own `catch` filter, not the
root cause).

New `FollowConnectResult`/`ChangeKindResult` (`EventStore.Projections.Host`)
mirror the server's own `FollowResult` (`Connected`/`UnregisteredEventType`/
`Forbidden`/`ValidationFailed`) exactly, one level up the HTTP boundary.
`ConnectAsync` is now a plain `Task<FollowConnectResult>` rather than an
`async IAsyncEnumerable` — an iterator method can't also return a
non-enumerable discriminated result for its own connect-time outcome, so
the streaming read (`ReadEventsAsync`) is a private iterator only ever
invoked once a `Connected` outcome is already known, wrapping the still-open
`HttpResponseMessage` and disposing it once the stream ends.

Both real callers — `ProjectionHost<TReadModel>.CatchUpOnceAsync` and
`EventStore.DevIdp.RbacProjectionWorker.CatchUpOnceAsync` (`Samples.Orders
.Projections` and every Vitals/Meridian flow worker go through the shared
`ProjectionHost<TReadModel>`, not a third independent call site) — now
switch on the discriminated result and return a new
`(CatchUpOutcome, EventsConsumed)` tuple instead of a bare `int`.
`UnregisteredEventType`/`Forbidden` are logged at `Warning` (moved from
`RbacProjectionWorker`'s own StatusCode-filtered `catch` into
`CatchUpOnceAsync` itself, where the discriminated result is already being
inspected) and return `0` immediately, no exception anywhere in the path;
`ValidationFailed` (a genuinely unexpected outcome for these clients' own
always-valid, hardcoded request shape) still throws, since that's a real
bug, not a routine branch. `ProjectionHost` couldn't reuse
`RbacProjectionWorker`'s own `DuplexInstrumentation.WorkerTailReconnects`
OTel counter for its own routine-case logging — `EventStore.Projections
.Host`'s own project file deliberately has no reference to
`EventStore.Domain` at all ("its only dependency on the write side is an
HTTP client," enforced by the reference graph itself), so it keeps a plain
`ILogger` warning instead.

`tests/EventStore.IntegrationTests/RbacProjectionWorkerHttpSqliteTests.cs`'s
own `CatchUpOnceAsyncThrowsAnHttpRequestExceptionWithNotFoundStatusForA
GenuinelyUnregisteredEventType` test — which explicitly asserted the OLD
throwing behavior — is now
`CatchUpOnceAsyncReturnsUnregisteredEventTypeInsteadOfThrowingForA
GenuinelyUnregisteredEventType`, asserting the new graceful outcome
instead. Full regression: `EventStore.UnitTests` 86/86,
`EventStore.IntegrationTests` 246/247 (the one failure,
`ASchemaInvalidButNotMalformedEventInsideABatchStillPersistsWithAnAdvisory
SchemaStatus`, is unrelated to this change and passes cleanly in
isolation — a pre-existing parallel-execution flake, the same class this
repo's own `[DoNotParallelize]` convention already documents elsewhere).

## Corrected and closed (in part): App.vue's "Dispatch a command" demo panel (TODO.md)

Yesterday's TODO.md entry for this gap made a claim that turned out to be
wrong on inspection, found before any code was written against it: "no
client-side mechanism exists today to recover a schema's original casing
from a registered `EventTypeDefinition`." One already exists —
`useEventComposer.getEventTypeDetail` (the Compose tab's own schema
introspection, `composer-client`'s `registry:admin` scope) already fetches
a schema's real, PascalCase-declared field names via `eventType(...)
.jsonSchema`, never the GraphQL-camelCased names `App.vue`'s cached read
side data uses. `.claude/protocols/verify-before-citing.md`'s own standing
rule paid for itself here — corrected before it propagated into a bigger,
unnecessary build (a new registry endpoint that already existed in a
different form).

**Fixed for real**: new `resolveEventTypeFieldCasing` (`useEventComposer
.ts`) reuses that same introspection purely to build a
`lowercase -> real-declared-name` map; `App.vue.submitAmountCommand` now
re-cases its merged cached-entity-fields patch through this map before
dispatching, satisfying any real schema's own case-sensitive `required`
check regardless of domain. Fetched once per instance (`fieldCasing` ref),
not on every dispatch. Falls back to an empty map (patch sent un-recased,
the prior behavior) rather than throwing if introspection itself fails —
never blocks a dispatch outright.

**A second, real, previously-undiscovered gap found and closed along the
way, unrelated to casing**: `types.ts`'s own `OutboxEntryStatus` already
declared a `'Failed'` terminal state, and `exportOutboxBundle`'s own
comment already assumed it existed ("Delivered/Failed entries are
excluded"), but nothing anywhere had ever actually set it —
`useOutboxStore.flush` only ever left a rejected entry `Pending`,
retried forever, indistinguishable from an ordinary transient failure,
with no visible signal anything was permanently wrong. New
`PublishResult.permanentFailure` (`publishClient.ts`, also threaded
through `useEventComposer.publish`'s own return shape) is `true` for a
400 (validation failure) or 403 (Forbidden) — outcomes no amount of
retrying will ever change — and `false`/absent for everything else
(a dropped connection, a real 5xx; a thrown exception is always treated
as transient too, since only a real HTTP response the server actually
returned can be a permanent rejection). `flush` now moves a
`permanentFailure` entry to `'Failed'` instead of incrementing `attempts`
forever; a new `failedFor` store getter and an `App.vue` header
`data-testid="failed-count"` element make a permanent rejection visible
instead of silently vanishing from the queued-command count.

**What's still genuinely open, left as a real TODO.md item, not decided
here**: no DevIdp-seeded HTTP client anywhere holds the specific
`RequiredClaims` a real Vitals/Meridian business event type demands (e.g.
`PatientScreened`'s `patient:enroll`) — those events have only ever been
created in-process by the seeders, bypassing the HTTP auth layer's claim
check entirely. This is a real security-policy fork (grant a narrow demo
claim vs. retire the generic panel), not a technical gap this pass
resolves — but a genuine rejection now fails loudly and visibly instead
of retrying forever in silence, which was the actual damage.

Tests: `outbox.spec.ts` gained 3 scenarios (permanent failure moves to
`Failed` and is never retried; an ordinary transient failure still
retries; a thrown exception is always transient). `useEventComposer.spec
.ts` gained 3 scenarios for `resolveEventTypeFieldCasing` (real casing
recovered; empty map for an unknown event type; empty map, never thrown,
when introspection itself fails) plus 2 for `permanentFailure`
propagating through `composer.publish` (403 flagged, 503 not).
`OfflineOutboxSyncPlaybookTests.cs` re-run live end to end after both
fixes, still passing — `docs/playbooks/core/user/go-offline-and-
resync.md` regenerated with fresh screenshots. Full `mvvm-client` suite:
145/145 (27 files).

## Closed: schema-registry state now replicates across peers (`ADR-103`, resolves `docs/10-open-questions.md` row 1)

Direct request, resolving the fork `docs/10-open-questions.md` row 1
left open after `ADR-102`'s own live cross-provider verification found
it: build a real fold worker, or permanently downgrade `ADR-033`'s own
claim. Built the fold worker.

`SchemaRegisteredEventType.cs`'s own header comment already gives the
real reason `EventTypeDefinition` was never rearchitected onto a
generic Router-fold — every prior build-plan item's tests assume
synchronous, immediately-consistent LOCAL registration, which an async
fold would break. This work respects that boundary: the local,
originating registration is completely unchanged; a **second**,
narrowly-scoped mechanism replicates it to peers that never
independently register it.

**What shipped**:
- `AppendSchemaRegisteredAsync`'s notification payload widened from
  `{EventTypeName, Version}` to the full registration — `JsonSchema`,
  `ChangeKind`, `EntityIdField`, `EntityType`, `ParentValidationMode`,
  `RejectionBehavior`, `RequiredClaims`, `FilterableFields` (including
  `ADR-096`/`097`'s `SearchableIndexConfig`), `RequiredSignature`,
  `ExpectedResponse`, `Upcast`/`DowncastFromPrevious`. Additive only —
  `required` stays `["EventTypeName", "Version"]`.
- **A real, separate bug found while designing this**: the notification
  stamped the literal string `"local"` as `OriginId` for *every* site,
  unlike an ordinary published event's own configurable
  `OriginIdOptions`. Every site's own notification was indistinguishable
  from every other's the moment they synced together — exactly the
  signal a fold reactor needs to tell "mine" from "elsewhere." Fixed via
  a new `SchemaRegistryOriginIdOptions`, duplicated from (not
  referencing) `EventStore.Inbox.OriginIdOptions` — `EventStore.Inbox`
  depends on `EventStore.SchemaRegistry` (`ADR-033`/`090`), so a
  reference the other way would cycle; both bind to the same
  `"OriginId"` configuration section, so a real deployment's one
  configured value resolves identically through either type.
- New `SchemaRegistrationReplicationResolver` (`EventStore.Router`) — the
  same "special-purpose reactor, one event type" shape
  `AuthorityDecisionResolver`/`EntityErasureResolver` already establish,
  deliberately not a generic rearchitecture. Fires only for a
  `SchemaRegistered` notification whose `OriginId` differs from this
  site's own (a peer-synced copy, never the local one, which already
  applied directly and synchronously). **A real bug found only by
  actually running this cross-peer**: had to be checked *ahead of*
  `ADR-038`'s own rollback gate, not after it — that gate treats "this
  deployment has never registered `SchemaRegistered` itself for this
  AppId" as "predates the shape, defer forever," which is exactly true
  and *permanent* for a peer that only ever receives replicated schemas.
  Moved the check earlier, with an early return that skips this
  notification's own otherwise-generic (and, for this reason, otherwise
  perpetually-deferred) entity fold — cosmetic lineage bookkeeping
  nothing in this codebase queries.
- New `SchemaRegistryService.ApplyReplicatedRegistrationAsync` — the
  peer-sync counterpart to `RegisterAsync`: trusts the origin's
  already-validated decision completely (no re-validation, no
  auto-incrementing version — the replicated version is authoritative),
  idempotent on `(AppId, Name, Version)` already present, never
  regresses `IsActive` backward on an out-of-order/late redelivery,
  never re-notifies (would gossip-amplify forever in a full-mesh
  topology), and replicates a `FilterableField`'s own provider-specific
  index DDL too.

**Live-verified cross-provider**, the same bar `ADR-102` set:
`ReplicationCrossProviderHttpTests.cs`'s new
`AnEventTypeRegisteredOnlyAtTheSqliteSiteBecomesQueryableAtTheRealSqlServerSiteWithNoIndependentRegistrationThere`
registers a schema with a real indexed field only at a real SQLite
site, confirms a 404 at a real SQL Server site first, pushes the SQLite
site's event log over real HTTP via `PeerSyncClient`, and polls until
the schema is genuinely queryable at SQL Server — proving both the
`EventTypeDefinition` row and its index actually replicate.

`docs/10-open-questions.md` row 1 deleted; `ADR-033`'s own struck-through
correction note now points at `ADR-103` instead of describing the gap
as unresolved. New `docs/adrs/adr-103-schema-registry-cross-peer-
replication.md`; `docs/07-adrs.md` and `08-build-plan.md`'s "Sharding &
Replication" item both updated. A separate, pre-existing dangling
`docs/10-open-questions.md` row 1 citation was also found and fixed
while here (`08-build-plan.md`'s "Non-Authoritative Capture" item — that
row's own content has been reused and deleted more than once for
unrelated findings over this project's history; the citation had never
been updated).

Regression: full `EventStore.UnitTests` 86/86; full
`EventStore.IntegrationTests` unaffected (the two real failures seen
this pass, `ASchemaInvalidButNotMalformedEventInsideABatchStillPersists
WithAnAdvisorySchemaStatus` and `ProblemDetailsHttpSqliteTests`'s own
`ClassCleanup`, are both confirmed pre-existing, unrelated parallel-
execution flakes — each passes cleanly in isolation, the same class of
flakiness `RbacProjectionWorkerHttpSqliteTests.cs`'s own
`[DoNotParallelize]` comment already documents elsewhere in this repo).

## Closed: `docs/patterns/fault-injection-chaos-engineering.md` written (TODO.md)

A pre-existing dangling reference — `docs/references.md`'s own "Chaos
Engineering" row has pointed at this file since `ADR-063`, but the file
itself was never actually written. New standalone pattern doc, matching
`docs/patterns/README.md`'s own catalog convention: the general
discipline first (principlesofchaos.org), then how this project applies
it — including the real, worth-recording correction that the mechanism
itself changed after adoption (`Polly`+`Simmy` originally, replaced by a
~10-line hand-rolled `FaultInjector` once `Polly` announced a
per-organization Open Source Maintenance Fee). Catalog row updated from
"Catalog only" to "Written," with the "Applied in" text corrected to
name `FaultInjector`, not the removed `Polly`+`Simmy`.

## Closed: `PlaybookRecorder` now emits its own diagram image reference, surviving regeneration (TODO.md)

`scripts/extract-diagrams.mjs` inserts a `![...](....svg)` reference
immediately above a fenced diagram the first time it runs — but
`PlaybookRecorder.WriteMarkdownAsync` regenerates a whole playbook file
from scratch on every E2E test run, silently dropping that insertion on
the next regeneration, since the two pipelines were never coordinated.
Worked around previously by re-running the extraction script after any
E2E run touching a playbook; fixed for real now: `WriteMarkdownAsync`
computes and emits the exact same reference itself (mirroring
`extract-diagrams.mjs`'s own `processFile`/`slugify` path convention —
a playbook's own sequence diagram is always diagram index 1, heading
"Sequence Diagram", so the target is always
`docs/diagrams/playbooks/{domain}/{role}/{task}/01-sequence-diagram.svg`),
computed purely from `_markdownPath`'s own already-known `docs/...`
shape, no filesystem access needed. A later `extract-diagrams.mjs` run
now recognizes the reference via that script's own idempotency check
and never duplicates it — still needed to keep the underlying `.puml`
extraction current when a diagram's own content changes, but no longer
needed just to restore a reference this method now emits correctly on
its own, every time. Verified live: re-ran a real, already-existing
playbook test (`TasksListPlaybookTests`) end to end and confirmed the
regenerated file carries the correct reference.

**A second, real, previously-undiscovered bug found while verifying
this fix, affecting every playbook doc, not just the one touched
first**: `extract-diagrams.mjs`'s own heading-detection regex
(`/^#{1,6}\s+(.+)$/`) never matched a heading at all in a Windows-CRLF
file — every `PlaybookRecorder`-generated playbook doc, since C#'s
`StringBuilder.AppendLine` uses `Environment.NewLine`. A bare `$`
anchor only special-cases a trailing `"\n"` as end-of-line, per the
ECMAScript spec, never a trailing `"\r"` left over from splitting a
CRLF file on `"\n"` alone. `lastHeading` silently stayed at its own
initial `"diagram"` default for every fence in every such file,
regardless of that file's real heading text — every playbook's own
diagram had always been extracted as `01-diagram.svg`, never the
heading-derived name (`01-sequence-diagram.svg`) the feature actually
promises. Not a broken output (still a unique, valid path) but a real,
silent defect, invisible until this pass's own reference computation
gave something to cross-check the script's real behavior against.

Fixed by stripping a trailing `"\r"` for the heading match only (not
from the line itself, so a CRLF file's own line endings are completely
unaffected on write). Closing the loop required updating every
already-affected doc, not just the two touched directly this pass —
left half-fixed, the corrected script would keep silently generating an
orphaned, differently-named `.puml` duplicate next to each doc's still-
stale reference every time anyone re-ran it. Fixed for good: all 12
pre-existing playbook docs (`vitals`/`meridian`) plus `docs/mental-
vision.md` (an unrelated, separately-found doc that had simply never
been run through the extraction pipeline at all until this pass's
repo-wide verification) had their own stale `![diagram](.../01-
diagram.svg)` reference corrected to the real, heading-derived name,
and each superseded `01-diagram.{puml,svg}` pair deleted. Confirmed
fully idempotent afterward: three consecutive `node scripts/extract-
diagrams.mjs` runs produce zero further changes across all 103 `docs/`
files carrying a diagram.

One real, hands-on mistake made and caught during this cleanup, worth
recording: an overly-broad `rm -rf` on a batch of "stale" diagram
directories briefly deleted several genuinely pre-existing, unrelated,
already-committed files alongside the intended ones (including a real,
independently-authored `VitalsAdverseEvent_Playbook_Sequence.svg` with
no connection to this bug at all) — caught immediately via `git status`
showing tracked deletions, restored via `git checkout --` before
proceeding, and the cleanup redone file-by-file instead of by directory.

## Closed: branch/PR structure for this session's authorization-model design work (TODO.md)

Decided: implementation work gets its own dev branch(es) once something
moves from design to code, but the design/planning phase itself — the
authorization-model comparison doc, the OIDC/OAuth2/OpenID-Federation
scope decisions, the scheduling-standards and vCard groundwork, the new
vehicle/equipment-maintenance domain candidate — all stays on one branch,
`dev/architecture-design-explorations` (renamed in place from
`dev/rbac-permission-matrix-and-app-scopes`, which had never diverged
from `main`, so the rename was non-destructive). `docs/comparisons/
authorization-model.md` is the first doc produced there.

## Found and fixed: a stale build-plan status entry; three real gaps recovered

A vision-completeness audit ("does the current build overlook anything
important relative to `README.md`/`docs/naming.md`'s stated vision?")
found `08-build-plan.md`'s own item 40 (`Signing Secret Rotation, Dual
Signature`) status text was stale: it said the ticket-exchange half was
"deliberately descoped... tracked in `TODO.md`," but `ADR-093`'s own
"Built, later pass" notes already record that half being built for real
(`EventStore.DevIdp`'s `ClientSecretRotationStore`) and its `TODO.md`
item already removed — the build-plan text was simply never updated to
match. Fixed both the summary-table row and the item's own detail-section
status line.

The same audit recovered three genuine gaps that had no forward-looking
owner anywhere: `06-solution-structure.md`'s own text claims a
project-list reconciliation task is "tracked in `TODO.md`" when it
wasn't (added now); `ADR-036`'s 2026-08-12 correction note (the original
DID/UCAN token-exchange bridge was never built, a different mechanism
serves `ADR-043`/`044` instead) had no tracker entry deciding whether to
build it for real or additively correct the ADR — added as
`docs/10-open-questions.md` row 1; and the SDK-codegen/package-
distribution story (`ADR-054`/`062`) has a real design but has never
actually been exercised end to end — no package has ever been published
or consumed — added to `TODO.md` alongside a new item for a missing
getting-started/quickstart doc, both raised directly by the audit against
this framework's own stated "plausible real infra product" ambition.

## Added: a five-phase design-phase program (TODO.md)

Per direct request: missing-documents sweep (`docs/patterns/README.md`'s
35+ "Catalog only" rows plus the two doc gaps already found this
session) -> a full ADR review across all 103 ADRs for missing/duplicate/
conflicting decisions -> reviewing and fleshing out the Vitals/Meridian
proving-ground domains -> identifying cross-domain functionality that
belongs at framework level -> writing/updating an architecture/design
compliance guideline -> configuring linting/analysis tooling to enforce
it. Explicitly design-phase only (docs/ADRs, no implementation), and a
standing rule was added per direct correction: a found gap isn't
automatically a mistake to fix silently — it may have been intentionally
descoped, so anything not objectively verifiable surfaces as a question
(an open-questions.md row or a direct question) rather than a unilateral
correction.

## Closed: Phase 0's `docs/patterns/README.md` backlog — 41 pattern docs written

All 41 rows previously marked "Catalog only" now have full standalone
write-ups (general pattern explained and cited, PlantUML diagram, "when
you'd reach for it," a real cost/tradeoff, "how this application uses
it"), matching `fault-injection-chaos-engineering.md`'s established
template. Dispatched as 8 parallel background agents of 5 patterns each
(`.claude/protocols/parallel-batch-dispatch.md`), plus one more
(`PlantUML-native executable flow diagram`) caught and written directly
after noticing the original 8-batch split had missed it — 41 catalog
rows existed, not the 40 originally counted. Every agent verified its
own citations independently (RFC/W3C/OASIS/NIST specs, named academic
papers, or a recognized industry source) rather than recalling from
memory, and none guessed past an ambiguity — several genuine,
previously-unrecorded drifts were found and corrected honestly instead
of being smoothed over:

- `ArchivalService` (`ADR-089`) does **not** actually follow the Humble
  Object shape the old catalog description implied — it has no
  `BackgroundService` wrapper or extracted static core, by design
  (`ADR-056` owns *when* archival runs, not yet built; `ADR-089` only
  owns *how*). The pattern doc's own "Applied in" list was corrected to
  drop it, with the reasoning stated in place rather than silently
  dropped.
- Merkle tree catch-up is decided in `ADR-033` but genuinely not yet
  built (a plain resync-since-last-ack ships today) — the catalog row
  now says so instead of implying it's live.
- `ADR-043`'s delegated grants have no revocation-before-expiry
  mechanism and no Event Log audit trail of issuance — a real,
  pre-existing gap, added as `docs/10-open-questions.md` row 2 rather
  than assumed one way or the other, per this session's own standing
  rule about not guessing whether a found gap was intentional.

`docs/patterns/README.md`'s four catalog tables were consolidated
centrally (not edited by the parallel agents themselves, to avoid
concurrent-write conflicts on one shared file) after a spot-check of a
sample of the actual written files confirmed template compliance and
citation quality. `TODO.md`'s Phase 0 item is updated to reflect this
part done; the getting-started/quickstart doc and the `06-solution-
structure.md` reconciliation remain.

## Closed: the last two Phase 0 items — Phase 0 is now fully done

**`docs/getting-started.md`** (new, linked from `README.md`'s document
index): a from-scratch walkthrough — stand up the local stack via
`dotnet run --project src/EventStore.AppHost`, get a token from
`EventStore.DevIdp`, register an event type, publish against it, confirm
it via Scalar/GraphQL. Every request/response shape shown was checked
directly against the real code (`RegisterEventTypeRequest.cs`,
`PublishEventRequest.cs`, `DevIdpSeeder.cs`'s actual seeded
`operator-client`/`publisher-client` credentials), not written from
impression. Also states the framework's language/platform scope
explicitly, per the item's own ask: server side is .NET only, client
side is TypeScript/Vue only, `ADR-054`'s SDK generation targets only
those two — a deliberate boundary, not an oversight, since nothing in
this design has ever needed a third language.

**`06-solution-structure.md`'s project-list reconciliation**: checked
the file's own project tree against the real `src/` directory listing.
The renames/consolidations the file already documented
(`.PeerSync`→`.Replication`, `.InterchangeAdapters`→`.Interchange`,
`.Fold`/`.Sharding` never built as their own projects) all still held.
Found **twelve real projects with zero mention anywhere in the file**:
`EventStore.Flows` (ADR-101's flow engine — a significant, undocumented
gap given how central it is), `EventStore.WorkerWakeSignal` (ADR-095),
`EventStore.SqlClr.SqlServer` (ADR-098's native predicate evaluator),
`EventStore.Benchmarks` (a manual BenchmarkDotNet-style console app, not
part of any test suite), and all eight `Samples.Vitals*`/
`Samples.Meridian*` projects (the two proving-ground domains' own
seed/simulator/flow-host applications) — added to the tree in their
right places, each description verified against the actual code rather
than guessed. This was objectively verifiable drift (real, currently-
existing projects simply never added), not a judgment call, so fixed
directly per this session's own standing rule.

**Phase 0 (missing-documents sweep) is now fully closed.** Phase 1 (the
full ADR review — missing/duplicate/conflicting across all 103 ADRs) is
next per `TODO.md`'s design-phase program.

## Closed: Phase 1 — full ADR review across all 103 ADRs

Read-only audit first, per this session's own standing rule (a found gap
may be intentional — verify before fixing): 8 parallel background agents
covering ADR-001 through ADR-103, each cross-checking central claims
against real `src/` code, not just prose, and classifying findings into
Tier A (objectively verifiable, direct textual/code evidence) and Tier B
(a genuine judgment call, left for a human). 21 Tier A findings came
back; all fixed, per the additive-history convention (strike through,
never delete, "Corrected"/"Superseded by" markers in place):

- **Unmarked ADR supersessions/extensions** (the most significant class
  found): `ADR-003` was fully superseded by `ADR-037` with zero marker
  anywhere — Decision text still literally described the retired OData
  `400`-at-parse-time behavior. `ADR-093` revises `ADR-040`/`ADR-060`
  with no backward-pointing note in either original. `ADR-096`/`ADR-097`
  extend `ADR-057` the same way. All four now cross-reference correctly.
- **Stale correction notes that were themselves wrong**: `ADR-046`'s own
  "Superseded by `ADR-067`" note claimed `Role`/`UserPermission` became
  core-engine entities — `docs/data/schema-registry.md` had already
  corrected this to the real shape (`EventStore.DevIdp`-owned tables,
  folded via `RbacProjectionWorker`) but the fix was never carried back
  into `ADR-046` itself.
- **Claimed-done propagation that was never done**: `ADR-071`/`072`/`073`
  each claimed a doc update was "not yet done, flagged as remaining
  propagation work" when it had actually been completed —
  `docs/data/schema-registry.md`'s `PCI-SAD` value, `06-solution-
  structure.md`'s MLLP-listener detail, and `08-build-plan.md`'s
  accessibility exit criterion all already existed. `ADR-102`'s own
  Consequences claimed a note was added to `docs/comparisons/peer-sync-
  topology.md` — verified it wasn't; added now.
- **A stale claim that had propagated into shipped source code, not just
  docs**: `ADR-078`'s Decision text still listed `UpcastMaterializer` as
  having its own leader-election lease, contradicted by its own later
  Consequences note — and the identical stale enumeration had been
  copied verbatim into `LeaderLease.cs`'s own header comment. The ADR is
  fixed; the code comment is deferred to `TODO.md` since this session is
  design-phase-only, no `src/` changes.
- **Real, no-longer-true claims caught by comparing against git history,
  not just prose**: `ADR-091` and `08-build-plan.md` item 39 both still
  claimed `.github/workflows/ci.yml`/`dependabot.yml` existed and were
  Done — `git log` confirmed both were deliberately deleted 2026-08-27
  per direct request (logged in `docs/changes/2026-08-28.md`), but
  neither doc was ever updated afterward. Corrected to reflect current
  reality; whether to rebuild the workflow is left as a genuine open
  question (`docs/10-open-questions.md` row 3), not decided here.
- **Smaller citation/attribution drift**: `ADR-001`'s "queued cross-
  provider-peer-sync ADR" placeholder backfilled to `ADR-033` now that
  it exists; `ADR-005` corrected to name the real enforcement site
  (`PublishService`, not a `ParentLinkService` that was never split out);
  `ADR-008`'s Decision section struck through to match the `ADR-050`
  field-list generalization its own Consequences already noted;
  `ADR-021`'s citation corrected from index-only `02-data-model.md` to
  `docs/data/entity-store.md`; `ADR-024` corrected from a misattributed
  `ADR-029` to the real `ADR-037` GraphQL citation (`08-build-plan.md`
  had already caught and fixed the identical mistake in its own text);
  `ADR-028` given a confirming note that its predicted OData-to-CEL
  symmetry actually held; `ADR-036`'s stale "tracked as an open
  question" text updated to point at `ADR-079`, which resolved it;
  `ADR-015`'s "runs as its own deployable" claim corrected per `ADR-026`'s
  own already-existing finding (no `Program.cs` — the real deployable is
  `Samples.Orders.Projections`); `ADR-068`'s two file paths corrected
  (nested under `client-web/packages/reference-app/`, not top-level);
  `ADR-085` given the same "actually done" correction as 071/072/073,
  for `docs/libraries/dotnet/benchmarkdotnet.md`. `CLAUDE.md`'s own
  "Decision index" corrected from a stale "All 95 ADRs" to the real 103.
  `docs/patterns/test-pyramid.md` (written earlier this session) fixed
  for self-contradicting itself on `Moq` usage — confirmed zero actual
  usage anywhere in the repo, consistent with the rest of its own text.

**Three Tier B findings were surfaced but deliberately not resolved**,
per this session's standing rule against guessing at genuinely ambiguous
gaps:
- Whether `ADR-010` needs an explicit cross-reference from `ADR-037`
  (its `mode`/`fromSequenceNumber` mechanism was re-expressed as GraphQL
  subscription arguments, but `ADR-037` never mentions `ADR-010` by
  name) — unclear if this is a real gap or acceptable as-is.
- Whether `ADR-011`'s lightweight, un-struck-through parenthetical
  correction style ("`202`, same status envelope — `ADR-023` superseded
  this row's original `201` framing") is an accepted lighter-weight
  variant of the strikethrough convention, or should also be struck
  through per the protocol's letter.
- Whether `docs/comparisons/distributed-correctness-testing.md` should
  get the same kind of "implementation note" correction `docs/patterns/
  fault-injection-chaos-engineering.md` already received — it still
  reads as if Polly/Simmy were never adopted, when they actually went
  through a full adopt-then-reject cycle for a documented reason
  (`Polly`'s Open Source Maintenance Fee).

No duplicate ADRs and no unacknowledged conflicting ADRs were found
anywhere across the full 103-ADR corpus — every apparent overlap checked
resolved to either a correctly-marked, intentional revision, or one of
the findings above (a marker that should exist but didn't).

## Closed: all 3 Phase 1 Tier B items, resolved directly by the user

- **`ADR-010` ↔ `ADR-037` cross-reference: yes.** Added reciprocally —
  `ADR-010`'s own Consequences now note it was re-expressed (not
  replaced) by `ADR-037`'s GraphQL Subscription arguments, and
  `ADR-037`'s Consequences now note the `mode`/`fromSequenceNumber`
  semantics it carries are `ADR-010`'s, unchanged in substance.
- **`ADR-011`'s `202`/`201` correction: literal strikethrough.** Replaced
  the lightweight parenthetical ("`202`, same status envelope — `ADR-023`
  superseded this row's original `201` framing") with `~~201~~` struck
  through next to `202`, matching every other correction in this repo.
- **`docs/comparisons/distributed-correctness-testing.md`: corrected.**
  Added the same implementation note `docs/patterns/fault-injection-
  chaos-engineering.md` already carries — `Polly`+`Simmy` was adopted for
  real, then removed once `Polly` announced its Open Source Maintenance
  Fee, replaced by a hand-rolled `FaultInjector`. Updated Option B's own
  "Fits this design's stack?" row and the staged-decision-path's step 2
  to match, rather than reading as if Polly were still just a considered,
  unadopted candidate.

**Phase 1 is now fully closed.** Phase 2 (review and flesh out the
Vitals/Meridian proving-ground domains) is next.

## Closed: Phase 2 — proving-ground domain review

Two parallel read-only audits (one per domain), each checking every
feature doc against `.claude/templates/feature-doc-template.md`'s shape
(context, PlantUML sequence diagram, ER diagram, Salt mockup or explicit
N/A, embedded Gherkin) and cross-checking claims against the real
`src/Samples.Vitals`/`Samples.Meridian` code, not just prose. Both
domains came back structurally excellent — no missing sections, no thin
Gherkin coverage, no orphaned feature docs, substantive Special Concerns
and Glossaries in both READMEs.

Four real, code-confirmed divergences found between a feature doc's
illustrative narrative and what was actually built:

- Vitals' `patient-enrollment-and-informed-consent.md` describes a
  standalone `ConsentApproval` event type and `ConsentApprovalResolver`
  class; the real sample reuses the core engine's own generic
  `authoritydecision` reactor instead, since `EventStore.Router`'s fold
  primitives are internal and unreachable from a separate sample
  assembly.
- Vitals' `intraoperative-monitoring-and-alert-response.md`'s Background
  and ER diagram said `IonmAlertRaised` is `ChangeKind: "Full"`; the real
  registration is `"Partial"` — a genuine, found-by-running-it
  correctness fix (a `"Full"` fold would silently erase the `AckedBy`
  field `IonmAlertAcknowledged`'s own `Partial` fold contributes,
  depending on fold order).
- Meridian's `customer-onboarding-and-identity-verification.md`
  describes a Router-initiated, asynchronous OAuth Token Exchange call
  upgrading `AuthorityStatus` to `pending_review`; no such logic exists
  anywhere in `EventStore.Router`/`EventStore.Inbox` — the real
  mechanism is entirely caller-initiated, so self-attestation goes
  straight to `unattested` instead, resolved by the same
  `authorityDecision` reactor.
- Meridian's `relying-party-verification-request.md` describes
  `accessGrant`/`accessGrantRevoked` published-as-events and a generic
  `QUERY { entity(id) {...} }` field; neither exists — delegation is a
  client-signed `UcanDelegation` token, and the only real claims-gated
  entity-scoped read is `revealField`.

All four divergences were **already** documented centrally, in
`docs/domains/README.md`'s own numbered "six run-and-found divergences"
list — this audit's actual finding was that none of the four were
cross-referenced *from inside the specific feature doc a reader would
actually open*. Added an inline pointer to each, rather than rewriting
the illustrative narratives themselves (which the domains/README.md text
explicitly frames as a deliberate choice, not an error).

Also fixed: `docs/domains/digital-identity-kyc/README.md`'s
`## Applicable ADRs` section was missing five ADRs demonstrably
load-bearing throughout its own feature docs — `ADR-008` (the base
`RequiredClaims` mechanism every feature doc's event types actually use),
`ADR-066` (step-up auth, load-bearing for the entire SAR-filing flow),
`ADR-079` (the sanctions-screening seam this domain's own use case
motivated), `ADR-096` (searchable encryption, present in all 4 feature
docs), and `ADR-101` (the flow engine backing the README's own workflow
diagrams). Added all five to the correct fit tier.

**Phase 2 is now fully closed.** Phase 3 (identify cross-domain
functionality that belongs at framework level) is next.

## Closed: Phase 3 — cross-domain functionality review

A read-only research pass looked for functionality built inside Vitals/
Meridian sample code that's actually generic and not currently
acknowledged as such. Two candidates checked and correctly ruled out:
`AuthorityQueue.vue` is already the fully generic queue component
(`VitalsPiQueue.vue`/`MeridianAnalystQueue.vue` are genuinely thin,
domain-fact-only wrappers around it); the "secondary opinion" delegated-
access terminology used identically by both domains is already a
promoted, shared mechanism (`docs/patterns/delegated-capped-time-boxed-
access-grants.md`), not a hidden duplicate.

Two real candidates found:

- **A missing pattern doc, written this session**:
  `docs/patterns/interactions/claim-gated-step-up-signoff.md` documents
  the "capture → claim-gated decision → step-up sign-off → authoritative
  fold" composition (`ADR-035`/`042`/`008`/`050`/`066`) both Vitals'
  adverse-event review and Meridian's SAR-filing/analyst review
  independently build the identical way — both feature docs explicitly
  say they introduce zero new framework mechanism, but no single doc
  previously narrated the full composition end to end (the existing
  `gated-authoritative-publish.md` interaction doc stops short of the
  claim-gate/step-up half). Added to `docs/patterns/README.md`'s
  Interactions table.
- **A framework-level code promotion, tracked but not built (design-
  phase-only)**: `VitalsSharedTypes.cs`'s and `MeridianSharedTypes.cs`'s
  `EnsureAuthorityDecisionRegisteredAsync` registration helpers are
  byte-for-byte identical in schema, near-identical in body — and this
  duplication already produced a real, observed cost: Vitals' copy
  supports `RequiredSignature`, Meridian's doesn't, so Meridian's own
  SAR-filing workflow had to hand-register a wholly separate event type
  instead of extending the shared one the way Vitals did. Added to
  `TODO.md` as a proposal for a small `EventStore.SchemaRegistry` helper
  — needs both a decision and a code change, neither done this session.

**Phase 3 is now fully closed.** Phase 4 (architecture/design compliance
guideline) is next.

## Closed: Phase 4 — new architecture & design compliance guideline

Written as a new standalone doc, per direct request:
`docs/architecture-design-guidelines.md`, linked from `README.md`'s
document index and pointed at from `CLAUDE.md`'s own layout list.
Consolidates the cross-cutting rules previously scattered across
`CLAUDE.md`'s "Conventions established so far" section and all six
`.claude/protocols/*.md` files into one compliance/consistency-checking
reference: which doc kind to use for a given piece of work; research and
citation discipline; ADR additive-history discipline; standards-adoption
discipline; the no-`!include`-in-PlantUML rule; "run the real thing"
verification discipline; code-level conventions (constructor-injection-
only DI, Humble Object, Known Outcomes Are Not Exceptions, no
AutoMapper/Newtonsoft, no `--` in `.csproj` XML comments, test trait
tagging); tracking-file discipline (one authoritative home per kind of
fact, delete-on-completion rather than narrate-in-place); the parallel-
batch-dispatch approach for large multi-file work; the "a found gap
isn't automatically a defect" standing rule from this session; and
build-plan tracking discipline.

Each section is marked 🔧 (mechanically checkable, a real Phase 5
lint/analyzer candidate) or 👁️ (needs human/AI judgment, a review
checklist item, not a lint rule) — a closing summary section names the
specific rules genuinely worth Phase 5 tooling investment (no
`!include`, no AutoMapper/Newtonsoft references, no `--` in `.csproj`
comments, a `dotnet test --logger` check, a constructor-injection
analyzer) and explicitly scopes Phase 5 away from trying to force
everything into a linter that can't actually check it. `TODO.md`'s
Phase 5 item updated to point at this scoping directly.

**Phase 4 is now fully closed.** Phase 5 (linting/static-analysis
tooling) is next.
