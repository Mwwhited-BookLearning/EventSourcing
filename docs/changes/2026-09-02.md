# 2026-09-02

## Closed: `FollowClient` returns discriminated results instead of throwing for known outcomes (TODO.md)

`docs/patterns/known-outcomes-are-not-exceptions.md`'s own named follow-on,
finally done: `FollowClient.TailAsync` (renamed `ConnectAsync`) and
`GetChangeKindAsync` used to call `EnsureSuccessStatusCode()`, converting a
well-understood, routine outcome (an event type with no registered schema
yet for this AppId; a `RequiredClaims` gate the caller's own token doesn't
satisfy) into a thrown `HttpRequestException` the moment it crossed the
HTTP boundary — exactly the class of bug
`docs/bugs/framework/service/rbac-fold-404-logged-as-error-forever.md`
found and only partially fixed (one caller's own `catch` filter, not the
root cause).

New `FollowConnectResult`/`ChangeKindResult` (`EventStore.Projections.Host`)
mirror the server's own `FollowResult` (`Connected`/`UnregisteredEventType`/
`Forbidden`/`ValidationFailed`) exactly, one level up the HTTP boundary.
`ConnectAsync` is now a plain `Task<FollowConnectResult>` rather than an
`async IAsyncEnumerable` — an iterator method can't also return a
non-enumerable discriminated result for its own connect-time outcome, so
the streaming read (`ReadEventsAsync`) is a private iterator only ever
invoked once a `Connected` outcome is already known, wrapping the still-open
`HttpResponseMessage` and disposing it once the stream ends.

Both real callers — `ProjectionHost<TReadModel>.CatchUpOnceAsync` and
`EventStore.DevIdp.RbacProjectionWorker.CatchUpOnceAsync` (`Samples.Orders
.Projections` and every Vitals/Meridian flow worker go through the shared
`ProjectionHost<TReadModel>`, not a third independent call site) — now
switch on the discriminated result and return a new
`(CatchUpOutcome, EventsConsumed)` tuple instead of a bare `int`.
`UnregisteredEventType`/`Forbidden` are logged at `Warning` (moved from
`RbacProjectionWorker`'s own StatusCode-filtered `catch` into
`CatchUpOnceAsync` itself, where the discriminated result is already being
inspected) and return `0` immediately, no exception anywhere in the path;
`ValidationFailed` (a genuinely unexpected outcome for these clients' own
always-valid, hardcoded request shape) still throws, since that's a real
bug, not a routine branch. `ProjectionHost` couldn't reuse
`RbacProjectionWorker`'s own `DuplexInstrumentation.WorkerTailReconnects`
OTel counter for its own routine-case logging — `EventStore.Projections
.Host`'s own project file deliberately has no reference to
`EventStore.Domain` at all ("its only dependency on the write side is an
HTTP client," enforced by the reference graph itself), so it keeps a plain
`ILogger` warning instead.

`tests/EventStore.IntegrationTests/RbacProjectionWorkerHttpSqliteTests.cs`'s
own `CatchUpOnceAsyncThrowsAnHttpRequestExceptionWithNotFoundStatusForA
GenuinelyUnregisteredEventType` test — which explicitly asserted the OLD
throwing behavior — is now
`CatchUpOnceAsyncReturnsUnregisteredEventTypeInsteadOfThrowingForA
GenuinelyUnregisteredEventType`, asserting the new graceful outcome
instead. Full regression: `EventStore.UnitTests` 86/86,
`EventStore.IntegrationTests` 246/247 (the one failure,
`ASchemaInvalidButNotMalformedEventInsideABatchStillPersistsWithAnAdvisory
SchemaStatus`, is unrelated to this change and passes cleanly in
isolation — a pre-existing parallel-execution flake, the same class this
repo's own `[DoNotParallelize]` convention already documents elsewhere).

## Corrected and closed (in part): App.vue's "Dispatch a command" demo panel (TODO.md)

Yesterday's TODO.md entry for this gap made a claim that turned out to be
wrong on inspection, found before any code was written against it: "no
client-side mechanism exists today to recover a schema's original casing
from a registered `EventTypeDefinition`." One already exists —
`useEventComposer.getEventTypeDetail` (the Compose tab's own schema
introspection, `composer-client`'s `registry:admin` scope) already fetches
a schema's real, PascalCase-declared field names via `eventType(...)
.jsonSchema`, never the GraphQL-camelCased names `App.vue`'s cached read
side data uses. `.claude/protocols/verify-before-citing.md`'s own standing
rule paid for itself here — corrected before it propagated into a bigger,
unnecessary build (a new registry endpoint that already existed in a
different form).

**Fixed for real**: new `resolveEventTypeFieldCasing` (`useEventComposer
.ts`) reuses that same introspection purely to build a
`lowercase -> real-declared-name` map; `App.vue.submitAmountCommand` now
re-cases its merged cached-entity-fields patch through this map before
dispatching, satisfying any real schema's own case-sensitive `required`
check regardless of domain. Fetched once per instance (`fieldCasing` ref),
not on every dispatch. Falls back to an empty map (patch sent un-recased,
the prior behavior) rather than throwing if introspection itself fails —
never blocks a dispatch outright.

**A second, real, previously-undiscovered gap found and closed along the
way, unrelated to casing**: `types.ts`'s own `OutboxEntryStatus` already
declared a `'Failed'` terminal state, and `exportOutboxBundle`'s own
comment already assumed it existed ("Delivered/Failed entries are
excluded"), but nothing anywhere had ever actually set it —
`useOutboxStore.flush` only ever left a rejected entry `Pending`,
retried forever, indistinguishable from an ordinary transient failure,
with no visible signal anything was permanently wrong. New
`PublishResult.permanentFailure` (`publishClient.ts`, also threaded
through `useEventComposer.publish`'s own return shape) is `true` for a
400 (validation failure) or 403 (Forbidden) — outcomes no amount of
retrying will ever change — and `false`/absent for everything else
(a dropped connection, a real 5xx; a thrown exception is always treated
as transient too, since only a real HTTP response the server actually
returned can be a permanent rejection). `flush` now moves a
`permanentFailure` entry to `'Failed'` instead of incrementing `attempts`
forever; a new `failedFor` store getter and an `App.vue` header
`data-testid="failed-count"` element make a permanent rejection visible
instead of silently vanishing from the queued-command count.

**What's still genuinely open, left as a real TODO.md item, not decided
here**: no DevIdp-seeded HTTP client anywhere holds the specific
`RequiredClaims` a real Vitals/Meridian business event type demands (e.g.
`PatientScreened`'s `patient:enroll`) — those events have only ever been
created in-process by the seeders, bypassing the HTTP auth layer's claim
check entirely. This is a real security-policy fork (grant a narrow demo
claim vs. retire the generic panel), not a technical gap this pass
resolves — but a genuine rejection now fails loudly and visibly instead
of retrying forever in silence, which was the actual damage.

Tests: `outbox.spec.ts` gained 3 scenarios (permanent failure moves to
`Failed` and is never retried; an ordinary transient failure still
retries; a thrown exception is always transient). `useEventComposer.spec
.ts` gained 3 scenarios for `resolveEventTypeFieldCasing` (real casing
recovered; empty map for an unknown event type; empty map, never thrown,
when introspection itself fails) plus 2 for `permanentFailure`
propagating through `composer.publish` (403 flagged, 503 not).
`OfflineOutboxSyncPlaybookTests.cs` re-run live end to end after both
fixes, still passing — `docs/playbooks/core/user/go-offline-and-
resync.md` regenerated with fresh screenshots. Full `mvvm-client` suite:
145/145 (27 files).
