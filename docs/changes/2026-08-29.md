# 2026-08-29

## Investigated Postgres 40001 noise under real concurrent load — root cause found, two fix attempts tried and reverted, not yet safely fixed

Direct report: "I'm getting a ton of errors running the application...
`Npgsql.PostgresException: '40001: could not serialize access due to
read/write dependencies among transactions'`," then "it should not
error this much. it should be much more graceful for retrys and so on."
Confirmed real and routine (not just under heavy load) by running a
live `AppHost` and reading the Postgres container's own logs directly.
Root cause: `EventAppender`/`AccessLogAppender`'s Serializable-isolation
"read tail, insert, compute chain" critical section is exactly the
shape Postgres's SSI flags whenever two appends overlap — already
correctly retried by the existing `EnableRetryOnFailure` config (a
noise problem, not a correctness bug).

Tried a `pg_advisory_xact_lock` (acquired after `BeginTransactionAsync`)
to convert the race into a queue — a dedicated new regression test
(30 concurrent publishers, counting EF's own retry diagnostic event)
proved it didn't actually reduce conflicts at all (~97 retries still
observed). Tried a session-scoped `pg_advisory_lock`/`unlock` pair
acquired *before* the transaction instead — the same test hung
indefinitely, a real deadlock, before it could even be measured. Both
attempts reverted rather than ship an ineffective or unsafe change;
`EventAppender.cs`/`AccessLogAppender.cs` are back to their original,
proven-correct state. Full findings (including precisely why each
attempt failed, and the actual fix this points to — dropping to
`ReadCommitted` + `SELECT ... FOR UPDATE`, not attempted yet) are
tracked as a `TODO.md` item rather than a `docs/bugs/` report, since
this project's own bug-report protocol is for fixed defects and this
one isn't fixed yet — the diagnostic value is preserved there instead
of being lost.

## Established the bug-report tracking protocol

Direct request: `docs/bugs/{scope}/{tier}/{summary-title}.md` (`{scope}`
`framework`/`proving-ground`, `{tier}` `ui`/`client`/`service`/
`database`/`test`/...) as the durable, indexed record for a genuine,
**fixed** defect found by actually running the real thing — distinct
from this changelog's own day-of narrative, which points to the bug
file instead of repeating it in full once one exists. Also requires a
regression test tagged `[TestProperty("BugReport", "<path>")]`, proven
red-then-green, referenced from the bug report's own "Resolution"
section. New protocol:
[`.claude/protocols/bug-report-tracking.md`](../../.claude/protocols/bug-report-tracking.md);
indexed in `CLAUDE.md`'s "Reusable process material" and "Layout"
sections. No bug has actually been filed under it yet — the Postgres
investigation above was the first candidate, but didn't reach a safe
fix this pass.

## Adopted `--logger "console;verbosity=detailed"` for every `dotnet test` invocation

Direct request: run tests so underlying exceptions/diagnostic output
aren't silently eaten by a terse default run. Verified: a plain
`dotnet test` run of `RetryOnFailurePostgresTests` shows only
pass/fail; the same run with `--logger "console;verbosity=detailed"`
also prints each test's own "Standard Output Messages" (Testcontainers
container lifecycle in this case) that were otherwise fully suppressed.
Added to `CLAUDE.md`'s standing conventions. This is exactly what
surfaced the real exception detail (`Npgsql.PostgresException 40001`)
behind the misleading-looking retry counts during the Postgres
investigation above.

## Corrected a wrong-actor error in the Adverse Event playbook's sequence diagram

Direct request to review `docs/playbooks/vitals/site-coordinator/
capture-and-review-adverse-event.md`'s PlantUML. Checked it against the
actual authority for that flow, `docs/domains/clinical-trials-device-
telemetry/features/adverse-event-capture-and-review.md`'s own
`AeReview_Sequence` diagram (per this repo's verify-before-citing rule)
— found the playbook's diagram had the site coordinator delegating the
`ADR-043` secondary-opinion grant to the colleague; the feature doc is
unambiguous that only the **PI** can (`pi -> inbox: POST /publish/
accessGrant`), matching `ADR-043`'s own design (a PI-held claim being
capped and delegated, not one the coordinator holds at all). Fixed at
the source — `tests/EventStore.E2ETests/
VitalsWorkflowBAdverseEventPlaybookTests.cs`'s own embedded
`sequenceDiagram` string, per the playbook file's own "don't hand-edit,
edit the owning test" rule — will regenerate correctly the next time
this playbook runs (this file is also being touched by an in-progress
Naive UI/Vue Router pass for navigation-selector updates; the diagram
fix rides along with whatever commit lands that work). Not filed under
`docs/bugs/`: a documentation-accuracy correction with no runtime code
defect, which the new bug-report protocol explicitly excludes. The
other 11 playbooks' diagrams weren't swept for the same class of error
in this pass — scoped to the one file asked about.
