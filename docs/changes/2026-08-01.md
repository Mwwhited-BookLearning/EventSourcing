# Change log — 2026-08-01

Continues the "work the TODO list" pass started 2026-07-31 (see that
file for the session's full run-up); the date rolled over mid-session,
this file picks up where it left off.

## Phase 1 (GraphQL/API-contract cluster), second item done: `03-api-contracts.md`

Full rewrite (~450+ lines) to the GraphQL/OpenAPI-split contract shape
`ADR-037` committed to: Follow as a GraphQL Subscription over
`graphql-sse` (verified HotChocolate supports this protocol natively
since v13 before writing it down — the SSE *transport* survives
unchanged, only the query-document syntax moved from OData to GraphQL),
Lineage and registry listing as GraphQL Query fields with cursor
pagination replacing `$top`/`$skip`, `RequiredPublishClaim`/
`RequiredReadClaim` updated to `RequiredClaims`/`Direction` throughout.
Also added sections this contract never had before: RFC 9470 step-up
challenges for signature-required event types, the `ADR-040` ticket-
exchange three-hop flow, `ADR-072`'s bulk-ingestion endpoint (explicitly
*not* a new endpoint for `IInterchangeFormatAdapter` itself — that stays
an internal transform feeding the ordinary publish path), `revealField`,
lineage export/bitemporal playback, and webhook subscription
registration. The old OData-era Follow/Lineage sections are gone, not
preserved historically here — `04-odata-filter-pushdown.md` already
carries that history for the shared pushdown mechanism; this file's job
was always the contract shape, not the mechanism.

## Phase 1 (GraphQL/API-contract cluster), third item done: `06-solution-structure.md`

Brought the DI-wiring code sketches current, closing out the file's own
long-standing propagation-note banner:

- **Project layout gained the `ADR-054`+ projects the banner had flagged
  as missing**: `EventStore.Gateway` (YARP, `ADR-049` — itself a real gap
  found beyond the tracked list: no gateway project existed in this
  file at all, despite `ADR-049` requiring one; folded in here since it's
  also where `ADR-058`'s per-tenant rate limiting is enforced),
  `EventStore.Webhooks` (outbound dispatcher, `ADR-060`), a note on
  `EventStore.SpecGeneration` clarifying SDK generation (`ADR-054`) is a
  consumer-build-time step with no project of its own in this solution,
  and `EventStore.Client.DeviceInput` plus `client-web` annotations for
  `ADR-070`'s browser-API/native-bridge device-input adapters.
- **`FollowEndpoint`/`LineageEndpoint` renamed throughout** to the GraphQL
  Follow Subscription resolver / Lineage Query resolver they actually are
  now (`ADR-037`) — `PublishEndpoint` kept, since `ADR-037` never touched
  the publish side.
- **The per-surface `MapMethods(..., ["QUERY"], ...)` routing sketch
  replaced** with an accurate description of `EventStore.GraphQL`'s single
  mapped endpoint, plus the real integration detail (HotChocolate defaults
  to `POST`; serving over `QUERY` needs a small custom endpoint mapping,
  not a config flag) cited from `docs/libraries/dotnet/hotchocolate.md`.
- **`RequiredPublishClaim`/`RequiredReadClaim` updated to `RequiredClaims`**
  (`ADR-050`'s list/`OR`-match shape) everywhere in this file, including
  the illustrative `HasRequiredClaim` helper, which gained a companion
  `HasAnyRequiredClaim` matching the actual list shape rather than leaving
  a single-string signature next to list-shaped call sites.
- **`AsyncApiDocumentBuilder`/`asyncapi.json` marked inline as superseded**
  (`ADR-037` — Follow's spec is GraphQL SDL, served by HotChocolate itself,
  not a second hand-built document) rather than silently left presented as
  current; the code is preserved as a description of the pre-`ADR-037`
  mechanism, matching this project's additive-history convention.
- The file's top banner rewritten to describe what's now current vs. what
  (explicit-composition-era wiring past `ADR-041`, not yet re-verified
  line-by-line) still carries the older "concept accurate, exact wiring
  unverified" caveat.

Removed from `TODO.md`'s Phase 1. One item left in that phase: the
`docs/features/*.md` Gherkin rewrite for `ADR-054`–`074` coverage.
