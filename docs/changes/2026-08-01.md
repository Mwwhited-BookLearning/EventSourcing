# Change log — 2026-08-01

Continues the "work the TODO list" pass started 2026-07-31 (see that
file for the session's full run-up); the date rolled over mid-session,
this file picks up where it left off.

## Phase 1 (GraphQL/API-contract cluster), second item done: `03-api-contracts.md`

Full rewrite (~450+ lines) to the GraphQL/OpenAPI-split contract shape
`ADR-037` committed to: Follow as a GraphQL Subscription over
`graphql-sse` (verified HotChocolate supports this protocol natively
since v13 before writing it down — the SSE *transport* survives
unchanged, only the query-document syntax moved from OData to GraphQL),
Lineage and registry listing as GraphQL Query fields with cursor
pagination replacing `$top`/`$skip`, `RequiredPublishClaim`/
`RequiredReadClaim` updated to `RequiredClaims`/`Direction` throughout.
Also added sections this contract never had before: RFC 9470 step-up
challenges for signature-required event types, the `ADR-040` ticket-
exchange three-hop flow, `ADR-072`'s bulk-ingestion endpoint (explicitly
*not* a new endpoint for `IInterchangeFormatAdapter` itself — that stays
an internal transform feeding the ordinary publish path), `revealField`,
lineage export/bitemporal playback, and webhook subscription
registration. The old OData-era Follow/Lineage sections are gone, not
preserved historically here — `04-odata-filter-pushdown.md` already
carries that history for the shared pushdown mechanism; this file's job
was always the contract shape, not the mechanism.

## Phase 1 (GraphQL/API-contract cluster), third item done: `06-solution-structure.md`

Brought the DI-wiring code sketches current, closing out the file's own
long-standing propagation-note banner:

- **Project layout gained the `ADR-054`+ projects the banner had flagged
  as missing**: `EventStore.Gateway` (YARP, `ADR-049` — itself a real gap
  found beyond the tracked list: no gateway project existed in this
  file at all, despite `ADR-049` requiring one; folded in here since it's
  also where `ADR-058`'s per-tenant rate limiting is enforced),
  `EventStore.Webhooks` (outbound dispatcher, `ADR-060`), a note on
  `EventStore.SpecGeneration` clarifying SDK generation (`ADR-054`) is a
  consumer-build-time step with no project of its own in this solution,
  and `EventStore.Client.DeviceInput` plus `client-web` annotations for
  `ADR-070`'s browser-API/native-bridge device-input adapters.
- **`FollowEndpoint`/`LineageEndpoint` renamed throughout** to the GraphQL
  Follow Subscription resolver / Lineage Query resolver they actually are
  now (`ADR-037`) — `PublishEndpoint` kept, since `ADR-037` never touched
  the publish side.
- **The per-surface `MapMethods(..., ["QUERY"], ...)` routing sketch
  replaced** with an accurate description of `EventStore.GraphQL`'s single
  mapped endpoint, plus the real integration detail (HotChocolate defaults
  to `POST`; serving over `QUERY` needs a small custom endpoint mapping,
  not a config flag) cited from `docs/libraries/dotnet/hotchocolate.md`.
- **`RequiredPublishClaim`/`RequiredReadClaim` updated to `RequiredClaims`**
  (`ADR-050`'s list/`OR`-match shape) everywhere in this file, including
  the illustrative `HasRequiredClaim` helper, which gained a companion
  `HasAnyRequiredClaim` matching the actual list shape rather than leaving
  a single-string signature next to list-shaped call sites.
- **`AsyncApiDocumentBuilder`/`asyncapi.json` marked inline as superseded**
  (`ADR-037` — Follow's spec is GraphQL SDL, served by HotChocolate itself,
  not a second hand-built document) rather than silently left presented as
  current; the code is preserved as a description of the pre-`ADR-037`
  mechanism, matching this project's additive-history convention.
- The file's top banner rewritten to describe what's now current vs. what
  (explicit-composition-era wiring past `ADR-041`, not yet re-verified
  line-by-line) still carries the older "concept accurate, exact wiring
  unverified" caveat.

Removed from `TODO.md`'s Phase 1.

## Phase 1 (GraphQL/API-contract cluster), fourth item done: `docs/features/*.md` stale-scenario sweep

Split the original combined TODO item in two before starting, rather than
risk the harder "zero coverage for `ADR-054`–`074`" half getting silently
dropped once the easier half was done: this section covers only "fix the
already-stale scenarios," now complete. The `054`–`074` coverage gap, and
a newly-found, much wider `RequiredPublishClaim`/`RequiredReadClaim`
staleness spreading well past `docs/features/*.md`, are both still open —
see `TODO.md`.

Dispatched as 4 parallel background agents
(`.claude/protocols/parallel-batch-dispatch.md`), disjoint file ownership,
covering all 11 files found stale (9 already banner'd, plus 2 found stale
with no banner at all: `event-security.md` and `spec-generation.md`).
Every file now reflects `202`+`SchemaStatus` (`ADR-023`), GraphQL syntax
for Follow/Lineage/registry listing (`ADR-037`), and `RequiredClaims`
(`ADR-050`) in place of the pre-ADR mechanics, with stale banners removed
or rewritten to state only what's genuinely still open. Two agents caught
and **corrected wrong assumptions in my own dispatch instructions** rather
than blindly applying them — a real save, not just compliance:

- **Registration-time `400`s are not part of `ADR-023`'s persist-
  everything posture and were never meant to become `202`.** `ADR-023`'s
  persist-everything applies to *publish*, not to schema *registration*
  (`PUT /registry/{event-type}`) — verified against `ADR-013`'s Problem
  Details table, whose `masking-invalid`/`change-kind-required`
  registration-time rows are never struck through, unlike the two
  publish-time rows `ADR-023` actually superseded. `masking.md` and
  `cqrs-projections.md` both had a prior-session banner asserting the
  wrong version of this; both banners rewritten to state the corrected
  fact instead of the original (wrong) claim.
- **`event-chains.md`'s own banner was also wrong**: it claimed a Strict-
  mode unresolved-parent publish now persists as `SchemaStatus: invalid`
  under `ADR-023` — checked against `ADR-013`'s table (that row isn't
  struck through) and `ADR-023`'s own decision text (unresolved
  `parentEventIds` isn't in its persisted-not-rejected list) and found
  false. That branch correctly stays a blocking `400`.

**A real error of my own, caught by spot-checking the batch results
against each other**: while rewriting `06-solution-structure.md`'s "Event
upcasting" section earlier this pass, I wrote that `upcastFromPrevious`
"keeps using OData's `compute()` grammar unchanged," reasoning it was an
unrelated schema-mapping DSL untouched by `ADR-037`'s query-surface move.
This was wrong — `ADR-037` explicitly moved upcast mapping *off* OData
`compute()` specifically because that choice's whole justification was
reusing the OData parser `$filter` already needed, and `ADR-053`
established the pluggable `IUpcastExpressionEvaluator` seam (CEL default,
JSONata alternative, Jint for the complex-case escape hatch) as the
replacement. Caught because batch A's independent rewrite of
`schema-registry.md` stated the correct version, contradicting what I'd
just written — cross-checked against `ADR-037`/`ADR-053` directly, and
`06-solution-structure.md` fixed to match (the `ComputeEvaluator`/
`Microsoft.OData.UriParser` code sample replaced with `IUpcastExpressionEvaluator`).
**Lesson for next time, worth repeating alongside the existing one**: a
rewrite pass earlier in the *same* session is not yet-verified ground
truth just because it's more recent than what it's citing against.

Also fixed directly (small, mechanical, done inline rather than via
agent): `docs/features/streaming-channels.md` and `multi-tenancy.md`'s ER
diagrams still had the old `RequiredPublishClaim`/`RequiredReadClaim`
fields, missed by the earlier session's data-model sweep; `auth.md`'s
residual stale `201` (should be `202`) and its browser-SSE section (still
showing the pre-`ADR-037` bare `QUERY`+`$filter` shape after batch D
correctly deferred that specific fix to `follow-subscribe.md`'s own
agent, which doesn't touch `auth.md`) — closed the gap between the two
files' now-inconsistent Follow framing.

`TODO.md`'s Phase 1 first item removed. Two items remain, now more
precisely scoped than when first found: the `ADR-054`–`074` feature-doc
coverage gap, and the wider `RequiredClaims`-naming sweep (now also
naming `docs/05-schema-registry-and-spec-generation.md`'s own un-rewritten
body and `ADR-013`'s error table as additional instances). A third,
newly-found item added: `masking.md`'s wrapper needs `ADR-057`'s `erased`
branch, bundled with a genuine, still-unresolved contradiction between
`masking.md`'s "v1 is `FixedValue`-only" claim and `06-solution-
structure.md`'s three registered masking strategies.

## Phase 2, restructured: `08-build-plan.md` moves off fixed `Phase N` numbering

Direct request, offered as an explicit choice before starting given the
size (full restructure now vs. a lighter backfill vs. deferring to do
Phase 3 first) — user chose the full restructure. Converted all 24
original `Phase N` sections (`ADR-001`–`049`) to named items, each
declaring its own prerequisite items by name instead of a phase number,
and backfilled real (not one-line) entries for every one of `ADR-050`–
`093` that didn't already have a documented home:

- **5 of the 44 new ADRs already had a home** in the original 24 items,
  found by reading the actual file rather than assuming the TODO item's
  own list was complete: `ADR-050`→"Property-Level Masking", `ADR-051`→
  "Sharding & Replication", `ADR-052`→"Streaming Channels", `ADR-053`→
  "Upcast Materialization + Downcast", `ADR-075`→"Multi-Tenancy" (all
  already had "Extended by"/"Boundary note" call-outs from earlier
  sessions) — not duplicated as new items.
- **3 testing-strategy ADRs** (`ADR-055`/`063`/`085`) and **3 cross-
  cutting discipline/scope ADRs** (`ADR-059`/`084`/`092`) folded into an
  expanded "Cross-cutting, every item" section rather than given
  standalone entries with their own exit criteria — none of the six gates
  any single item's own build the way a real dependency does.
- **25 new named items** for the rest, read from their actual ADR text
  (not just `07-adrs.md`'s one-line index) before writing scope/depends-
  on/exit-criteria for each, including the two `TODO.md` had specifically
  flagged as needing real exit criteria: "GDPR/CCPA Erasure via Crypto-
  Shredding" (`ADR-057`) and "Release Engineering, Packaging & Supply
  Chain" (`ADR-062`, bundled with `ADR-074`/`076`/`080`/`091` — five
  release-process ADRs sharing one "does this actually ship" exit-
  criteria shape rather than five near-identical items).
- **Revisions modeled as forward-pointing notes, not reordering** —
  the same pattern "Multi-Tenancy" already used for `ADR-075`: "Delegated
  Grants, RBAC & Read Audit Logging" keeps its original position and
  scope, with a new note that "Control-Plane Actions as Reserved Events"
  (`ADR-067`) later revises its storage mechanism (Role/UserPermission
  become folded read models over reserved events, not plain CRUD tables)
  without changing its externally-observable exit criteria.
- **Two separate dependency diagrams**, not one 48-node graph: the
  original core-build diagram, relabeled but otherwise unchanged, plus a
  new one for the 25 additions, with every edge crossing back into the
  core diagram written out as plain text underneath rather than
  duplicated into the diagram itself.
- **Six stale cross-references to the old `Phase N` scheme, found by
  grepping the whole repo, not just this file**: `docs/data/schema-
  registry.md`, `docs/adrs/adr-002`, `docs/features/streaming-
  channels.md`, `docs/adrs/adr-008`, `docs/features/mvvm-client.md`,
  `docs/adrs/adr-076`, and `README.md` all cited `08-build-plan.md` by a
  now-defunct phase number — all six updated to cite by name.
  `CLAUDE.md`'s own "a new capability gets a Phase" convention bullet
  updated to match the new naming scheme.

`TODO.md`'s former Phase 2 removed outright; the 13-domain Salt-mockup
batch renumbered to Phase 2.
