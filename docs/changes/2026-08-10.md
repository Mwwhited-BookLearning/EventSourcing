# 2026-08-10

**Item 26, "GDPR/CCPA Erasure via Crypto-Shredding," is Done** (`ADR-057`).
`docs/08-build-plan.md`'s status table and `BuildPlan_Additions` diagram
fill both updated to Done/`#palegreen` in this pass.

## New `EventStore.Erasure` project

- [`IErasureKeyStore.cs`](../../src/EventStore.Erasure/IErasureKeyStore.cs) —
  the keyed-DI Strategy seam (`create`/`encrypt`/`decrypt`/`destroy`),
  the same shape `IMaskingStrategy`/`ADR-052`'s `IStreamRedactionStrategy`
  already established.
- [`LocalErasureKeyStore.cs`](../../src/EventStore.Erasure/LocalErasureKeyStore.cs) —
  AES-256-GCM, key material in a new
  [`LocalErasureKeyMaterial.cs`](../../src/EventStore.Domain/EntityStore/LocalErasureKeyMaterial.cs)
  table inside `EventStoreContext` — deliberately durable, not in-memory
  (losing it is equivalent to erasing every subject it protects at once).
- [`HashiCorpVaultErasureKeyStore.cs`](../../src/EventStore.Erasure/HashiCorpVaultErasureKeyStore.cs) —
  real VaultSharp transit-engine calls, verified against the actual
  installed package via reflection, not the library doc's own (incorrect)
  usage snippet.
- [`ErasureKeyService.cs`](../../src/EventStore.Erasure/ErasureKeyService.cs) —
  orchestrates [`EntityErasureKey.cs`](../../src/EventStore.Domain/EntityStore/EntityErasureKey.cs)
  metadata against whichever backend an `AppId` is configured for
  (`ErasureOptions.BackendByAppId`).
- [`PayloadEncryptor.cs`](../../src/EventStore.Erasure/PayloadEncryptor.cs) /
  [`ErasureScopeResolver.cs`](../../src/EventStore.Erasure/ErasureScopeResolver.cs) —
  the publish-time encrypt walk and the shared `erasureScope`
  JSON-pointer resolution both the encrypt and decrypt paths use.
- [`EntityErasureRequestedEventType.cs`](../../src/EventStore.Erasure/EntityErasureRequestedEventType.cs) /
  [`EntityErasureResolver.cs`](../../src/EventStore.Erasure/EntityErasureResolver.cs) —
  the reserved event and its `RouterWorker` reactor.

Cloud KMS backends (Azure Key Vault, AWS KMS, Google Cloud KMS) are
documented in `08-build-plan.md`'s own section as future, same-seam
additions — not built this pass, flagged there rather than silently
dropped. The seam already proves it needs no design change to add an Nth
backend, by the fact that Local and HashiCorp Vault already coexist.

## Two architecture-vs-ADR conflicts, found before writing any code

`ADR-057` assumed the pre-"Entity-Centric Core Rebuild" synchronous
validate-then-persist pipeline. The actual architecture is always-202
Inbox + async Router (`ADR-023`) — encryption still has to happen
synchronously at publish time (this ADR's own explicit ordering
requirement, before `Payload` is persisted and hashed), so
[`PublishService.cs`](../../src/EventStore.Inbox/PublishService.cs)
independently resolves `EntityId` for encryption-scoping purposes only,
via the same `EntityIdResolver` pure function `RouterWorker` uses —
`StoredEvent.EntityId` itself is unaffected, still starting empty and
still filled in only by the Router.

Random-nonce AES-GCM would have broken `ADR-011`'s publish-idempotency
hash-matching — retrying an identical logical publish containing a
classified field would encrypt differently each time (a fresh random
nonce), making `PayloadHash` differ and falsely reporting `409 Conflict`
instead of an idempotent replay. Resolved via deterministic/convergent
encryption before writing any integration test: an HMAC-derived nonce for
`LocalErasureKeyStore` (`DeriveNonce(key, plaintext)`), and Vault's own
`ConvergentEncryption`+`Derived`+a fixed `Base64EncodedContext` for
`HashiCorpVaultErasureKeyStore` — verified via a standalone probe that
determinism actually holds, not assumed.

## Fold-time and registration-time validator changes

[`JsonSchemaInstanceValidator.cs`](../../src/EventStore.SchemaRegistry/JsonSchemaInstanceValidator.cs)
(shared by `RouterWorker`'s fold-time re-validation and
`UpcastMaterializer`'s upcast-result check) gained one exemption: a field
carrying `x-masking.regulatoryClassification` is always stored (and
re-derived) as ciphertext — always a string — regardless of its
originally-declared type, so type-checking it against that declared type
is expected to "fail" and must be skipped. Fixed in the one shared
validator, not taught to each caller individually.

[`MaskingSchemaValidator.cs`](../../src/EventStore.SchemaRegistry/MaskingSchemaValidator.cs)
gained format validation for the new optional `x-masking.erasureScope`
field, reusing `JsonPathValidation`'s existing safe-subset grammar
(`EntityIdField`'s own injection-surface protection).

## A doc bug found and fixed via reflection, not guessed

[`docs/libraries/dotnet/hashicorp-vault.md`](../libraries/dotnet/hashicorp-vault.md)'s
own usage snippet named a non-existent
`vaultClient.V1.System.DeleteTransitKeyAsync` method. The real API,
confirmed against the actual installed VaultSharp assembly via a
throwaway reflection probe, is
`ITransitSecretsEngine.DeleteEncryptionKeyAsync` (requires
`UpdateEncryptionKeyConfigAsync(..., DeletionAllowed: true)` first, since
Vault refuses key deletion otherwise). Fixed in the same pass.

## The reserved event, and how it's actually triggered

`EntityErasureRequested` is registered lazily, the first time a given
`AppId` ever creates its first DEK
(`ErasureKeyService.GetOrCreateAsync`) — the same "not seeded up front"
treatment `ADR-031`'s `ChannelLagDetectedEventType` already established
— rather than at Host startup (no `AppId` is known then). Requesting
erasure is an ordinary publish of this reserved type through the SAME
Publish API any other event type uses — no bespoke DELETE-shaped
endpoint — gated by an ordinary `requiredClaim` (`erasure:request`,
`ADR-050`'s existing mechanism). `RouterWorker`'s existing
"special-purpose reactor" shape (`AuthorityDecisionResolver`'s own
precedent) folds it into its own entity like any other type, then
performs the additional side effect: destroying the target entity's DEK.

**A real bug caught while writing the erasure test suite, not by reading
the code back**: `RequiredClaimRequest.Direction` only parses to
`"Publish"` or `"Read"` (`ClaimDirection`'s two actual enum members) — an
initial `"Write"` value silently failed `Enum.TryParse` inside
`SchemaRegistryService.RegisterAsync`, which records a validation error
rather than throwing, so the reserved type's own registration would have
quietly stored no required claim at all. Caught because the erasure-
request test scenario explicitly asserted claim enforcement, not because
registration itself failed loudly — fixed by using `"Publish"`.

## Two different test-migration strategies for two different signature changes

`PublishService` gained a new `PayloadEncryptor` constructor parameter.
Made **optional** (`= null`), matching `IOptions<OriginIdOptions>?`'s own
established precedent immediately above it in the same file, for the
identical reason: ~37 pre-existing test files construct `PublishService`
directly and never register a classified field, so a null encryptor
correctly means "nothing to encrypt," not a missing dependency — avoiding
a 37-file mechanical sweep for a concern almost none of those files
exercise.

`IPayloadMasker.MaskAsync` (renamed from `Mask`, now `async`, now taking
`entityId`) has no such escape — every real caller needs real
decrypt/erasure-check behavior. Confirmed via `grep` that
[`EventTailReader.cs`](../../src/EventStore.Follow.Api/EventTailReader.cs)'s
`MaskPayload` is the ONLY real production call site (`PartialRevealStreamRedactionStrategy`'s
similarly-named `.Mask(...)` call is a *different* interface,
`IMaskingStrategy`, unaffected) — updated directly. `MaskingTestSupport.
CreatePayloadMasker` was changed to take the caller's own already-
constructed `db`/`registry` (needed to resolve the new `ErasureKeyService`
dependency `PayloadMasker` now has), with a handful of call-site fixes
across `Masking*Tests`/`Follow*Tests`/`Replication*Tests` (the last three
needed their `IOptions<OriginIdOptions>` argument moved from positional
to named, since a new optional parameter was inserted before it in the
constructor) — not a large sweep.

## Tests

New [`ErasureScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/ErasureScenarioAssertions.cs)
(5 scenarios: ciphertext at rest via direct DB inspection, claim-holder
decrypt with non-claim-holder masking proven unaffected by encryption,
erasure producing `{"erased": true}` unconditionally even for a claim
holder, the hash chain surviving erasure completely unchanged, and a
cross-entity `erasureScope` erasing the *scoped-to* entity's key, not the
event's own) run identically against SQLite/PostgreSQL/SQL Server
(`ErasureSqliteTests`/`Postgres`/`SqlServer`), plus a dedicated
[`ErasureVaultTests.cs`](../../tests/EventStore.IntegrationTests/ErasureVaultTests.cs)
(2 scenarios) against a REAL `hashicorp/vault:1.19` dev-mode
Testcontainer, not a mock: `IErasureKeyStore`'s own round trip against
Vault's real HTTP API, and `ADR-057`'s own named exit-criteria example —
two `AppId`s in one deployment, one on the default Local backend and one
explicitly configured to HashiCorp Vault, both correctly decrypting
through their own backend, confirmed via direct inspection that the
Vault-backed key never touches `LocalErasureKeyMaterial` at all.

`EventStore.IntegrationTests` now has 92 `[TestMethod]`s (up from 87) —
all pass reliably alone and in the SQLite-only subset; the full
multi-provider run hit the same pre-existing, unrelated Testcontainers
resource-contention flake already tracked in `TODO.md` (this run: 5-6
container-startup-race failures across `Replication`/`Streaming`/
`ViewDefinition`/`UpcastMaterialization`/a plain insert test — a
different rotating set each run, none touching this item's own code,
every one confirmed passing standalone).

# Item 27, "PCI-DSS Sensitive Authentication Data Registration Boundary," is Done

Much smaller than item 26, same day. `ADR-071`'s own Decision text already
said this needs no new mechanism, just an extension of an existing one —
held true: one added check inside
[`MaskingSchemaValidator.cs`](../../src/EventStore.SchemaRegistry/MaskingSchemaValidator.cs)'s
`ValidateMaskingConfig` rejects registration outright when any field
declares `x-masking.regulatoryClassification: "PCI-SAD"` — no new
project, no publish-path change, no Host wiring, since registration-time
rejection already flows through `SchemaRegistryService.RegisterAsync`'s
existing `errors` list the exact same way `EnumFallbackSchemaValidator`/
every other structural `x-masking` check already does.

`docs/data/schema-registry.md` already carried the `PCI-SAD` cross-
reference to `ADR-071` from an earlier documentation-only pass — no doc
drift found this time, only code catching up to what the doc already
said.

Two new scenarios added to
[`SchemaRegistryScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/SchemaRegistryScenarioAssertions.cs):
a `PCI-SAD`-declaring field is rejected `400`; the ordinary `"PCI"`
classification for a full card number still registers successfully,
confirming the boundary is scoped to SAD specifically, not every
PCI-related field. Wired into `SchemaRegistrySqliteTests`/`Postgres`/
`SqlServer`'s existing single `AllSchemaRegistryScenarios` `[TestMethod]`
each — `EventStore.IntegrationTests` still has 92 `[TestMethod]`s. All
pass across SQLite/PostgreSQL/SQL Server.

# `08-build-plan.md`'s two dependency diagrams consolidated into one

Per direct request. `BuildPlan_CorePhases` and `BuildPlan_Additions` (a
core-build graph and an additions-since-`ADR-050` graph, split apart
during the prior rework session specifically because "a single 48-node
graph would be denser than useful") are now one `BuildPlan_All` diagram —
every cross-edge an addition item had onto the core build, previously
only written out as prose beneath the second diagram, is now a real edge
in the merged graph. `CLAUDE.md`'s own standing convention referencing
the two diagrams by name was updated in the same pass, and the prose
beneath the diagram was adjusted to describe edges the diagram itself now
shows, rather than being the only place they were recorded.

# `BuildPlan_All` framed into four application tiers

Per direct request. All 48 items plus `ActorId on Every Event` are now
grouped into four PlantUML composite states — External Services,
Persistence, Local Services, UI — laid over the exact same node set and
edges, changing nothing about what depends on what. Tier assignment
follows each item's own primary purpose (e.g. Outbound Webhooks/RFC 3161
Timestamping/Tenant Federation Mapping → External Services, since their
whole point is talking to a system outside this framework's own control)
rather than strictly where its code happens to run, and is recorded as a
rough architectural framing, not a rigid boundary, directly above the
diagram.

# Item 28, "Local/Edge Active-Scope Caching & Erasure Invalidation," is Done

One deliberately, explicitly accepted narrowing, checked with the user
before building rather than assumed. `ADR-065`'s "falling out of scope
proactively evicts the local copy" claim has a real gap: the Subscription
filter is enforced server-side, per event, so an entity's own later
disqualifying update is simply never delivered at all — there is no
push-based "you fell out of scope" signal for the client to react to.
Asked explicitly which way to resolve it: client-side re-evaluation of
the full scope predicate on every update (would need a second, locally-
evaluable filter representation), or accept the server-filter-only gap as
a named limitation. The user chose the latter. Both halves of
[`docs/features/mvvm-client.md`](../features/mvvm-client.md)'s own
Gherkin were updated to describe this honestly, not silently narrowed.

Implemented client-side only (`client-web`):
[`subscriptionBuilder.ts`](../../client-web/src/api/subscriptionBuilder.ts)
gained `ScopeFilterClause`/`serializeWhereClauses`, threading an optional
`where` argument (the same `[EventFilterInput!]` shape the server already
exposes) through `buildSubscriptionQuery`; `ClientConfig.scopeFilter`
carries it from
[`useEntityViewActions.ts`](../../client-web/src/composables/useEntityViewActions.ts)'s
`subscribe()`. Erasure invalidation is a SECOND, independent subscription
(`subscribeToErasure`, same introspect-then-subscribe shape as the entity
subscription) to the reserved `EntityErasureRequested` type for the
client's own `AppId` — on receipt, `targetEntityId` is resolved and
`entityCacheStore.purge()` (already stubbed by "MVVM Client" exactly for
this item) is called immediately, never deferred.

## A real, pre-existing bug found and fixed while building this item's own mechanism

`payloadTypeName`/`subscriptionFieldName` never lowercased the event type
before sanitizing it, but `SchemaRegistryService.RegisterAsync` always
stores `EventTypeDefinition.Name` lowercased
(`normalizedName = eventTypeName.ToLowerInvariant()`) before
`FollowSubscriptionTypeModule` ever builds a field name from it
server-side. Every existing client call site (including this item's own
new `EntityErasureRequested` subscription, which would have failed
identically) passed the type's natural casing (e.g. `"OrderPlaced"`),
silently subscribing to a GraphQL field name that never matched anything
the server actually exposes — a `data[fieldName]` lookup that's simply
always `undefined`, no error, no crash, just no data ever arriving. Fixed
by lowercasing the event type (never `AppId`, which the server never
lowercases) inside those two functions; the existing
`subscriptionBuilder.spec.ts` assertions that had encoded the wrong
(mixed-case) expected behavior were corrected in the same pass.

## A second real gap found while verifying this item's own exit criteria

Every Subscription this client opens (the main entity one, and this
item's own new erasure one) is hardcoded `mode: TAIL`, with no persisted
resume cursor and no `mode: Replay`/`fromSequenceNumber` reconnect path
at all — a client that reconnects AFTER missing an event while
disconnected (the literal case this item's own "offline at the moment
erasure fires" exit criterion names) has no guaranteed catch-up today.
Pre-existing since "MVVM Client" (item 21) built the subscription
mechanism, not introduced here — surfaced because this is the first item
whose own exit criteria explicitly depend on reconnect behavior being
correct. Added as a new `TODO.md` item rather than silently claimed
satisfied.

3 new Vitest specs added to `useEntityViewActions.spec.ts` (scope filter
reaches the query; an `EntityErasureRequested` event purges the matching
cached entity; a differently-targeted one leaves an unrelated entity
untouched) plus 2 new `subscriptionBuilder.spec.ts` specs for `where`-
clause serialization — `client-web` now has 31 passing Vitest specs (up
from 26), `vue-tsc -b` and `vite build` both clean.

# Item 29, "Digital Sign-Off for Regulated Actions (Step-Up Authentication)," is Done

Four real bugs found while verifying this item's own exit criteria, same
day — none assumed satisfied without actually checking.

## Bug 1: `ActorId on Every Event` was never actually built, despite this diagram's own `#palegreen` claim

`PublishService` hardcoded the literal string `"unauthenticated"` for
every publish, regardless of the caller's real, already-verified
identity — contradicting `docs/data/event-log.md`'s own "ALWAYS
populated, blocking, not advisory" text and the build-plan diagram's own
fill color for this item's named dependency. Fixed by reusing
[`AccessLogReaderContext.Resolve`](../../src/EventStore.Domain/AccessLog/AccessLogReaderContext.cs)'s
existing claim resolution verbatim — the same JWT, the same claims, no
reason for a second implementation.

## Bug 2: the hash chain never actually covered `Signature`, despite `ADR-066`'s own claim

`ADR-066`'s Decision text says "non-repudiation reuses the existing hash
chain, no new primitive... exactly as tamper-evident as everything else
in the log." Checked, not assumed: `ChainHash`/`PayloadHash` only ever
cover `{EventType, Payload, parentEventIds}` — `Signature`, like every
other envelope field, sat completely outside what either hash actually
protects. A direct-database edit to a stored `Signature` would have gone
completely undetected.

Fixed by extending
[`EventChainHash.Compute`](../../src/EventStore.Domain/EventLog/EventChainHash.cs)
specifically, not `PayloadHash`: `PayloadHash` is also `ADR-011`'s own
idempotency-comparison basis, and `Signature.SignedAt` is genuinely
wall-clock-real at each publish attempt, not deterministic the way
`ADR-057`'s classified-field ciphertext had to be made — including
`Signature` there would make every legitimate retry of a signed publish
look like different content. `ChainHash` is never compared for
idempotency (only `PayloadHash` is), so extending it costs nothing there
while still making a tamper to `SignerId`/`SignedAt`/`Meaning`/`Acr`
diverge the chain at exactly that `SequenceNumber`. `Signature` is
omitted entirely (not hashed as a literal `null`) for an unsigned event,
so every event type that never uses `RequiredSignature` computes
byte-identical `ChainHash` values to before this parameter existed.
[`ChainVerificationService.cs`](../../src/EventStore.Inbox/ChainVerificationService.cs)
and
[`EventAppender.cs`](../../src/EventStore.Persistence/EventAppender.cs)
both updated to pass the row's own `Signature` through.

## Bug 3: `RequiredSignature`/`Signature` were the only two JSON-converted envelope fields missing an EF `ValueComparer`

Found while writing the `ChainHash` tamper-detection test itself: a
direct in-place mutation of an already-tracked `Signature.Meaning`
(`target.Signature!.Meaning = "TAMPERED"`) silently never reached the
database at all. Every list-typed JSON-converted property in
[`EventStoreContext.cs`](../../src/EventStore.Persistence/EventStoreContext.cs)
already has a `ListComparer<T>` attached; `RequiredSignature`/`Signature`
(both single class instances, not `IEnumerable`, so `ListComparer`'s own
constraint doesn't fit) were the only two missing the equivalent.  EF's
default reference-equality change detection for a converted class-typed
property never notices an in-place mutation of the SAME instance — only
assigning a NEW instance would have been detected. Fixed by adding
[`JsonValueConverter.NullableComparer<T>`](../../src/EventStore.Persistence/JsonValueConverter.cs),
the single-object counterpart to the existing `ListComparer<T>`, and
wiring it onto both properties.

## Bug 4: two real OpenIddict/JWT claim quirks, found only by running the actual RFC 9470 round trip over real HTTP

`EventStore.DevIdp` gained a dev-only step-up simulation: an opt-in `acr`
form parameter on the ordinary `client_credentials` flow, the same
"opt-in, every existing caller unaffected" shape `app_id`/`ADR-046`
already established — a real IdP only ever grants a given `acr` value
after actually performing that authentication method; this dev/POC IdP
has no interactive login for a machine caller to step up through at all,
so it just takes the caller's word for it, the same posture it already
takes for several other dev-only simplifications.

Running this against OpenIddict's real pipeline surfaced two distinct
failures, peeled back one at a time: (1) `ValidateSignInDemand` requires
`auth_time` to carry a genuinely numeric `ClaimValueType` — the plain
`SetClaim(string, string)` overload threw `"the auth_time claim...  is
malformed or isn't of the expected type"`; fixed via the dedicated
`SetClaim(string, long?)` overload (confirmed via a reflection probe
against the actual installed `OpenIddict.Abstractions` assembly, not
guessed). (2) Even after that fix, a step-up token that genuinely carried
`acr` was still rejected by `PublishService` — `JwtBearer`'s own default
`MapInboundClaims=true` remaps `acr` to
`http://schemas.microsoft.com/claims/authnclassreference` before any
resolver ever sees it (confirmed against
`JwtSecurityTokenHandler.DefaultInboundClaimTypeMap` via a second
reflection probe), the exact same remapping class
`AccessLogReaderContext.Resolve`'s own comment already documents for
`"sub"`/`ClaimTypes.NameIdentifier`. Fixed by checking both claim names,
the same way that resolver already does.

## RFC 9470, verified against the actual RFC text before writing it

[`PublishEndpoints.cs`](../../src/EventStore.Inbox/PublishEndpoints.cs)'s
hand-built `WWW-Authenticate` challenge (`Bearer
error="insufficient_user_authentication"[, acr_values="..."][,
max_age="..."]`, `401`) was checked against RFC 9470's own text via
`WebFetch` before being written, not recalled from memory — confirming
the exact parameter names/quoting and that `acr_values` is
space-separated when more than one value is configured.

## Tests

[`DigitalSignOffScenarioAssertions.cs`](../../tests/EventStore.IntegrationTests/DigitalSignOffScenarioAssertions.cs)
(10 scenarios: `RequiredSignature` registration validation, step-up
rejection for a missing/wrong `acr` and a stale `auth_time`, a
missing-`Meaning` rejection, full `Signature` population on success, the
`ChainHash` tamper-detection fix above, and the erasure-exemption
assertion composed with "GDPR/CCPA Erasure"'s own test infrastructure)
run across SQLite/PostgreSQL/SQL Server, plus
[`DigitalSignOffHttpSqliteTests.cs`](../../tests/EventStore.IntegrationTests/DigitalSignOffHttpSqliteTests.cs)
(3 real-HTTP scenarios: the RFC 9470 challenge header, a real step-up
token round trip against DevIdp, and the `Meaning`-omitted `400`).

`EventStore.IntegrationTests` now has 98 `[TestMethod]`s (up from 92) —
all pass across SQLite/PostgreSQL/SQL Server.

# Item 30, "Control-Plane Actions as Reserved Events," is Done

`ADR-067` treats control-plane mutations (schema registration, RBAC grants,
trust-root registration) as ordinary, reserved, hash-chained events in the
same Event Log — built in two stages.

## Stage 1: `SchemaRegistered`

`SchemaRegisteredEventType` (new, `EventStore.SchemaRegistry`) is a
reserved event type, registered lazily on the first registration for a
given `AppId` (the same precedent `ChannelLagDetectedEventType`/
`EntityErasureRequestedEventType` already established).
`SchemaRegistryService.RegisterAsync` appends this event immediately after
every successful registration — guarded against recursing into itself
during its own bootstrap registration — carrying `ActorId` resolved via
`AccessLogReaderContext.Resolve` (reused a third time, after AccessLog and
`PublishService`). Deliberately narrower than the ADR's own literal text:
`EventTypeDefinition`'s own write path is **not** rearchitected onto this
event — it stays a direct, synchronous write, because dozens of pre-existing
tests across the whole build assume synchronous read-your-own-write
registration, which an async fold would break. `SchemaRegistered` is a
genuine, hash-chained, lineage-traceable audit record layered alongside the
unchanged write, not a replacement for it.

Found and fixed one real regression in the same pass:
`SchemaRegistryService.ListAsync` didn't exclude this new reserved type,
so any `AppId`'s own type listing/pagination silently gained one extra row
the moment it registered its first real type — caught by
`ListingSupportsTopAndSkipPagination`'s own count assertion breaking.
Fixed by excluding the reserved name from `ListAsync`'s query; the same
latent gap for the three older reserved types is tracked in `TODO.md`
rather than silently widened beyond this item's own regression.

## Stage 2: RBAC grants and trust-root registration

A cross-process design, resolved by direct user consultation after research
surfaced that neither `ADR-067`, the build plan, nor `auth.md` committed to
*where* the RBAC write path should live or how DevIdp should observe the
folded state: **"fold into DevIdp via Follow."** A new Host-side project,
`EventStore.Rbac`, defines the 4 remaining reserved event types
(`RoleGrantedEventType`/`RoleRevokedEventType`/`PermissionGrantedEventType`/
`AppTrustRootRegisteredEventType`) and `RbacEndpoints.cs` — real, Bearer +
scope-gated Minimal API endpoints (`POST /rbac/roles/{roleName}/assignments`,
matching `DELETE`, `POST /rbac/permissions`, `PUT
/rbac/trust-roots/{issuerDid}`), gated by `registry:admin`/
`registry:trust-admin` + `AppIdScopeEvaluator.CanAdminister` for the
`admin`-scoped ones, mirroring `SchemaRegistryEndpoints.cs`'s own pattern.
Wired into all 3 `EventStore.Host.{Sqlite,Postgres,SqlServer}` projects.
`registry:trust-admin` needed a brand-new ASP.NET Core authorization policy
in `HostCoreExtensions.cs` — it existed only as a seeded OAuth scope before,
with no policy backing it at all (a real, if latent, gap: a
`registry:trust-admin`-scoped caller had nothing to actually authorize
against). `AppIdScopeEvaluator.CanAdminister` is deliberately **not**
applied to the trust-root endpoint — it only recognizes
`registry:admin`-shaped scopes, and `ADR-044` has never modeled an
AppId-scoped `registry:trust-admin:{appId}` variant; found by running a
real `registry:trust-admin`-scoped call and getting wrongly `403`'d.

`RoleGranted`/`RoleRevoked`/`PermissionGranted` use a synthetic composite
`EntityIdField` (`AssignmentKey`/`GrantKey`, computed by the endpoint as
`actorId:roleName`/`actorId:permission`) rather than the ADR's own literal
`{appId}:role:{roleId}`-shaped example — a role or permission can be
granted to many actors independently, and the generic patch-merge fold
semantics that shape implies would let one actor's grant silently overwrite
another's. `AppTrustRootRegistered` fits the literal example directly
(`$.IssuerDid`, one record per `(AppId, IssuerDid)`).

`EventStore.DevIdp` gained `RbacProjectionWorker`, a `BackgroundService`
that follows all 4 RBAC event types cross-process (via
`EventStore.Projections.Host.FollowClient`, the same client
`ProjectionHost<T>` uses, referenced directly rather than through the
higher-level generic engine, which is upsert-only with no delete concept —
a real mismatch for `RoleRevoked`) and folds them into DevIdp's OWN
existing `Role`/`RoleAssignment`/`UserPermission`/`AppTrustRoot` tables via
`RoleService`/`TrustRootService`'s own already-idempotent methods, reused
verbatim — only the caller changes, from an inbound HTTP request to a
Follow consumer. No persisted checkpoint: every (re)connect always replays
from `SequenceNumber 0`, accepted as a simple, low-risk choice for a
dev/POC, low-volume administrative stream, since the fold methods are
already no-ops on repeat. 4 of DevIdp's 6 old direct-write `/oauth/*`
endpoints are retired (`PUT /oauth/trust-roots`, `POST`/`DELETE
/oauth/role-assignments`, `POST /oauth/user-permissions`); `PUT
/oauth/roles` (what a role NAME bundles) and `PUT /oauth/federation-issuers`
stay, unaffected — neither is one of `ADR-067`'s own 5 named reserved event
types, a real narrowing this session's own research caught before assuming
otherwise. A new seeded client, `devidp-rbac-follower-client`, is
`RbacProjectionWorker`'s own identity.

`docs/data/schema-registry.md`'s `Role`/`UserPermission` section, written
in an earlier session, claimed these became **core-engine** entities under
`ADR-067` — building the actual mechanism this session showed that's wrong:
the fold happens entirely inside `EventStore.DevIdp` (an identity-provider
process, not the core engine), with no core-engine `EntityStoreRow`
involved anywhere. Corrected in the same pass, along with adding the
equivalent "folded from events" language to `AppTrustRoot`'s own section,
which a previous pass had missed entirely.

## Two real, pre-existing concurrency bugs found and fixed

Building `RbacProjectionWorker` (several concurrent Follow tail loops
sharing one `FollowClient` DI singleton) exercised concurrency patterns no
earlier caller ever had:
- `EventStore.Dpop.DpopKeyPair.CreateProof`/`SignJwt` were not safe for
  concurrent use against the same instance from multiple threads — hung
  indefinitely inside `JsonWebTokenHandler.CreateToken`, no exception, no
  timeout. Fixed with a lock around the actual signing call.
- `EventStore.Projections.Host.FollowClient` held ONE shared `DpopKeyPair`
  for its entire lifetime. Nothing in RFC 9449 requires reusing a key
  across unrelated token-acquisition + request-proof pairs — each
  `TailAsync` call already acquires its own access token, so it now
  generates its own key too. Its `/follow/{type}` request body also moved
  from `JsonContent.Create(new { ... })` to a hand-built JSON string —
  `System.Text.Json`'s reflection-based metadata resolution for a fresh
  anonymous type, invoked concurrently for the first time, hung the same
  way.

## Known gap

`RbacProjectionWorker`'s own live, cross-process Follow subscription is not
exercised end-to-end by any test. Running it inside
`DelegatedGrantsRbacFederationHttpSqliteTests.cs` surfaced a genuine
`WebApplicationFactory` hazard: `BackgroundService.StartAsync` invokes
`ExecuteAsync` synchronously, inline, until its first real suspension
point, and the worker's self-referential "DevIdp" `HttpClient` recursed
into its own still-building `WebApplicationFactory` one level up the same
call stack — silently, no exception, no log line, for the ENTIRE test
timeout. Worked around in production code with a real one-time startup
delay (documented in `RbacProjectionWorker.ExecuteAsync`'s own comment),
which resolves the hazard but was never proven against a live, in-process
`WebApplicationFactory` pair. The two RBAC-mutation tests instead verify
the Host's real write path (scope-gated publish through `/rbac/*`) and
DevIdp's own fold-target methods (`RoleService`/`TrustRootService`,
unchanged) directly. Tracked in `TODO.md`, along with the lineage-parent-
linking and replay-rebuild exit criteria this item's own build-plan section
still names but doesn't yet have a dedicated test for.

Verified against SQLite only, matching the SPIFFE/Gateway items' own
provider-agnostic precedent — this item's Host-side and DevIdp-side
mechanisms are equally provider-agnostic. All 53 SQLite integration tests
pass (no net new test count this pass — two pre-existing tests were
rewired to the new write path rather than new tests added).
