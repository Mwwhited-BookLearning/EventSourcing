# 2026-08-27

Direct request: support encrypting event payload fields for a referenced
key (crypto-shredding, already built per `ADR-057`), with the ability to
still run performant equality and range queries against the encrypted
values, and clean up the resulting search indexes as part of erasure.
Branch `dev/cryptoshredding` (off `main`), per direct request. **This
session is docs only** — no code changes; three new build-plan items
(54–56) track the later implementation pass.

## Research that shaped the design

Two verified findings changed the shape of the design before any doc was
written:

- `EnvelopeAesGcm` (`ADR-057`) already uses a deterministic nonce
  (`HMACSHA256(key, plaintext)[..12]`, required for `ADR-011`
  idempotency) — equal plaintexts already produce identical ciphertext
  *within one entity's key*. The actually-useful case (cross-entity
  search) needs a token comparable across independently-destroyable
  entity keys, which a purely per-entity key structurally cannot give.
- Property-preserving encryption's leakage-abuse break
  ([Naveed/Kamara/Wright, CCS 2015](https://www.microsoft.com/en-us/research/publication/inference-attacks-property-preserving-encrypted-databases/))
  recovers **exact plaintext**, not a fuzzy guess, for low-cardinality
  fields (birthdate, zip code, a diagnosis code) — verified directly
  against the paper this session after the user asked specifically
  whether a name could be recovered the same way. Answer, confirmed
  against the source: no, not to the same degree — the attack's power
  is concentrated in a small, guessable value domain; a high-cardinality
  field like a name is meaningfully more resistant. This directly shaped
  `ADR-096`'s guardrail into a cardinality-aware rule instead of a
  blanket classification rule.

## Decisions made, by direct user choice among researched options

1. **Build real, working examples of both** the safe bucketed-index
   approach and real Order-Revealing Encryption, each documented with
   its own pros/cons — not just adopt one and decline the other.
2. **Support both index-key scopes** (`Shared`, for real cross-entity
   search, with erasure deleting that entity's own derived index rows;
   `PerEntity`, for true crypto-shredding of the index token itself, at
   the cost of only ever answering single-known-entity lookups) as a
   per-field configurable choice, confirmed to never touch `ADR-019`'s
   hash chain or `ADR-033`'s Merkle-tree replication-sync (both operate
   only over `StoredEvent`/`Payload`).
3. **Queries should run inside the database as much as possible** —
   this is *why* the ORE option was built as a real alternative (the
   database compares ciphertext directly, zero decryption needed to
   filter) rather than only documented and declined, and why `ADR-098`
   designs an in-database native predicate evaluator seam
   (SQLCLR/PostgreSQL/SQLite) even though only its default app-tier
   implementation ships this pass.

## Docs written/updated this session

- New: `docs/comparisons/searchable-encryption-for-crypto-shredded-
  fields.md` — the full option comparison (blind index + bucketed range,
  real ORE, plain OPE, secure enclaves).
- New: `ADR-096` (blind index + bucketed range, cardinality-aware
  guardrail), `ADR-097` (opt-in ORE, no-override guardrail), `ADR-098`
  (in-database native predicate evaluator seam, designed not built).
- New: `docs/patterns/searchable-encryption-blind-index.md`.
- Updated: `docs/07-adrs.md`, `docs/comparisons/README.md`,
  `docs/patterns/README.md`, `docs/references.md` (adopted: CipherSweet,
  CryptDB, Always Encrypted, CLWW/Lewi-Wu ORE, both leakage-abuse papers;
  rejected: plain OPE, secure enclaves), `docs/glossary.md`,
  `docs/extensibility-points.md` (`ISearchIndexKeyStore`,
  `IEncryptedPredicateEvaluator`).
- Updated: `docs/data/schema-registry.md` (`FilterableFieldIndexKind`,
  `x-masking-searchable`, `EncryptedFieldIndexEntry`, per-provider index
  strategy addendum), `docs/02-data-model.md`'s classification table,
  `docs/04-odata-filter-pushdown.md` (routing section),
  `docs/features/filter-pushdown.md` (two new Gherkin scenarios).
- Updated: `docs/08-build-plan.md` — three new Not-started items (54–56),
  `BuildPlan_All` diagram nodes/edges, Implementation status table rows.

## Implementation pass (later the same day, direct request: "start building all of this work")

Built and tested items 54 and most of 55 against the design above, on the
same `dev/cryptoshredding` branch.

**Real findings during build, not assumed from the design docs**:
- **Found and fixed a prerequisite bug**: `PayloadEncryptor` (`ADR-057`'s
  own encryption) was never registered in any Host's DI — every real
  `PublishService` resolved it as `null`, so classified-field encryption
  was inert in production before this fix, exercised only by test code.
- `IErasureKeyStore` never exposes raw DEK bytes, so `PerEntity` key
  derivation is `SHA-256(EncryptAsync(keyReference, a fixed info
  string))`, not a literal HKDF — deterministic, entity-key-dependent,
  and permanently uncomputable once the DEK is destroyed.
- `ISearchIndexKeyStore`'s real shape is `ComputeHmacAsync(keyReference,
  data)`, not an envelope encrypt/decrypt pair — a Shared-scope index key
  computes a MAC, it never encrypts data at rest.
- `EncryptedFieldIndexEntry.Token` is a base64 `string`, not `byte[]` —
  corrected in `docs/data/schema-registry.md` in the same pass.
- `GraphQlFilterPredicateBuilder.Build` became `async` (resolving matches
  needs a database query); a bucketed range query now requires both a
  lower and upper bound in one clause (an open-ended range would
  enumerate an unbounded number of buckets).
- The ORE query path's app-tier default compares ciphertext in
  application memory across a field's own indexed rows, not via a native
  SQL operator — a real, named gap `ADR-098`/item 56 exists to close.

**Built**: `ADR-096` end to end (schema extension, registration guardrail,
`EncryptedFieldIndexEntry`, `PayloadIndexer`, `GraphQlFilterPredicateBuilder`
routing, `EntityErasureResolver` cleanup, the default
`AppTierEncryptedPredicateEvaluator`) and `ADR-097`'s
`OrderRevealingEncryption` construction, guardrail, and query routing.
EF Core migrations generated and verified clean on all three providers
(`AddEncryptedFieldIndexAndSearchIndexKeys`).

**Verified, not just built**: `EventStore.UnitTests.
OrderRevealingEncryptionTests` (order-preservation correctness across
many Number/DateTimeOffset pairs); `EventStore.IntegrationTests.
SearchableEncryptionSqliteTests` (equality query never touches plaintext;
erasure removes `Shared`-scope rows with `ChainHash` unchanged; both
registration guardrails refuse correctly); the full pre-existing Sqlite
suite (150/150) plus `ErasurePostgresTests`/`ErasureSqlServerTests`
against real Testcontainers-backed Postgres/SQL Server, confirming no
regression from the signature change or the DI fix on any provider.

**Not done this part of the session**: item 55 is built but not marked
Done — its own exit criteria require a dedicated security review this
pass didn't perform.

## Item 56 (later still, same day, direct request: continue after committing 54/55)

Built both native predicate evaluators from `ADR-098`, with genuinely
different confidence levels, stated plainly:

- **SQL Server — built and verified.** `src/EventStore.SqlClr.SqlServer/`
  targets `net48` (confirmed required: SQL Server's CLR host never loads
  .NET Core/.NET 5+ assemblies, only classic .NET Framework). `AesGcm` is
  otherwise .NET Standard 2.1+-only and unavailable there — made usable
  via `Microsoft.Bcl.Cryptography`, a real first-party Microsoft package,
  verified this session to build and run correctly under `net48` before
  any production code was written around it. `EncryptedPredicateFunctions.
  DecryptAndCompareCore` is cross-verified against a **golden ciphertext
  fixture generated from the real, production `EnvelopeAesGcm.Encrypt`**
  (net10.0) — proving genuine cross-runtime interoperability, not
  self-consistency — via `tests/EventStore.SqlClr.SqlServer.Tests` (5/5
  passing). Deployment script uses `sys.sp_add_trusted_assembly` (the
  modern CLR-strict-security mechanism), not a blanket security downgrade.
- **PostgreSQL — written, explicitly not verified.** Confirmed against
  current PostgreSQL docs first: `pgcrypto`'s raw encrypt/decrypt support
  only CBC/ECB, no GCM at all. Real AES-GCM needs `plpython3u` + Python's
  `cryptography` package — written as a real `plpython3u` function, but
  neither that language nor the package exists in the standard
  Testcontainers `postgres` image this project's other tests already use,
  and building a custom Postgres image for this one function is separate,
  not-yet-done infrastructure work, named rather than glossed over.
- Both scoped to the `Local` backend only (per `ADR-098`'s own Decision)
  — a Shared/`PerEntity` field on a real KMS/Vault backend needs network
  access neither evaluator has.

`08-build-plan.md` item 56 status set accordingly (not Done — SQL Server
verified, PostgreSQL unverified). Committed as a second commit on
`dev/cryptoshredding`, after the item 54/55 checkpoint above.

## Cloud/Vault ISearchIndexKeyStore backends (later still, same day, direct request)

Closed the last named gap from item 54: `ISearchIndexKeyStore` had only
a `Local` backend. Rather than reimplementing four cloud SDKs a second
time, `CloudSearchIndexKeyStoreAdapter` (`src/EventStore.Erasure/`) wraps
**any** existing `IErasureKeyStore` backend — Azure Key Vault, AWS KMS,
Google Cloud KMS, HashiCorp Vault all already expose exactly
`CreateKeyAsync`/`EncryptAsync`, the two primitives a search-index key
needs — via the same key-derivation trick `PerEntity` scope already
uses (encrypt a fixed label under the cloud-managed key, hash the
ciphertext into a local derived key, HMAC the caller's real data with
that). One mechanism, not four bespoke ones. Registered in
`ErasureServiceCollectionExtensions` alongside each corresponding
erasure backend, sharing the same SDK client instance, with its own
independent `SearchIndexKey` reference — no lifecycle coupling with any
entity's DEK despite the shared client.

Verified against `LocalErasureKeyStore` (the adapter's logic is
provider-agnostic — no real cloud credentials needed to test it):
`EventStore.UnitTests.CloudSearchIndexKeyStoreAdapterTests` (3/3)
confirms the HMAC is deterministic per `(keyReference, data)`, differs
for different keys or data, and fails once the wrapped key is
destroyed. Full unit suite (17/17) confirms no regression.
