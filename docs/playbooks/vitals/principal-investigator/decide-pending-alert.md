# Vitals -- Principal Investigator Queue: Decide a Pending IONM Alert

_Generated by `EventStore.E2ETests` against a real running deployment -- re-run `dotnet test tests/EventStore.E2ETests` to regenerate. Don't hand-edit this file directly; edit the owning test's own step captions instead, or the content will be overwritten on the next run._

## Sequence Diagram

![Sequence Diagram](../../../diagrams/playbooks/vitals/principal-investigator/decide-pending-alert/01-sequence-diagram.svg)

```plantuml
@startuml VitalsPiQueue_Playbook_Sequence
autonumber
participant "Samples.Vitals.Simulator" as simulator
participant "PublishEndpoint\n(Inbox)" as inbox
participant "Duplex Client\n(Queue tab, AuthorityQueue.vue)" as client
actor "Principal Investigator\n(review:ionm)" as pi
database "Entity Store" as entityStore

simulator -> inbox: POST /publish/IonmAlertRaised\n{ AlertId, SubjectId, Finding, Severity }\nReviewPending: true (every ~20s, forever)
inbox -> inbox: AuthorityStatus: "pending_review"
client -> inbox: subscription on_trial1_IonmAlertRaised (REPLAY)\n+ subscription on_trial1_authorityDecision (REPLAY)
inbox --> client: new pending item appears in the Queue list

pi -> client: fill Meaning, click Accept
client -> inbox: POST /publish/authorityDecision\n{ targetEventId, decision: "accepted", decidingActorId, reason }\nBearer <vitals-pi-client token, claims: review:ionm>

alt caller instead lacks "review:ionm"
  inbox --> client: 403 Forbidden
else PI clicks Reject instead of Accept
  inbox -> inbox: AuthorityStatus: "rejected" --\nnever folds into the authoritative Entity Store
end

inbox -> entityStore: fold now (catch-up) -- AuthorityStatus: "accepted"
inbox --> client: authorityDecision arrives over the live subscription
client -> client: remove the resolved item from the Queue list
@enduml
```

## Step 1. Opening the Principal Investigator Queue tab -- a real, already-built screen (VitalsPiQueue.vue) reachable from any Vitals client instance, not new this pass.

![Opening the Principal Investigator Queue tab -- a real, already-built screen (VitalsPiQueue.vue) reachable from any Vitals client instance, not new this pass.](decide-pending-alert/step-01.png)

## Step 2. Samples.Vitals.Simulator publishes a fresh IonmAlertRaised with ReviewPending: true roughly every 20 seconds -- this is a real, live pending item, not fixed seed data. The queue subscribes to both the raiser event type and authorityDecision live, so a decision anywhere resolves this list immediately.

![Samples.Vitals.Simulator publishes a fresh IonmAlertRaised with ReviewPending: true roughly every 20 seconds -- this is a real, live pending item, not fixed seed data. The queue subscribes to both the raiser event type and authorityDecision live, so a decision anywhere resolves this list immediately.](decide-pending-alert/step-02.png)

## Step 3. Filling in the required sign-off reason (Meaning, ADR-066) and clicking Accept publishes an authorityDecision as vitals-pi-client (claims: review:ionm) -- AuthorityDecisionResolver folds the target IonmAlertRaised into the authoritative Entity Store, and the item disappears from this queue the moment that decision arrives back over the same live subscription.

![Filling in the required sign-off reason (Meaning, ADR-066) and clicking Accept publishes an authorityDecision as vitals-pi-client (claims: review:ionm) -- AuthorityDecisionResolver folds the target IonmAlertRaised into the authoritative Entity Store, and the item disappears from this queue the moment that decision arrives back over the same live subscription.](decide-pending-alert/step-03.png)

