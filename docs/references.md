# References

A single bibliography for every real-world standard, spec, pattern, or
library this design package relies on or seriously considered. Each
numbered doc (`01`–`09`) also carries its own short "Suggested References"
section with just the citations relevant to that doc — this file is the
comprehensive index, organized by whether the concept was actually
**adopted** into the design or is recorded here as **reference-only**
(considered, and explicitly not built, with the reason stated).

## Adopted — used directly in this design

| Concept | Standard / spec | Where it's used |
|---|---|---|
| OAuth2 Client Credentials grant | [RFC 6749 §4.4](https://datatracker.ietf.org/doc/html/rfc6749#section-4.4) — The OAuth 2.0 Authorization Framework | `ADR-006` |
| Bearer token usage | [RFC 6750](https://datatracker.ietf.org/doc/html/rfc6750) — OAuth 2.0 Bearer Token Usage | `ADR-006` |
| Demonstrating Proof of Possession | [RFC 9449](https://datatracker.ietf.org/doc/html/rfc9449) — OAuth 2.0 DPoP | `ADR-017` |
| OIDC discovery | [OpenID Connect Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html) — OpenID Foundation | `ADR-006` (`/.well-known/openid-configuration`) |
| HTTP QUERY method | [RFC 10008](https://datatracker.ietf.org/doc/html/rfc10008) — The HTTP QUERY Method (June 2026) | `ADR-012` |
| Problem Details for HTTP APIs | [RFC 9457](https://datatracker.ietf.org/doc/html/rfc9457) (obsoletes RFC 7807) | `ADR-013` |
| CORS | [WHATWG Fetch Standard](https://fetch.spec.whatwg.org/#http-cors-protocol) — Cross-Origin Resource Sharing protocol | `ADR-014` |
| Server-Sent Events | [WHATWG HTML — Server-Sent Events](https://html.spec.whatwg.org/multipage/server-sent-events.html) | `03-api-contracts.md`, `features/follow-subscribe.md` |
| JSON | [RFC 8259](https://datatracker.ietf.org/doc/html/rfc8259) — The JavaScript Object Notation (JSON) Data Interchange Format | throughout (`Payload`, `JsonSchema` storage) |
| JSON Schema (2020-12) | [json-schema.org](https://json-schema.org/specification) | `02-data-model.md`, `05-schema-registry-and-spec-generation.md` |
| JSON Merge Patch | [RFC 7396](https://datatracker.ietf.org/doc/html/rfc7396) | `ADR-016`'s `SnapshotMerger` (overwrite-if-present half only — see `ADR-016`'s closing note on why the delete-on-`null` half is deliberately not used) |
| JSONPath-style path expressions | [RFC 9535](https://datatracker.ietf.org/doc/html/rfc9535) — JSONPath | `FilterableField.JsonPath` (`$.Amount` syntax), `04-odata-filter-pushdown.md` |
| OData `$filter`/`$top`/`$skip` syntax (borrowed, not full spec compliance) | [OASIS OData v4.01 — URL Conventions](https://docs.oasis-open.org/odata/odata/v4.01/odata-v4.01-part2-url-conventions.html) | `04-odata-filter-pushdown.md`, `ADR-003`, `ADR-012` |
| OpenAPI 3.1 | [OpenAPI Specification v3.1.1](https://spec.openapis.org/oas/v3.1.1.html) | `ADR-002`, `03-api-contracts.md`, via the `Microsoft.OpenApi` library |
| AsyncAPI 3.0 | [AsyncAPI Specification v3.0](https://www.asyncapi.com/docs/reference/specification/v3.0.0) | `ADR-002`, `03-api-contracts.md` |
| SHA-256 | [FIPS 180-4](https://csrc.nist.gov/pubs/fips/180-4/final) — Secure Hash Standard | `ADR-011` (`PayloadHash`), `ADR-019` (`ChainHash`) |
| Verifiable/tamper-evident append-only logs (model, not the binary-tree mechanics) | [RFC 9162](https://datatracker.ietf.org/doc/html/rfc9162) — Certificate Transparency v2.0 | `ADR-019` (explicitly a simpler *linear* chain, not CT's Merkle tree — see `ADR-019`'s Decision for why) |
| Event upcaster chains (the chaining *concept*, not this design's expression language) | [Axon Framework — Event Versioning](https://docs.axoniq.io/axon-framework-reference/4.11/events/event-versioning/) | `ADR-018`'s `UpcastChain` shape |
| Upcast mapping expressions | [OASIS OData Data Aggregation Extension v4.0 — `compute()`](https://docs.oasis-open.org/odata/odata-data-aggregation-ext/v4.0/odata-data-aggregation-ext-v4.0.html) | `ADR-018`'s `upcastFromPrevious` field — chosen over a general transform language (JSONata/JMESPath, see below) because an upcast mapping is always many-to-one/one-to-one, and reusing `Microsoft.OData.UriParser` (already a dependency for `$filter`) avoids a second expression grammar |
| C4 model | [c4model.com](https://c4model.com/) — Simon Brown | `01-c4-architecture.md` — the notation (Context/Container/Component), hand-styled in plain PlantUML rather than rendered via the C4-PlantUML macro library below |
| PlantUML | [plantuml.com](https://plantuml.com/) | Every diagram in this design package — every `!include` is banned project-wide (see `references.md`'s Reference-only entry for C4-PlantUML, and `CLAUDE.md`'s conventions) so a diagram never depends on network access or a bundled stdlib path to render |
| Gherkin / BDD | [Cucumber — Gherkin Reference](https://cucumber.io/docs/gherkin/reference/) | every `features/*.md` |
| EF Core | [Microsoft Learn — EF Core](https://learn.microsoft.com/en-us/ef/core/) | `02-data-model.md`, `06-solution-structure.md` — see `docs/libraries/dotnet/efcore.md` |
| OpenIddict | [openiddict.com](https://openiddict.com/) | `ADR-006`, `EventStore.DevIdp` — see `docs/libraries/dotnet/openiddict.md` |
| .NET Aspire | [Microsoft Learn — .NET Aspire overview](https://learn.microsoft.com/en-us/dotnet/aspire/get-started/aspire-overview) | `ADR-006`, `EventStore.AppHost` — see `docs/libraries/dotnet/aspire.md` |
| Testcontainers | [testcontainers.com](https://testcontainers.com/) | `06-solution-structure.md`, integration test strategy — see `docs/libraries/dotnet/testcontainers.md` |
| Event Sourcing / CQRS (the general pattern) | [Martin Fowler — Event Sourcing](https://martinfowler.com/eaaDev/EventSourcing.html) / [CQRS](https://martinfowler.com/bliki/CQRS.html) | `09-cqrs-read-models.md`, `ADR-015`/`ADR-016` |
| Scalar | [scalar/scalar](https://github.com/scalar/scalar), `Scalar.AspNetCore` NuGet package | `ADR-025` — OpenAPI docs UI — see `docs/libraries/dotnet/scalar.md` |
| AsyncAPI React component | [asyncapi/asyncapi-react](https://github.com/asyncapi/asyncapi-react) | `ADR-025` — AsyncAPI docs UI — see `docs/libraries/web/asyncapi-react.md` |
| HotChocolate | [chillicream.com/docs/hotchocolate](https://chillicream.com/docs/hotchocolate) | `ADR-037` — concrete GraphQL server — see `docs/libraries/dotnet/hotchocolate.md` |
| Jint | [github.com/sebastienros/jint](https://github.com/sebastienros/jint) | `ADR-018`/`ADR-037` — sandboxed JS for complex upcast mappings — see `docs/libraries/dotnet/jint.md` |
| CEL (Common Expression Language) for .NET — `Cel.NET` adopted | [cel.dev](https://cel.dev/) | `ADR-018`/`ADR-037` — declarative upcast mapping; the .NET port ecosystem is still fragmented, but `Cel.NET` was spiked and verified working (build-plan item 11) — see `docs/libraries/dotnet/cel-dotnet.md` |
| Write-Audit-Publish (WAP) pattern | Netflix (Michelle Winters, 2017), commonly implemented via [Apache Iceberg branching](https://www.dremio.com/blog/streamlining-data-quality-in-apache-iceberg-with-write-audit-publish-branching/) | `ADR-042` — the Event Log (write) / `AuthorityStatus` review (audit) / gated Entity Store fold (publish) shape |
| Quarantine pattern | [Azure Architecture Center](https://learn.microsoft.com/en-us/azure/architecture/patterns/quarantine) | `ADR-042` — informs the untrusted→trusted transition shape; deliberately NOT applied as written (this design makes untrusted data visible via a labeled Live View instead of blocking it) |
| Request-Reply / Correlation Identifier | [Hohpe & Woolf — Enterprise Integration Patterns: Correlation Identifier](https://www.enterpriseintegrationpatterns.com/patterns/messaging/CorrelationIdentifier.html) | `ADR-094` — `RespondsToEventId` envelope field; same authors' Idempotent Receiver/Inbox/Dead Letter Channel already inform `ADR-011`/`ADR-020`/`ADR-023` (`docs/patterns/idempotent-receiver-and-inbox.md`) |
| Vue 3 | [vuejs.org](https://vuejs.org/) | `ADR-039`, `mvvm-client-architecture.md` — see `docs/libraries/web/vue.md` |
| Pinia | [pinia.vuejs.org](https://pinia.vuejs.org/) | `mvvm-client-architecture.md` — see `docs/libraries/web/pinia.md` |
| Naive UI | [naiveui.com](https://www.naiveui.com/) | `mvvm-client-architecture.md` — see `docs/libraries/web/naive-ui.md` |
| OpenTelemetry | [opentelemetry.io](https://opentelemetry.io/), via .NET Aspire's `ServiceDefaults` | `ADR-026` — logging, tracing, and metrics for every service |
| Docker Compose | [docs.docker.com/compose](https://docs.docker.com/compose/) | `ADR-026` — production deployment path |
| OAuth 2.0 Token Exchange | [RFC 8693](https://datatracker.ietf.org/doc/html/rfc8693) | `ADR-036` — exchanging a self-attested UCAN for an ordinary bearer JWT. **Previously reference-only** (rejected for lack of an "act on behalf of" need) — un-rejected once `ADR-035`'s non-authoritative capture created a real one. |
| UCAN | [ucan.xyz](https://ucan.xyz/specification/) | `ADR-036` — offline-verifiable delegated capability for self-attested capture. **Previously reference-only** — see above; the exact same reversal. |
| W3C DIDs | [DIDs v1.1](https://www.w3.org/TR/did-1.1/) | `ADR-036` — proves cryptographic control of an identifier for a self-attested actor, deliberately not more than that. **Previously reference-only** — same reversal; Verifiable Credentials specifically (the sibling W3C spec) remain not adopted, since UCAN's delegation-chain model covers this design's actual need without them. |
| Break-glass access (healthcare emergency access) | [HIPAA §164.312(a)(2)(ii)](https://hipaa.yale.edu/security/break-glass-procedure-granting-emergency-access-critical-ephi-systems) | Informs `ADR-043`'s shape (temporary, capped, audited access) but adapted from self-service/emergency-triggered to peer-granted/deliberate — not adopted as-is. |
| HIPAA Audit Controls (who accessed what, when, protected from alteration, 6-year retention) | [45 CFR §164.312(b)](https://docs.alertlogic.com/analyze/reports/compliance/HIPAA-164.312-audit-controls.htm), [§164.316(b)(2)(i)](https://docs.alertlogic.com/analyze/reports/compliance/HIPAA-164.312-audit-controls.htm) | `ADR-045` — the shape `AccessLog` generalizes; this design isn't HIPAA-specific but the requirement shape transfers directly |
| Role-Based Access Control (RBAC) | Ferraiolo & Kuhn (NIST, 1992); standardized as [ANSI/INCITS 359](https://blog.ansi.org/ansi/role-based-access-control-rbac-incits-359/) (2004, rev. 2012/R2022) | `ADR-046` — base/flat-role tier adopted; Hierarchical RBAC and separation-of-duty constraints (also part of the standard) explicitly not adopted, no current need |
| Claims augmentation / token enrichment (federation pattern — Azure AD B2C custom policies, ADFS claim rules, Auth0 Actions/Rules all implement this shape) | industry pattern, no single governing spec | `ADR-047` — enriching an externally-issued token with locally-known application claims via `ADR-036`'s already-adopted RFC 8693 Token Exchange |
| Strategy pattern | [Gamma, Helm, Johnson, Vlissides — *Design Patterns: Elements of Reusable Object-Oriented Software*](https://en.wikipedia.org/wiki/Design_Patterns) (Addison-Wesley, 1994) | `ADR-009`'s `IMaskingStrategy`/`ADR-052`'s `IStreamRedactionStrategy` — one interchangeable class per masking/redaction content strategy, selected at runtime by the `x-masking`/`RedactedRange` `strategy` string |
| .NET keyed dependency injection services | [Microsoft Learn — Dependency injection: Keyed services](https://learn.microsoft.com/en-us/dotnet/core/extensions/dependency-injection#keyed-services) (introduced .NET 8) | `ADR-009`/`ADR-052` — the concrete mechanism resolving a Strategy-pattern implementation by its runtime string key, via `AddKeyedSingleton`/`GetRequiredKeyedService`, staying within `ADR-041`'s explicit-composition-root discipline |
| KISS (Keep It Simple, Stupid) | Popularized in software engineering via [Fowler/Beck-era XP literature](https://en.wikipedia.org/wiki/KISS_principle); originally an engineering-design heuristic (attributed to Kelly Johnson, Lockheed Skunk Works, 1960s) | `docs/comparisons/masking-strategies.md`/`ADR-009` — declining format-preserving encryption, generalization/bucketing, and tokenization absent a stated requirement, rather than leaving them open indefinitely |
| Row-Level Security (RLS) | [PostgreSQL — Row Security Policies](https://www.postgresql.org/docs/current/ddl-rowsecurity.html) | `ADR-043` — the named pattern this design's entity-scoped claims generalize; implemented at the application/claims layer, not via provider-native `CREATE POLICY`, since SQLite has no native RLS |
| PCI-DSS Requirement 3.3 (PAN masking — first six/last four digits displayed) | [strac.io — PCI masking requirements](https://www.strac.io/blog/pci-masking-requirements-credit-card) | `ADR-009` — the plain-language `showFirst`/`showLast` model `PartialReveal` is directly patterned on |
| `System.ComponentModel.MaskedTextProvider` mask syntax (`0`/`9`/`#`/`L`/`?`/`&` position codes) | [Microsoft Learn — MaskedTextProvider](https://learn.microsoft.com/en-us/dotnet/api/system.componentmodel.maskedtextprovider) | Considered for `ADR-009`'s `PartialReveal` shape, not adopted — a real, official .NET convention, but a symbolic code table is less human-readable than the named-field alternative chosen |
| NIST SP 800-63-3 (IAL/AAL/FAL — multi-axis identity assurance) | [pages.nist.gov/800-63-3](https://pages.nist.gov/800-63-3/sp800-63-3.html) | Raised as a possible generalization of `AuthorityStatus`/`ReaderTrustBasis` — not yet adopted, see `docs/patterns/multi-axis-authority-assurance.md` and `docs/10-open-questions.md` |
| SPIFFE/SPIRE workload identity | [spiffe.io](https://spiffe.io/docs/latest/spiffe-about/overview/), [SPIRE](https://spiffe.io/docs/latest/spire-about/spire-concepts/) | `ADR-048` — internal service-to-service and `ADR-033` peer-sync identity. **Previously reference-only, rejected** ("no multi-workload mesh... a handful of statically-seeded OAuth2 clients already covers this") — un-rejected once this design's own growth (many internal services, `ADR-033`'s cross-site peers) created the exact scenario the original rejection named as the reason to revisit. See `docs/libraries/dotnet/spiffe-spire.md` for the concrete .NET integration shape (no first-party client — a small community NuGet package, or a generic gRPC client against the Workload API). |
| UCAN root-of-trust (the one thing the UCAN spec itself leaves out-of-band) | [ucan.xyz specification](https://ucan.xyz/specification/) | `ADR-044` — resolves "which DID is authoritative for a capability namespace" via a per-`AppId` `AppTrustRoot` registry, since the spec deliberately doesn't define this itself |
| NIST SP 800-53 Rev. 5, AC-5 (Separation of Duties) | [CSF Tools — AC-5](https://csf.tools/reference/nist-sp-800-53/r5/ac/ac-5/) | `docs/comparisons/trust-root-registration-gate.md` — the general control informing the new `registry:trust-admin` scope's separation from `registry:admin` |
| Narrow, dedicated permission for establishing a new external trust anchor (cloud IAM convention — AWS `CreateOpenIDConnectProvider`/`CreateSAMLProvider`, GCP `roles/iam.workloadIdentityPoolAdmin`, Azure Entra "External Identity Provider Administrator") | [AWS IAM API reference](https://docs.aws.amazon.com/IAM/latest/APIReference/API_CreateOpenIDConnectProvider.html), [Google Cloud — Workload Identity Federation](https://docs.cloud.google.com/iam/docs/workload-identity-federation), [Microsoft Learn — direct federation](https://learn.microsoft.com/en-us/entra/external-id/direct-federation) | `docs/comparisons/trust-root-registration-gate.md` — industry pattern, no single governing spec; informs gating `AppTrustRoot` registration behind its own narrow scope rather than folding it into `registry:admin` |
| HTTP Range Requests | [RFC 7233](https://datatracker.ietf.org/doc/html/rfc7233) | `ADR-031`/`ADR-032` — seekable playback/retrieval for streaming channels and large attachments |
| Content-addressable storage (address-by-hash, dedup-by-construction) | [Git internals — Git Objects](https://git-scm.com/book/en/v2/Git-Internals-Git-Objects), [IPFS — content addressing](https://docs.ipfs.tech/concepts/content-addressing/) | `ADR-032`'s `Attachment.ContentHash` (SHA-256, the same primitive `ADR-011`/`ADR-019` already use). **Previously reference-only, rejected** by an earlier innovation-sprint review, for lack of a large-object use case at the time (`Payload` was small JSON only) — re-adopted once `ADR-032`'s binary attachments created a real one. |
| Media Fragments URI | [W3C Media Fragments URI 1.0](https://www.w3.org/TR/media-frags/) | `ADR-031` — deep-linking to a temporal point/interval in a streaming channel |
| MVVM (Model-View-ViewModel) | [John Gossman — Introduction to Model/View/ViewModel](https://learn.microsoft.com/en-us/archive/blogs/johngossman/introduction-to-modelviewviewmodel-pattern-for-building-wpf-apps) (2005) | `ADR-039` |
| MVP (Model-View-Presenter) | Mike Potel (Taligent, 1996) — [MVP: Model-View-Presenter](https://www.wildcrest.com/Potel/Portfolio/mvp.pdf) | `docs/comparisons/ui-architecture-patterns.md` — first fallback below MVVM |
| MVC (Model-View-Controller) | Trygve Reenskaug (Xerox PARC, 1979) | `docs/comparisons/ui-architecture-patterns.md` — second fallback below MVVM |
| Service Workers | [W3C Service Workers](https://www.w3.org/TR/service-workers/) | `ADR-039` — offline app-shell/data serving with no network present |
| Web App Manifest | [W3C Web App Manifest](https://www.w3.org/TR/appmanifest/) | `ADR-039` — installable web client |
| Background Sync API | [WICG Background Synchronization](https://wicg.github.io/background-sync/spec/) (Working Draft — not in Safari/WebKit as of this writing) | `ADR-039` — flushing the client outbox once connectivity returns; polyfilled on next open/focus where unsupported |
| Pure DI / Composition Root (manual, explicit dependency wiring, no convention-magic) | Mark Seemann — [Composition Root](https://blog.ploeh.dk/2011/07/28/CompositionRoot/), *Dependency Injection Principles, Practices, and Patterns* | `ADR-041` |
| `System.Text.Json` | [Microsoft Learn — System.Text.Json overview](https://learn.microsoft.com/en-us/dotnet/standard/serialization/system-text-json/overview) | `ADR-041` — the one JSON serializer used throughout, chosen over `Newtonsoft.Json` |
| OAuth 2.0 Token Introspection | [RFC 7662](https://datatracker.ietf.org/doc/html/rfc7662) | `ADR-040` — resolving an opaque, URL-embeddable ticket back to the original bearer token's claims; extended here with a non-standard `sig` parameter, not bare spec compliance |
| HMAC-signed URL / CDN token authentication (industry convention, no single governing RFC) | e.g. [Google Cloud Signed URLs](https://docs.cloud.google.com/cdn/docs/using-signed-urls), [AWS CloudFront signed URLs](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-signed-urls.html) | `ADR-040` — client-side HMAC-SHA256 over the ticket, appended to the target URL, for the one hop that can't carry a header |
| API Gateway pattern | [microservices.io — API Gateway](https://microservices.io/patterns/apigateway.html) | `ADR-049` — a single external entry point in front of this design's now-multiple independently-addressable services |
| OpenAPI/AsyncAPI Specification Extensions (`x-*` fields) | [OpenAPI 3.1 spec, §4.9.4](https://spec.openapis.org/oas/v3.1.1.html), [AsyncAPI 3.0 spec](https://www.asyncapi.com/docs/reference/specification/v3.0.0) (extension mechanism adapted directly from OpenAPI's, per AsyncAPI's own spec attribution) | `ADR-050` — `x-required-claims`/`x-masking` guaranteed to survive into generated docs |
| Microsoft.Extensions.Compliance.Redaction / Telemetry | [learn.microsoft.com/dotnet/core/extensions/data-redaction](https://learn.microsoft.com/en-us/dotnet/core/extensions/data-redaction) | `ADR-050` — log redaction reusing `ADR-009`'s existing masking classification metadata — see `docs/libraries/dotnet/compliance-redaction.md` |
| YARP | [dotnet/yarp](https://github.com/dotnet/yarp) | `ADR-049` — the concrete reverse-proxy library. **Previously reference-only, rejected** ("no gateway/reverse-proxy tier exists... each host is a single deployable") — un-rejected once this design's growth to multiple external surfaces created the exact scenario the rejection named as the reason to revisit — see `docs/libraries/dotnet/yarp.md` |
| Kiota | [github.com/microsoft/kiota](https://github.com/microsoft/kiota) | `ADR-054` — OpenAPI-based client SDK generation for both .NET and TypeScript consumers, from one spec; picked over `NSwag`/`OpenAPI Generator` for being Microsoft's own first-party tool (`ADR-041`) — see `docs/libraries/dotnet/kiota.md` |
| Strawberry Shake | [chillicream.com/docs/strawberryshake](https://chillicream.com/docs/strawberryshake/) | `ADR-054` — GraphQL client SDK generation for .NET consumers, same vendor (ChilliCream) as server-side `HotChocolate` — see `docs/libraries/dotnet/strawberry-shake.md` |
| GraphQL Code Generator | [the-guild.dev/graphql/codegen](https://the-guild.dev/graphql/codegen) | `ADR-054` — GraphQL client SDK generation for TypeScript consumers, the standard tool for that ecosystem — see `docs/libraries/web/graphql-code-generator.md` |
| MSTest | [learn.microsoft.com/dotnet/core/testing/unit-testing-mstest-intro](https://learn.microsoft.com/en-us/dotnet/core/testing/unit-testing-mstest-intro) | `ADR-055` — backend unit-test framework, Microsoft's own first-party choice — see `docs/libraries/dotnet/mstest.md` |
| Moq | [github.com/devlooped/moq](https://github.com/devlooped/moq) | `ADR-055` — mocking library, per direct preference. **Caveat recorded, not silently dropped**: the 2023 `SponsorLink` telemetry incident caused lasting community trust damage; technically resolved, but many teams permanently migrated to `NSubstitute` — see `docs/libraries/dotnet/moq.md` |
| Vitest | [vitest.dev](https://vitest.dev/) | `ADR-055` — frontend unit-test runner, maintained by the Vue/Vite team, zero extra config for `ADR-039`'s already-Vite-based client — see `docs/libraries/web/vitest.md` |
| Vue Test Utils | [test-utils.vuejs.org](https://test-utils.vuejs.org/) | `ADR-055` — Vue's own official component-testing library — see `docs/libraries/web/vue-test-utils.md` |
| Playwright | [playwright.dev/dotnet](https://playwright.dev/dotnet/) | `ADR-055` — cross-browser UI action/E2E tests, Microsoft's own, with MSTest base classes — see `docs/libraries/dotnet/playwright-dotnet.md` |
| ASP.NET Core Rate Limiting middleware | [learn.microsoft.com/aspnet/core/performance/rate-limit](https://learn.microsoft.com/en-us/aspnet/core/performance/rate-limit) | `ADR-058` — per-tenant rate limiting/quota, first-party, four built-in algorithms — see `docs/libraries/dotnet/aspnetcore-ratelimiting.md` |
| Crypto-shredding (cryptographic erasure) | [Mathias Verraes — Eventsourcing Patterns: Crypto-Shredding](https://verraes.net/2019/05/eventsourcing-patterns-throw-away-the-key/) | `ADR-057` — the canonical event-sourcing write-up of per-subject-key encryption + key destruction as erasure; also independently arrived at by [HashiCorp's own GDPR-compliant event-sourcing pattern](https://www.hashicorp.com/en/resources/gdpr-compliant-event-sourcing-with-hashicorp-vault) using Vault as the key backend |
| GDPR Art. 17 recognition of cryptographic erasure | European Data Protection Board Guidelines 5/2019; UK ICO; French CNIL (industry-summarized, no single public English-language primary text found for all three) | `ADR-057` — regulatory basis for treating irreversible key destruction as valid erasure, conditioned on strong encryption (AES-256), auditable destruction |
| Standard Webhooks specification | [github.com/standard-webhooks/standard-webhooks](https://github.com/standard-webhooks/standard-webhooks/blob/main/spec/standard-webhooks.md) | `ADR-060` — outbound webhook header/HMAC-signing/retry conventions, adopted directly rather than inventing a fourth signing scheme |
| Toxiproxy | [testcontainers.com/modules/toxiproxy](https://testcontainers.com/modules/toxiproxy/) | Candidate in `docs/comparisons/distributed-correctness-testing.md` — network-level fault injection for `ADR-033`'s replication convergence, alongside the already-adopted `Testcontainers` | 
| Jepsen | [jepsen.io](https://jepsen.io/) | Candidate in `docs/comparisons/distributed-correctness-testing.md` — the industry gold-standard black-box distributed-consistency verification tool; not yet adopted, named as an available escalation |
| FsCheck | [fscheck.github.io/FsCheck](https://fscheck.github.io/FsCheck/) | `ADR-063` — property-based testing, adopted now (staged) for `ADR-019`/`ADR-024`'s pure-logic invariants — see `docs/libraries/dotnet/fscheck.md` |
| Polly + Simmy | [github.com/App-vNext/Polly](https://github.com/App-vNext/Polly/blob/main/docs/chaos/index.md) | `ADR-063` — in-process fault injection, adopted now (staged) for `ADR-033`/`ADR-039`'s outbox crash-recovery path — see `docs/libraries/dotnet/polly-simmy.md` |
| SemVer 2.0.0 | [semver.org](https://semver.org/) | `ADR-062` — versioning policy for every published `EventStore.*` NuGet/npm package, once the framework is package-distributed rather than forked |
| Azure Cosmos DB data residency | [learn.microsoft.com/azure/cosmos-db/data-residency](https://learn.microsoft.com/en-us/azure/cosmos-db/data-residency) | `ADR-061` — prior art for explicit region-allowlisted replication, informing the per-`AppId` `AllowedRegions` shape |
| MongoDB Atlas Global Clusters | [mongodb.com/docs/atlas/architecture — Global Data](https://www.mongodb.com/docs/atlas/architecture/current/deployment-paradigms/global-data/) | `ADR-061` — prior art for folding region into sharding/replication assignment; this design keeps `ADR-034`'s `EntityType` shard key unchanged and layers region onto replication destination instead of the key itself |
| FDA 21 CFR Part 11 (electronic records/signatures) | [21 CFR Part 11](https://www.ecfr.gov/current/title-21/chapter-I/subchapter-A/part-11) | **Adopted.** `ADR-066` — §11.50's three linked signature-meaning elements (`SignerId`/`SignedAt`/`Meaning`), satisfied by the envelope `Signature` object; `ADR-063` — §11.10(a) (system validation, ability to discern invalid/altered records), satisfied by `FsCheck`'s hash-chain tamper-detection property; `ADR-068` — §11.10(e) (audit trail must not obscure previously recorded information), satisfied by the bitemporal "corrections land in place" playback rule |
| Bitemporal modeling (valid time vs. transaction time) | Snodgrass et al., TSQL2; standardized in [SQL:2011](https://www.researchgate.net/publication/261845780_Temporal_features_in_SQL2011); shipped as [SQL Server's `FOR SYSTEM_TIME AS OF`](https://learn.microsoft.com/en-us/sql/relational-databases/tables/temporal-tables?view=sql-server-ver17) | `ADR-068` — names the exact distinction between `ADR-021`'s valid-time-corrected `EntityStoreRow` and the new transaction-time ("system-time") playback query this ADR adds; `OccurredAt` is valid time, `SequenceNumber` is transaction time, both already existed, only the system-time query was missing |
| WebUSB | [MDN — WebUSB API](https://developer.mozilla.org/en-US/docs/Web/API/WebUSB_API) | `ADR-070` — raw USB device access from the MVVM client; Chromium desktop+Android only, ~76% global support |
| Web Serial API | [MDN — Web Serial API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Serial_API) | `ADR-070` — serial/COM-port device access; Chromium desktop only, ~72% global support |
| WebHID | [MDN — WebHID API](https://developer.mozilla.org/en-US/docs/Web/API/WebHID_API) | `ADR-070` — HID-class device access; Chromium desktop only, ~27% global support, the weakest of the four |
| Web Bluetooth API | [WebBluetoothCG/web-bluetooth](https://github.com/WebBluetoothCG/web-bluetooth) | `ADR-070` — BLE device access; Chromium desktop+Android only. Mozilla has explicitly declined it on its standards-positions list; WebKit ships no Web Bluetooth code at all |
| Local native-bridge pattern for unsupported browsers (e.g., "Haven Connect" for iOS/Safari BLE health devices) | industry pattern, no single governing spec | `ADR-070` — a companion app with real OS-level hardware access, exposing a local WebSocket/HTTP server the web app talks to instead, closing the Firefox/Safari gap the four browser APIs leave open |
| Standalone offline evidence viewers (e-discovery/forensics industry convention) | [MetaDiscovery](https://metadigitalforensics.com/) (self-executing load-file viewer), [OSForensics HTML case reports](https://www.osforensics.com/generate-reports.html), [SANS EZViewer](https://www.sans.org/tools/ezviewer) (zero-dependency standalone) | `ADR-068` — prior art confirming a self-contained, install-free offline viewer is the industry-standard delivery format for this exact use case, not a novel idea |
| vite-plugin-singlefile | [github.com/richardtallent/vite-plugin-singlefile](https://github.com/richardtallent/vite-plugin-singlefile) | `ADR-068` — inlines a Vite build into one static HTML file for the offline litigation-review player — see `docs/libraries/web/vite-plugin-singlefile.md` |
| Web Periodic Background Synchronization API | [MDN — Web Periodic Background Synchronization API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Periodic_Background_Synchronization_API) | `ADR-069` — candidate for the "scheduled phone-home" outbox-flush trigger; checked and found Chromium-only, experimental, not Baseline, unsupported in Firefox/Safari — used where available, not assumed |
| HL7 v2 / MLLP | [Minimal Lower Layer Protocol — Wikipedia](https://en.wikipedia.org/wiki/Minimal_Lower_Layer_Protocol), [Google Cloud's MLLP adapter](https://github.com/GoogleCloudPlatform/mllp/) | `ADR-072` — inbound EMR/hospital integration; MLLP is the real, verified transport nearly every production HL7v2 interface uses (Mirth Connect, Rhapsody, Cloverleaf), not HTTP |
| HL7 FHIR | [hl7.org/fhir](https://hl7.org/fhir/) | `ADR-072` — RESTful/HTTP-native alternative to HL7v2 for newer EMR integrations, no MLLP bridge needed |
| PCI-DSS Requirement 3.2/3.2.2 (Sensitive Authentication Data non-persistence) | [PCI Security Standards Council FAQ](https://blog.pcisecuritystandards.org/faq-can-cvc-be-stored-for-card-on-file-or-recurring-transactions), [PCI DSS Requirement 3 explained](https://pcidssguide.com/pci-dss-requirement-3/) | `ADR-071` — CVV2/CVC2/CID, full track data, and PIN blocks may never be stored after authorization, even encrypted, a hard incompatibility with `ADR-023`'s persist-everything posture that only a registration-time boundary (not a storage-layer mechanism) can resolve |
| SEC Rule 17a-4 (broker-dealer electronic recordkeeping — WORM / audit-trail alternative) | [SEC — Amendments to Electronic Recordkeeping Requirements for Broker-Dealers](https://www.sec.gov/investment/amendments-electronic-recordkeeping-requirements-broker-dealers) | `ADR-071` — a confirming, non-action finding: the rule's 2022–2023 audit-trail alternative to strict WORM format is already satisfied by `ADR-019`'s hash-chained, append-only Event Log, if brokerage is ever built as a proving-ground domain |
| Executive Order 14028 (SBOM requirement for federal software) | [NIST — Software Security in Supply Chains](https://www.nist.gov/itl/executive-order-14028-improving-nations-cybersecurity/software-security-supply-chains-software-1) | `ADR-074` — SBOMs in NTIA/CISA-accepted formats (SPDX, CycloneDX, SWID) |
| FDA Section 524B (medical device cybersecurity/SBOM) | [FD&C Act §524B, effective March 2023](https://censinet.com/perspectives/ultimate-guide-sboms-fda-regulated-devices) | `ADR-074` — machine-readable SBOM required for any "cyber device" premarket submission, directly relevant to the clinical-trials proving-ground domain |
| IEC 62304 (medical device software life cycle — SOUP) | [IEC 62304 SOUP list guidance](https://openregulatory.com/document_templates/soup-list-software-of-unknown-provenance) | `ADR-074` — every adopted library is SOUP by this standard's own definition; `docs/libraries/README.md` formalized as the SOUP list |
| microsoft/sbom-tool | [github.com/microsoft/sbom-tool](https://github.com/microsoft/sbom-tool) | `ADR-074` — first-party SPDX 2.2 SBOM generator, auto-detects both NuGet and npm dependency graphs |
| Dependabot | [GitHub-native dependency alerts/PRs](https://github.com/dependabot) | `ADR-080` — vulnerability scanning across both `ADR-062`'s NuGet and npm ecosystems |
| SLSA (Supply-chain Levels for Software Artifacts) | [slsa.dev](https://slsa.dev/spec/v1.0/about) | `ADR-080` — Level 2 (signed provenance + dedicated build infrastructure) adopted as the target; Level 3 (hermetic/ephemeral builds) named as a future escalation, not decided |
| Sigstore / npm package provenance | [docs.npmjs.com/generating-provenance-statements](https://docs.npmjs.com/generating-provenance-statements/) | `ADR-080` — `npm publish --provenance` generates a Sigstore-signed SLSA attestation logged to the public Rekor transparency log; requires publishing from GitHub Actions or GitLab CI/CD specifically |
| NuGet author package signing | [learn.microsoft.com/nuget/create-packages/sign-a-package](https://learn.microsoft.com/en-us/nuget/create-packages/sign-a-package) | `ADR-080` — every published NuGet package signed with a registered X.509 certificate, validated by NuGet.org at submission |
| RFC 3161 (Time-Stamp Protocol) | [datatracker.ietf.org/doc/html/rfc3161](https://datatracker.ietf.org/doc/html/rfc3161) | `ADR-086` — a TSA's `TimeStampToken` over an event's `ChainHash`, independently proving `ADR-066` signatures/`ADR-068` litigation exports existed at or before a given time |
| RFC 9110 §12 (HTTP Accept-Language content negotiation) | [datatracker.ietf.org/doc/html/rfc9110#section-12](https://datatracker.ietf.org/doc/html/rfc9110) | `ADR-087` — standard locale negotiation, not a bespoke locale parameter/header |
| CSS Logical Properties and Values (W3C) | [w3.org/TR/css-logical-1](https://www.w3.org/TR/css-logical-1/) | `ADR-087` — RTL-correct layout via `margin-inline-start`-style properties, not a second mirrored stylesheet |
| WCAG 2.1/2.2 AA, ADA Title II | [DOJ ADA Title II final rule](https://www.ada.gov/resources/web-rule-first-steps/) | `ADR-073` — accessibility standard, checked and confirmed as a real, already-in-effect requirement (April 2026 deadline for larger entities) |
| GDPR Article 17(3) (right to erasure — exemptions) | [gdpr-info.eu/art-17-gdpr](https://gdpr-info.eu/art-17-gdpr/) | `ADR-066` — 17(3)(b) (legal-obligation compliance) and 17(3)(e) (establishment/exercise/defence of legal claims) are the reasoned basis for `SignerId`/`Signature` being categorically exempt from `ADR-057`'s erasure, not merely unreachable by accident |
| RFC 9470 (OAuth 2.0 Step Up Authentication Challenge Protocol) | [rfc-editor.org/rfc/rfc9470](https://www.rfc-editor.org/rfc/rfc9470.html) | `ADR-066` — the `acr_values`/`max_age` challenge mechanism for regulated digital sign-off actions; the framework never implements password/OTP/WebAuthn verification itself, only the challenge/enforcement around whatever the IdP (`ADR-006`) supports |
| Password-field reveal toggle (show/hide, WCAG-accessible) | [uxpatterns.dev — Password pattern](https://uxpatterns.dev/patterns/forms/password) | `ADR-009`'s `revealOnDemand` — generalizes the standard masked-input-with-reveal-toggle UX pattern from passwords to any classified field, for shoulder-surfing mitigation distinct from claims-based access masking |
| CouchDB filtered replication | [Couchbase — Offline data storage and sync with PouchDB](https://www.couchbase.com/blog/introduction-offline-data-storage-sync-pouchdb-couchbase/) | `ADR-065` — long-established prior art for syncing only a query-scoped subset to an offline-capable client; this design reuses its own existing `FilterableFields`/GraphQL Subscription filtering rather than adopting CouchDB/PouchDB itself |
| NIST SP 800-53 Rev. 5 — IA-2 (Identification and Authentication) | [csf.tools — IA-2](https://csf.tools/reference/nist-sp-800-53/r5/ia/ia-2/) | `ADR-006` — the foundational identity-verification step every later audit/access-control mechanism (`ADR-045`, `ADR-046`, `ADR-064`) depends on |
| NIST SP 800-53 Rev. 5 — AC-2 (Account Management) / AC-3 (Access Enforcement) | [csf.tools — AC-2](https://csf.tools/reference/nist-sp-800-53/r5/ac/ac-2/), [AC-3](https://csf.tools/reference/nist-sp-800-53/r5/ac/ac-3/) | `ADR-008` (per-event-type enforcement point), `ADR-046` (RBAC — already cited this control family inline, now given its own row alongside the `AC-5` row above) |
| NIST SP 800-63B — §5.2.5 (verifier-impersonation resistance) / §5.2.8 (authenticator replay resistance) | [pages.nist.gov/800-63-3/sp800-63b.html](https://pages.nist.gov/800-63-3/sp800-63b.html) | `ADR-017` — DPoP as a real identity-assurance uplift, not just defense-in-depth |
| SOC 2 (AICPA Trust Services Criteria) / ISO/IEC 27001 | [AICPA — SOC 2](https://www.aicpa-cima.com/resources/landing/system-and-organization-controls-soc-suite-of-services), [ISO/IEC 27001](https://www.iso.org/standard/27001) | `ADR-030` — baseline multi-tenant SaaS expectation, tied to `AppId`-scoped logical tenant isolation |
| HIPAA Security Rule — Access Control, 45 CFR §164.312(a) | [law.cornell.edu/cfr/text/45/164.312](https://www.law.cornell.edu/cfr/text/45/164.312) | `ADR-031` — claims-gated redaction of recorded PHI in streaming channels |
| HIPAA Security Rule — Contingency Plan, 45 CFR §164.308(a)(7) (incl. Data Backup Plan, §164.308(a)(7)(ii)(A)) | [law.cornell.edu/cfr/text/45/164.308](https://www.law.cornell.edu/cfr/text/45/164.308) | `ADR-033` (minimum replication factor of 2), `ADR-056` (authoritative-store backup classification) |
| FDA 21 CFR §314.80 / §600.80 (postmarketing adverse drug/biologic experience reporting) | [law.cornell.edu/cfr/text/21/314.80](https://www.law.cornell.edu/cfr/text/21/314.80), [§600.80](https://www.law.cornell.edu/cfr/text/21/600.80) | `ADR-035` — `AuthorityStatus`'s unattested→pending_review→accepted lifecycle, matching this ADR's pharmacovigilance-domain fit |
| HIPAA Privacy Rule — Minimum Necessary Standard, 45 CFR §164.502(b) | [law.cornell.edu/cfr/text/45/164.502](https://www.law.cornell.edu/cfr/text/45/164.502) | `ADR-037` (PII/PHI kept out of URLs/logs), `ADR-050` (log-redaction sink), `ADR-060` (fixed-per-subscription claim set masking webhook payloads before external delivery) |
| GDPR Art. 5(1)(f) (integrity and confidentiality principle) | [gdpr-info.eu/art-5-gdpr](https://gdpr-info.eu/art-5-gdpr/) | `ADR-037`, `ADR-050` |
| HIPAA Security Rule — Transmission Security, 45 CFR §164.312(e) | [law.cornell.edu/cfr/text/45/164.312](https://www.law.cornell.edu/cfr/text/45/164.312) | `ADR-040` — addressable integrity/encryption specs, tied to the HMAC-signed single-use ticket mechanism |
| FDA "Data Integrity and Compliance With Drug CGMP" guidance (ALCOA+) | [FDA guidance document](https://www.fda.gov/regulatory-information/search-fda-guidance-documents/data-integrity-and-compliance-drug-cgmp-questions-and-answers) | `ADR-042` — the "Complete" ALCOA+ principle (rejected results stay retained/reviewable, never silently excluded), matching the non-authoritative Live View's visible-not-blocked shape |
| NIST SP 800-63C (Federation Assurance Level, FAL) | [pages.nist.gov/800-63-3/sp800-63c.html](https://pages.nist.gov/800-63-3/sp800-63c.html) | `ADR-047` — assertion-integrity requirement behind the "augments, never replaces" rule for federated claims |
| NIST SP 800-207 (Zero Trust Architecture) / SP 800-207A | [csrc.nist.gov/pubs/sp/800/207/final](https://csrc.nist.gov/pubs/sp/800/207/final) | `ADR-048` — names SPIFFE directly as reference Zero Trust infrastructure |
| NIST SP 800-171 / CMMC | [csrc.nist.gov/pubs/sp/800/171/r3/final](https://csrc.nist.gov/pubs/sp/800/171/r3/final) | `ADR-048` — relevant to ITAR/defense-scoped deployments of SPIFFE-based service identity; already used narratively in `docs/domains/itar-export-controlled-defense-data/README.md` |
| NIST SP 800-53 Rev. 5 — SC-7 (Boundary Protection) | [csf.tools — SC-7](https://csf.tools/reference/nist-sp-800-53/r5/sc/sc-7/) | `ADR-049` — the control family that names a managed gateway as the required external interface |
| HIPAA Safe Harbor de-identification, 45 CFR §164.514(b)(2) (18-identifier list) | [law.cornell.edu/cfr/text/45/164.514](https://www.law.cornell.edu/cfr/text/45/164.514) | `ADR-052` — identifier #17 ("full-face photographic images and comparable images"), tied to the video-channel blank-frame redaction default |
| SOX Section 404 — IT General Controls (program change management domain) | [SEC — Sarbanes-Oxley Section 404](https://www.sec.gov/rules/final/33-8238.htm) | `ADR-038` (Expand/Contract/N-1/N+1/rollback discipline as the change-management control); confirmed a non-gap for `ADR-045`/`019`/`067` in the brokerage-domain review |
| Federal Rules of Evidence 902(13)/(14) (self-authentication of machine-generated/digitally-identified electronic records) | [law.cornell.edu/rules/fre/rule_902](https://www.law.cornell.edu/rules/fre/rule_902) | `ADR-068` — the offline player's independent hash re-verification |
| Anti-Corruption Layer | [Eric Evans — *Domain-Driven Design*](https://www.domainlanguage.com/ddd/); [Azure Architecture Center — Anti-Corruption Layer](https://learn.microsoft.com/en-us/azure/architecture/patterns/anti-corruption-layer) | `ADR-072`'s `IInterchangeFormatAdapter` (HL7v2/FHIR inbound, ICH E2B(R3)/GS1-EPCIS outbound) — see `docs/patterns/anti-corruption-layer.md` |
| Test Pyramid | [Mike Cohn — *Succeeding with Agile*](https://www.mountaingoatsoftware.com/blog/the-forgotten-layer-of-the-test-automation-pyramid); [Martin Fowler — TestPyramid](https://martinfowler.com/bliki/TestPyramid.html) | `ADR-055` — the shape behind unit/integration/E2E/UI test-layer proportions — see `docs/patterns/test-pyramid.md` |
| Chaos Engineering | [principlesofchaos.org](https://principlesofchaos.org/) | `ADR-063` — the discipline `Polly`+`Simmy`'s in-process fault injection implements — see `docs/patterns/fault-injection-chaos-engineering.md` |
| Azure Key Vault | [learn.microsoft.com — Key Vault overview](https://learn.microsoft.com/en-us/azure/key-vault/general/overview) | `ADR-057` — one of four keyed `IErasureKeyStore` cloud backends — see `docs/libraries/dotnet/azure-key-vault.md` |
| AWS KMS | [aws.amazon.com/kms](https://aws.amazon.com/kms/) | `ADR-057` — see `docs/libraries/dotnet/aws-kms.md` |
| Google Cloud KMS | [cloud.google.com — Cloud KMS](https://cloud.google.com/security/products/security-key-management) | `ADR-057` — see `docs/libraries/dotnet/google-cloud-kms.md` |
| HashiCorp Vault | [vaultproject.io](https://www.vaultproject.io/) | `ADR-057` — the on-prem/self-hosted `IErasureKeyStore` backend, independently validated by HashiCorp's own GDPR-compliant event-sourcing write-up (row above) — see `docs/libraries/dotnet/hashicorp-vault.md` |
| AWS Well-Architected SaaS Lens — Silo Isolation | [docs.aws.amazon.com — Silo isolation](https://docs.aws.amazon.com/wellarchitected/latest/saas-lens/silo-isolation.html) | `ADR-075` — real-world naming/rationale for dedicated-per-tenant deployment ("supporting challenging compliance models," "reduced scope of impact") |
| Azure Architecture Center — Tenancy models for a multitenant solution | [learn.microsoft.com — tenancy-models](https://learn.microsoft.com/en-us/azure/architecture/guide/multitenant/considerations/tenancy-models) | `ADR-075` — pool/silo/bridge terminology, and the Deployment Stamps pattern behind "Automated single-tenant deployments" |
| AS2 / AS4 (B2B EDI transport) | [SEEBURGER — AS4](https://www.seeburger.com/resources/good-to-know/what-is-as4), [Cleo — AS2](https://www.cleo.com/blog/demystifying-as2-certificates) | `ADR-075` — prior art for independently-owned systems exchanging documents point-to-point with no shared database, informing tenant-to-tenant federation |
| Federated architecture (health-information-exchange pattern) | [Federated EHR interoperability framework](https://everant.org/index.php/etj/article/view/1887) | `ADR-075` — each institution keeps its own data, exchanges specific records via standard APIs, never a central repository — the pattern `ADR-060`/`ADR-072` already implement for cross-tenant federation |
| The rsync algorithm | Tridgell & Mackerras, [ANU TR-CS-96-05](https://rsync.samba.org/tech_report/tech_report.html) | `ADR-032` — considered and distinguished from content-defined chunking: fixed-block-plus-rolling-search, good for pairwise diff-transfer of one already-known file pair, not adopted for multi-party chunk-level dedup |
| Content-Defined Chunking (restic, Borg Backup, casync) | [restic references](https://restic.readthedocs.io/en/latest/100_references.html), [Borg data structures](https://borgbackup.readthedocs.io/en/stable/internals/data-structures.html), [casync](https://github.com/systemd/casync) | `ADR-032` — the verified real precedent for `ChunkIndex`: a rolling hash (Buzhash/Rabin fingerprint) finds content-defined chunk boundaries, each chunk fingerprinted with SHA-256 |
| BitTorrent piece-hashing (BEP 3) | [bittorrent.org — BEP 3](https://www.bittorrent.org/beps/bep_0003.html) | `ADR-032` — considered and rejected in favor of content-defined chunking: fixed-size pieces don't survive insertions/deletions without re-hashing everything downstream |
| IPFS / UnixFS chunking | [specs.ipfs.tech — UnixFS](https://specs.ipfs.tech/unixfs/) | `ADR-032` — considered alongside BitTorrent as a fixed/target-sized chunking precedent, not content-defined by default |

## Reference-only — considered, not adopted

Recorded here for the same reason `README.md` states its own "deliberately
is not" scope decisions plainly rather than silently: so a reader doesn't
have to wonder whether something was overlooked versus deliberately left
out.

| Concept | Standard / spec | Why it's not in this design |
|---|---|---|
| Transactional Outbox pattern | [microservices.io — Transactional Outbox](https://microservices.io/patterns/data/transactional-outbox.html) | Still not needed for its classic purpose — "how do I atomically update my own row *and* publish an event about it" (the dual-write problem) — since `Events` **is** the log Follow tails directly; publish and append are the same write, no second system to keep in sync with (`ADR-015`'s consequences touch on this from the projection side). **Disambiguation, not a reversal**: this design's own "outbox" terminology elsewhere (`ADR-033`'s peer-sync outbox/inbox, `ADR-039`'s client-local outbox) is a durable-retry-queue primitive, a different problem from the classic Transactional Outbox's dual-write atomicity — same word, two different mechanisms, per this design's own "disambiguate terminology collisions" convention. |
| MongoDB Atlas Device Sync | [MongoDB — Realm/Atlas Device Sync deprecation notice](https://www.mongodb.com/docs/atlas/app-services/sync/) | Checked as candidate prior art for `ADR-065`'s local/edge active-scope sync; found deprecated September 2024 and shut down September 2025 — not a live option, correctly not adopted |
| DNS-SD / mDNS service discovery | [RFC 6763](https://datatracker.ietf.org/doc/html/rfc6763), [RFC 6762](https://datatracker.ietf.org/doc/html/rfc6762) | Still not adopted: `ADR-033`'s cross-site peer discovery (how a new peer server finds other sites) is a related, real question orchestration alone doesn't answer once sites are independently deployed — but mDNS/DNS-SD are LAN-scoped discovery protocols, the wrong tool for cross-internet, cross-site discovery specifically, so they don't become the answer just because a related need appeared. Resolved instead by `ADR-051` (static seed-peer list) — see `docs/comparisons/peer-discovery.md`. |
| C4-PlantUML | [plantuml-stdlib/C4-PlantUML](https://github.com/plantuml-stdlib/C4-PlantUML) | **Previously adopted, now rejected.** Every `C4_Context`/`C4_Container`/`C4_Component` macro requires an `!include` — either a live fetch of the library's `.puml` files from `raw.githubusercontent.com`, or a bundled copy of the PlantUML standard library at a fixed `<C4/...>` path. Both fail silently (a blank or broken diagram, no readable error) in any renderer without internet access or without that stdlib path configured, which turned out to be most local/offline PlantUML setups actually used against this repo — happened repeatedly, not once. `01-c4-architecture.md`'s diagrams (and the two C4-styled component diagrams in `docs/patterns/cqrs-and-materialized-views.md`/`mvvm-client-architecture.md`) now hand-style plain PlantUML (`rectangle`/`database` + stereotypes + `skinparam` color tiers) into the same Context/Container/Component notation instead — no `!include` of any kind, anywhere, is now a standing project-wide convention (`CLAUDE.md`), not just a fix for this one file. |
| Strangler Fig migration pattern | [Martin Fowler — StranglerFigApplication](https://martinfowler.com/bliki/StranglerFigApplication.html) | This is a from-scratch design, not a legacy migration — there's nothing being strangled. |
| Capability tokens (Macaroons/Biscuit) | [Macaroons (Google/NDSS 2014)](https://research.google/pubs/macaroons-cookies-with-contextual-caveats-for-decentralized-authorization-in-the-cloud/), [Biscuit](https://doc.biscuitsec.org/reference/specifications.html) | **Reasoning updated, still not adopted.** This entry's own stated bar for reconsidering — "a caller needing to mint a narrower credential for someone else without going back to the IdP" — has since arisen (`ADR-043`'s delegated access grants). It was filled by UCAN (already adopted, `ADR-036`), not Macaroons/Biscuit: UCAN's attenuated-delegation-chain model already covers the exact same territory these would have, so adding a second capability-token format alongside it would be redundant, not additive. |
| Local-first software / CRDTs | [Ink & Switch — Local-first software](https://www.inkandswitch.com/essay/local-first/), [Automerge](https://automerge.org/docs/hello/), [Yjs](https://docs.yjs.dev/) | Re-examined after `ADR-033` (multi-origin replication) and `ADR-039` (offline-first client outbox) — both are genuinely local-first/concurrent-write scenarios this rejection's original reasoning predates. Still not adopted: this design resolves concurrent writes via flagged stream-order LWW (`ADR-024`'s `ConflictFlag`, reused unchanged for cross-server divergence), not field-level CRDT merge functions — richer per-field policies (summing deltas, etc.) are reserved case-by-case for specifically contentious fields (`docs/patterns/optimistic-concurrency.md`), not adopted as a system-wide default. |
| WebDAV, entirely | [RFC 4918](https://datatracker.ietf.org/doc/html/rfc4918) | Considered for `ADR-032`'s browsable attachment access, decided against — see `docs/comparisons/webdav-library.md`. Every library option was flawed (NWebDav archived, Dav.AspNetCore.Server thin, IT Hit commercial), and once the attachment store's other access paths were confirmed already served by plain HTTP + GraphQL, WebDAV's one unique value (OS-native mounting) wasn't worth any of the three trade-offs. |
| NWebDav | [github.com/ramondeklein/nwebdav](https://github.com/ramondeklein/nwebdav) | Considered for `ADR-032`; repository archived (read-only) July 19, 2025, disqualifying it outright regardless of the WebDAV-entirely decision above — `docs/comparisons/webdav-library.md` |
| Dav.AspNetCore.Server | [github.com/ThuCommix/Dav.AspNetCore.Server](https://github.com/ThuCommix/Dav.AspNetCore.Server) | Considered for `ADR-032` — free and current, but thin (3 stars/7 forks) for security-relevant surface area; moot once WebDAV itself was declined — `docs/comparisons/webdav-library.md` |
| IT Hit WebDAV Server Engine for .NET | [webdavsystem.com/server](https://www.webdavsystem.com/server/) | Considered for `ADR-032` — the only genuinely well-maintained WebDAV library found (v14+, full cross-platform ASP.NET Core support), but commercial/paid, which would have been this design's first paid dependency — `docs/comparisons/webdav-library.md` |
| Chain-of-Responsibility (rendering fallback) | *Design Patterns* (Gamma, Helm, Johnson, Vlissides), 1994 | **Reasoning updated, still not adopted.** This design now has a real UI/rendering layer (`ADR-039`'s MVVM client, `features/mvvm-client.md`'s real Salt mockup) — the original "no UI layer at all" reasoning is stale. Still not Chain-of-Responsibility specifically: `ADR-039`'s view-definition fallback is a simple two-tier "specific view, else generic" choice, not a chain of handlers each deciding whether to pass a request along — the defining property Chain-of-Responsibility actually needs. |
| Four Eyes Principle / two-person rule / dual control | [goteleport.com — Four Eyes Principle](https://goteleport.com/blog/four-eyes-principle/) | Named by the requester for `ADR-043`, but not what got built — the classical principle requires two people to jointly approve *one* action; `ADR-043` is one person unilaterally delegating access to another. Cited for disambiguation, not adopted. **Reconsidered for `AppTrustRoot` registration** (`docs/comparisons/trust-root-registration-gate.md`) — a genuinely closer fit there (one discrete, high-stakes action, not a delegation) but still not adopted as a system-wide requirement, since that registration's blast radius stays contained to one `AppId`'s own namespace rather than the whole system's trust root. |
| PKI root/intermediate CA key ceremony — dual control, split knowledge, M-of-N quorum | [Encryption Consulting — Root CA Key Ceremony](https://www.encryptionconsulting.com/education-center/root-ca-key-ceremony/), [APNIC Blog — What is "M of N"](https://blog.apnic.net/2021/05/28/what-is-m-of-n-in-public-private-key-signing/) | Considered for `AppTrustRoot` registration (`docs/comparisons/trust-root-registration-gate.md`) as the closest real analogue to "minting a root of trust" — not adopted, since ceremony-grade quorum is disproportionate to an `AppTrustRoot`'s per-`AppId`-contained blast radius (unlike a root CA, whose compromise cascades across an entire certificate hierarchy). |
| CAS (Central Authentication Service) protocol | [Apereo CAS Protocol](https://apereo.github.io/cas/), [Wikipedia — CAS](https://en.wikipedia.org/wiki/Central_Authentication_Service) | Informs `ADR-040`'s shape (URL-embeddable service ticket, validated by the receiving service calling back to a trusted backend) but not adopted wholesale — no browser-redirect SSO login flow, no XML `serviceValidate` response format; the actual mechanism is built from RFC 8693 + RFC 7662 instead |
| Raw `access_token` as a URL query parameter (SignalR/Azure SignalR convention, for browser WebSocket/SSE clients that can't set headers) | [ASP.NET Core SignalR — Authentication](https://learn.microsoft.com/en-us/aspnet/core/signalr/authn-and-authz) | Reference-only, and the specific anti-pattern `ADR-040` avoids repeating: this design already removed its own version of this (`ADR-006`'s original workaround, deleted by `ADR-012`) because a raw bearer token in a URL leaks via logs/history/`Referer`/caches; `ADR-040` solves the same header-incapable-client problem with an opaque single-use ticket instead |
| Off-the-shelf event-store products | [EventStoreDB](https://www.eventstore.com/eventstoredb), [Marten](https://martendb.io/) (.NET/Postgres) | Deliberately not adopted — building the write/read mechanism from scratch, end-to-end, is this project's stated purpose (`README.md`: "a worked example, not just a store"), not a gap a real product would fill. Worth knowing about if this pattern is ever needed for production rather than as a teaching example. **Expanded into a full competitive-landscape check** — see `docs/comparisons/build-vs-buy-platform.md` and the rows immediately below for every other candidate verified this pass. |
| Axon Server / Axon Framework | [axoniq.io](https://www.axoniq.io/) — [multitenancy](https://www.axoniq.io/blog/multitenancy-with-axon), [GDPR crypto-shredding module](https://www.prnewswire.com/news-releases/axoniq-delivers-gdpr-module-to-enable-mandatory-erasure-of-data-in-immutable-event-driven-systems-657197853.html) | Considered in `docs/comparisons/build-vs-buy-platform.md` — the closest single-product match to Duplex's core-engine-plus-erasure pairing (real event sourcing, CQRS, multi-tenant contexts, and a shipped GDPR erasure module); not adopted, since it's Java/gRPC-centric against this design's .NET-first-party stack (`ADR-041`) and has no GraphQL, gossip replication, bitemporal playback, or DID/UCAN |
| Confluent Platform / Kafka + Schema Registry | [Confluent — multi-tenant Schema Registry](https://docs.confluent.io/platform/current/schema-registry/fundamentals/index.html), [RBAC](https://docs.confluent.io/platform/current/schema-registry/security/rbac-schema-registry.html) | Considered — real multi-tenant schema governance, RBAC, and stream masking, but no entity-fold/`ExpectedVersion` model, no crypto-shredding erasure, no bitemporal query, and its "GraphQL API" is catalog search, not a business-data query surface |
| Amazon QLDB | [InfoQ — AWS discontinuing QLDB](https://www.infoq.com/news/2024/07/aws-kill-qldb/) | **Confirmed discontinued** (service ends July 31, 2025) — checked as the closest immutable-ledger analog, correctly excluded as a live candidate; AWS's own migration guidance (Aurora Postgres) doesn't preserve permanent immutability |
| Azure SQL Database Ledger | [Microsoft Learn — Ledger overview](https://learn.microsoft.com/en-us/sql/relational-databases/security/ledger/ledger-overview?view=sql-server-ver17) | Considered — real SHA-256 hash-chained tamper-evidence over relational tables, the closest verified analog to `ADR-019`'s own chain mechanism; not adopted as a replacement since it's a relational-table feature, not an event-sourced/entity-fold system |
| Datomic | [docs.datomic.com — releases](https://docs.datomic.com/releases.html) | Considered — immutable and time-aware, but confirmed **unitemporal** (transaction time only), not the valid-time-vs-transaction-time distinction `ADR-068` needs |
| XTDB v2 | [docs.xtdb.com — Time in XTDB](https://docs.xtdb.com/about/time-in-xtdb.html) | Considered — the one verified genuinely bitemporal engine found (SQL:2011 valid-time + system-time), a direct precedent for `ADR-068`'s bitemporal playback; not adopted since it has no schema registry, RBAC, masking, erasure, or GraphQL layer |
| Hyperledger Fabric, R3 Corda | [Hyperledger Fabric TOC report](https://toc.hyperledger.org/project-reports/2024/2024-Q2-Hyperledger-Fabric.html), [R3 Corda](https://r3.com/r3-announces-launch-of-corda-protocol-on-behalf-of-r3-foundation-to-bring-institutional-grade-curated-yield-to-solana/) | Considered — both are consensus-based permissioned DLTs, explicitly out of scope per this design's own gossip-replication-between-trusted-peers model (not a trustless network); neither offers GraphQL, bitemporal playback, or crypto-shredding natively |
| Microsoft Dataverse, Salesforce Shield | [Dataverse — GDPR DSR guide](https://learn.microsoft.com/en-us/power-platform/admin/dataverse-privacy-dsr-guide), [Salesforce Shield](https://www.salesforce.com/platform/shield/guide/) | Considered — broad RBAC/field-level-security/audit/GDPR-erasure enterprise platforms, but neither is event-sourced at the storage layer at all |
| JSONata (general JSON-to-JSON transform language) | [docs.jsonata.org](https://docs.jsonata.org/overview.html), .NET port: [`Jsonata.Net.Native`](https://www.nuget.org/packages/Jsonata.Net.Native/) | Seriously considered for `ADR-018`'s upcast mapping — genuinely the most "universal" option (independent implementations in JS/Java/Python/Go/C++/.NET/Rust) and a real fit for "path expressions with functional descriptions." Not adopted because it's strictly more general than the problem: an upcast mapping is always many-to-one/one-to-one (never a restructuring/fan-out), so its object/array-construction and looping constructs would never be exercised — [Jint](libraries/dotnet/jint.md)/[CEL](libraries/dotnet/cel-dotnet.md) (`ADR-037`, superseding this entry's original `compute()` framing once OData was swapped out) already cover the actual need, with no new dependency. **Resolved, build-plan item 11**: `Cel.NET` was spiked and verified working — see [the full comparison](comparisons/upcast-transform-language.md) and `docs/libraries/dotnet/cel-dotnet.md`. `Jsonata.Net.Native` remains the documented `ADR-053` alternative for array-aggregation upcasts CEL's macro set can't express natively, not a maturity fallback. |
| JMESPath (JSON query/reshape language) | [jmespath.org](https://jmespath.org/specification.html) | Same consideration as JSONata, one notch narrower (no user-defined functions/closures) — real, multi-language (used across the AWS SDKs), but not adopted for the same "more general than the problem" reason. |
| JOLT (declarative JSON-to-JSON spec) | [bazaarvoice/jolt](https://github.com/bazaarvoice/jolt) | Considered and ruled out — Java-only with no cross-language ports, and structural-only by design (its own docs say to "write code to fix values" for anything computed), which fails the "functional descriptions" requirement outright. |
| AutoMapper (convention/reflection-based object-to-object mapping) | [automapper.org](https://automapper.org/) | `ADR-041` — rejected in favor of small, explicit, hand-written mapping methods; this design's actual mappings are simple enough that convention-inference costs more in debuggability than it saves in typing. |
| `Newtonsoft.Json` (Json.NET) | [newtonsoft.com/json](https://www.newtonsoft.com/json) | `ADR-041` — rejected in favor of `System.Text.Json`; nothing in this design needs a `Newtonsoft.Json`-specific feature. |
| Serilog / NLog / log4net (third-party structured logging frameworks) | [serilog.net](https://serilog.net/), [nlog-project.org](https://nlog-project.org/) | `ADR-041` — rejected in favor of `Microsoft.Extensions.Logging` alone, already load-bearing via `ADR-026`'s Aspire/OpenTelemetry `ServiceDefaults`; a second logging framework underneath or alongside it would duplicate that pipeline, not improve it. |
| NIST SP 800-188 (De-Identifying Government Datasets: Techniques and Governance) | [csrc.nist.gov/pubs/sp/800/188/final](https://csrc.nist.gov/pubs/sp/800/188/final) | `docs/comparisons/masking-strategies.md` — cited to ground the redaction/pseudonymization/anonymization/masking terminology distinctions `ADR-009`'s feature had been using loosely; no de-identification mechanism from it is adopted, since this design retains every real value forever by design (`ADR-009`) rather than de-identifying a released dataset. |
| GDPR Article 4(5) / Recital 26 (pseudonymisation definition) | [Article 4(5) GDPR](https://gdprlocal.com/data-pseudonymisation-vs-anonymisation/) | `docs/comparisons/masking-strategies.md` — cited to disambiguate `ADR-009`'s claims-gated masking (access control on one stored value, no separate pseudonym artifact) from GDPR pseudonymisation proper (a substitute identifier plus a separately-held, access-controlled reversal key); not adopted, since nothing in this design generates or stores a separate pseudonym. |
| NIST SP 800-38G (FF1/FF3-1 format-preserving encryption) | [csrc.nist.gov/pubs/sp/800/38/g/upd1/final](https://csrc.nist.gov/pubs/sp/800/38/g/upd1/final) | `docs/comparisons/masking-strategies.md`'s format-preserving-masking option — genuine, standardized fit if that option is ever built, but not adopted now: no masking strategy beyond `FixedValue` is built (`ADR-009`), and real FF1/FF3-1 would be the first feature in this design needing symmetric-key management, which doesn't exist elsewhere here. |
| SQL Server Dynamic Data Masking `Partial()` function (industry pattern, vendor-specific, no governing spec) | [Microsoft Learn — Dynamic Data Masking](https://learn.microsoft.com/en-us/sql/relational-databases/security/dynamic-data-masking) | `docs/comparisons/masking-strategies.md`'s configurable-partial-reveal option — the concrete real-world precedent for a prefix/suffix reveal rule, generalizing `ADR-009`'s own sketched "keep the last N characters" `PartialReveal` proposal; not adopted as a mechanism itself (this design masks at the application layer per `ADR-009`, never via a database-native feature), only as the shape a future strategy value would copy. |
| k-anonymity (generalization/suppression for formal re-identification guarantees) | Sweeney, ["Achieving k-Anonymity Privacy Protection Using Generalization and Suppression,"](https://www.worldscientific.com/doi/abs/10.1142/S021848850200165X) *International Journal of Uncertainty, Fuzziness and Knowledge-Based Systems*, 2002 | `docs/comparisons/masking-strategies.md`'s generalization/bucketing option — not adopted: the formal k-anonymity guarantee needs dataset-wide analysis of which records are released together, which `ADR-009`'s per-event, per-payload pure-function transform structurally cannot compute. Single-value bucketing (map one field to a coarser bucket) is a legitimate, much smaller idea borrowed from the same literature, but must not be described as delivering k-anonymity's guarantee if it's ever built. |
| SWGDE Video and Audio Redaction Guidelines (M-18-001) | [SWGDE — Video and Audio Redaction Guidelines](https://www.swgde.org/documents/published-complete-listing/18-m-001-video-and-audio-redaction-guidelines/) | `docs/comparisons/streaming-redaction-mechanism.md` — the closest real governing practice document for `ADR-031`'s streaming-channel redaction mechanism, resolved in `ADR-052`; its stated reason for preferring a distinctive tone over silence for audio (ambiguity with genuinely-silent real content) directly informs that comparison's recommendation. Not adopted wholesale — it's forensic-export process guidance for one-time releases, not a per-viewer-claim-gated read-time transform, which is what this design actually needs. |
| ONVIF `PrivacyMask` (Media 2 Service Specification) | [ONVIF — Media 2 Service Specification v16.12](https://www.onvif.org/specs/srv/media/ONVIF-Media2-Service-Spec-v1612.pdf) | `docs/comparisons/streaming-redaction-mechanism.md` — a real, formal spec for live-camera privacy masking, considered and not adopted: it defines a spatial mask (a region within every frame, same for every viewer), whereas `RedactedRange` is temporal (a whole-frame span) and per-viewer-claim-gated — a structurally different problem. |
| SCTE-35 (Digital Program Insertion Cueing Message) | [Wikipedia — SCTE-35](https://en.wikipedia.org/wiki/SCTE-35); [SCTE standards catalog](https://account.scte.org/standards/library/catalog/scte-35-digital-program-insertion-cueing-message/) | `docs/comparisons/streaming-redaction-mechanism.md` — the closest formal precedent for in-band signaling of a span for entitlement-based substitution (broadcast regional blackout); considered and not adopted, since it only standardizes *where* to substitute, leaving *what* to substitute and *who* it applies to entirely to downstream systems — exactly the gap this design still has to fill itself. |
| Differential privacy — additive noise mechanisms | Dwork, McSherry, Nissim, Smith, ["Calibrating Noise to Sensitivity in Private Data Analysis,"](https://link.springer.com/chapter/10.1007/11681878_14) *TCC 2006* | `docs/comparisons/streaming-redaction-mechanism.md` — the formal basis for the statistical-noise-substitution option considered for `RawScalar`/`RawBinary` telemetry redaction; not adopted as the core-engine default (zero-fill is, instead), since calibrating noise to a channel's real distribution requires exactly the domain-specific knowledge `ADR-030`/`ADR-031` keep out of the core engine — left available as an opt-in application-level `Derived`-channel transform if a real need surfaces. |
