<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\EventStore.Spiffe\EventStore.Spiffe.csproj" />
    <ProjectReference Include="..\EventStore.Host.Core\EventStore.Host.Core.csproj" />
    <ProjectReference Include="..\EventStore.ServiceDefaults\EventStore.ServiceDefaults.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Yarp.ReverseProxy" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- ADR-062: Microsoft.NET.Sdk.Web defaults IsPackable to false, but
         this is one of the ADR's "and so on" projects, not provider
         glue, not a sample, overridden back to true like every other
         published package. -->
    <IsPackable>true</IsPackable>
  </PropertyGroup>

</Project>
